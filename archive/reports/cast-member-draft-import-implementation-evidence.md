# Cast Member draft import implementation evidence

Status: working implementation evidence for GitHub issues #117-#121. This report records fixed-point inputs, pre-red decisions, red/green chronology, and the pre-review audit. Terminal reviewed-HEAD and final-SHA evidence belongs in the external tracker closeout sink, not in this tracked report.

## Fixed-point scope

- Exact issue snapshot: `/tmp/continuity-loom-issues-117-121.json`, captured from GitHub before implementation.
- Authoritative acceptance manifest: `/tmp/continuity-loom-117-acceptance-manifest.json`, generated from that snapshot; 61 checks total (#117: 24, #118: 8, #119: 11, #120: 13, #121: 5).
- Working audit scaffold: `/tmp/continuity-loom-117-working-audit.md`.
- Starting commit: `10e1c19f9cdfe4b460ac1c9949f033499bf7a720` on `main`, equal to `origin/main` at intake.
- Approved FOUNDATIONS amendment: exact wording and explicit 2026-07-20 user sign-off are preserved in PRD #117 comment `https://github.com/joeloverbeck/continuity-loom/issues/117#issuecomment-5017616209`; exact-read verification matched all 3,521 local and remote bytes before the amendment edit.
- Unrelated dirty files, excluded from every implementation stage: `.claude/skills/to-issues/SKILL.md`, `.claude/skills/to-issues/references/publication-protocol.md`, `.claude/skills/to-prd/SKILL.md`, `.claude/skills/to-prd/references/source-durability.md`.

## TDD preflight

- CONTEXT.md status: absent; `docs/agents/domain.md` says to proceed silently when it does not exist.
- ADRs/principles/docs status: read `docs/ACTIVE-DOCS.md`, all of `docs/FOUNDATIONS.md`, all of `docs/story-record-schema.md`, `docs/archival-workflow.md`, `docs/agents/domain.md`, `docs/adr/0001-version-playtest-prep-contracts.md`, and the then-active reports now archived unchanged at `archive/reports/cast-member-draft-import-prd-prep.md` and `archive/reports/cast-member-record-field-guide.md`; initial review found the imported prefill conflicted with then-current §§26.1 and 29.2, and the user explicitly approved the exact narrow amendment recorded on PRD #117 before the amendment edit; the sole ADR is unrelated, and the constitution's own amendment procedure is the decision record.
- Pre-red evidence reference: durable file-backed ledger file `archive/reports/cast-member-draft-import-implementation-evidence.md` at the `TDD preflight` heading; file line order proves the preflight and compact table precede the first red command in `Command chronology` row 1.
- Agreed seams: PRD-ratified primary `@loom/core` public template and pure parse/map/report functions; secondary existing `@loom/web` Cast Member editor component; production-route browser path; no server seam.
- Authoritative acceptance manifest: `/tmp/continuity-loom-117-acceptance-manifest.json`, generated from the saved exact issue snapshot and reused for TDD, review, and closeout.
- Row plan: #118 and #121 no-runnable document/archival conformance; #119 core-template and web-copy red-first seams plus browser evidence-only; #120 core-import and web-import red-first seams plus browser evidence-only; #117 aggregate manifest and child-state evidence-only.
- Acceptance atom map: all rows in the acceptance audit below list authoritative atoms and proof surfaces; every manifest check is keyed in `Acceptance atom and sequence map`.
- Acceptance sequence map: all rows list ordered proof or a justified sequence N/A; sequence-sensitive checks are named explicitly in `Sequence-sensitive checks`.
- Proof server plan: configured dev ports 5173/5174 and production port 4173 were verified free at preflight; final proof will use a proof-owned production server on `127.0.0.1:4173`, record PID/command, probe the current app, use a clean Playwright CLI session, record console/network state, and stop only the proof-owned process/session.
- Shared-boundary table stub: created below; every in-scope issue and agreed seam has a row.
- Existing expectation rewrite checkpoint: none yet; add an `existing contract-change expectation` row immediately if any existing expected value is rewritten after a failing command.
- User confirmation needed: no outstanding confirmation; the PRD and child issues ratified the core, editor-component, and no-server seams, and the user explicitly approved the exact §26.1/§26.2/§29.2 wording recorded on PRD #117 before the amendment edit.

## Acceptance atom and sequence map

### Parent #117

- `Parent-Solution`: atoms: complete static copy prompt, tolerant local paste-back, partial prefill, three-band report, review-before-save, no provider call, no residue; proof surfaces: #118 doc, #119 core/web/browser, #120 core/web/browser, #121 archive; sequence: copy out -> external interaction outside app -> paste back -> review -> explicit save, observed in component and browser evidence.
- `US1`: atoms: editor copy button, complete prompt, one clipboard write; proof surfaces: #119 component and browser; sequence: activate copy -> clipboard receives current constant -> success feedback.
- `US2`: atoms: purpose of every schema leaf including five sample-utterance leaves; proof surfaces: #118 doc plus #119 drift/doc conformance; sequence: N/A because the criterion is not sequence-sensitive.
- `US3`: atoms: evidence-first drafting, character-fitting invention when evidence is absent, memorability aim, declaration of invented fields; proof surfaces: #118 doc/template tests; sequence: N/A because the criterion is not sequence-sensitive.
- `US4`: atoms: exactly one fenced JSON object, exact payload names, uncertainties, invented-fields list; proof surfaces: #118 doc/template and #120 parser fixtures; sequence: N/A because the criterion is not sequence-sensitive.
- `US5`: atoms: no `entity_id` output, injected `entity_id` stripped/reported, form link preserved; proof surfaces: #118 contract, #120 core/component/browser; sequence: parse injected link -> skip/report -> merge while retaining current link.
- `US6`: atoms: no project records, story configuration, or user data in copied text; proof surfaces: #118 static doc/template plus #119 clipboard assertion; sequence: N/A because the criterion is not sequence-sensitive.
- `US7`: atoms: Cast Member editor import entry point and paste surface; proof surfaces: #120 component and browser; sequence: open editor -> open import surface -> paste.
- `US8`: atoms: fenced envelope and prose-wrapped object; proof surfaces: #120 core fixtures; sequence: input envelope -> deterministic extraction -> JSON parse.
- `US9`: atoms: valid siblings imported despite invalid field; proof surfaces: #120 core partial-valid fixture and web merge assertion; sequence: parse -> validate independently -> retain valid values.
- `US10`: atoms: every rejected payload path named with category-specific reason; proof surfaces: #120 core report fixtures and rendered skipped band; sequence: validate -> accumulate exact skips -> render.
- `US11`: atoms: distinct filled, skipped, needs-author bands; proof surfaces: #120 report function, component, browser; sequence: import -> apply -> render all three bands.
- `US12`: atoms: uncertainties and invented field paths shown as review-only material; proof surfaces: #120 report/component/browser; sequence: parse metadata -> exclude from payload -> render needs-author band.
- `US13`: atoms: standalone create link and linked-flow pre-bound link preserved; proof surfaces: #120 component tests and linked production route; sequence: pre-bind/select entity -> import -> same entity remains.
- `US14`: atoms: import available in edit mode and can update present fields; proof surfaces: #120 edit-mode component and browser; sequence: open saved dossier -> import -> confirm changes.
- `US15`: atoms: detect only differing non-empty targets, list exact paths, block overwrite before confirmation, cancel unchanged; proof surfaces: #120 component and browser; sequence: review import -> confirmation appears before reset -> cancel unchanged or confirm applies.
- `US16`: atoms: paste-absent leaves and groups remain unchanged; proof surfaces: #120 merge component assertion; sequence: snapshot current form -> merge present paths only -> inspect absent path.
- `US17`: atoms: import performs no create/update, imported state labeled unsaved, explicit Save is the only write; proof surfaces: #120 component API mocks and browser; sequence: import -> verify no write -> review -> explicit Save -> write.
- `US18`: atoms: paste/report/draft only mounted state, cancel unchanged, discard restores pre-import state, navigation/unmount leaves no browser/project residue; proof surfaces: #120 component storage spies/unmount and browser; sequence: import/cancel or import/discard or navigate -> remount -> no assistance scratch.
- `US19`: atoms: copy and import paths have no provider/network call; proof surfaces: #119/#120 component API assertions and browser request inventory; sequence: copy/import actions -> inspect requests -> no provider/server request from either action.
- `US20`: atoms: independent version identity, schema-derived leaf inventory, missing-leaf failure, doc/core byte match; proof surfaces: #119 core tests; sequence: N/A because the criterion is not sequence-sensitive.
- `Parent-Implementation-Decisions`: atoms: (1) record-free clipboard round trip with no provider/server, (2) self-contained complete prompt and strict output contract, (3) character-fitting invention flagged for review, (4) independently versioned core ownership plus schema drift protection, (5) pure tolerant core parse/map/report, (6) create/linked/edit editor controls with present-only merge/link preservation/pre-overwrite confirmation, (7) ephemeral three-band report with every named skip and needs-author class, (8) opt-in/provenance-visible/residue-free unsaved prefill under the exact approved §26.2 exception, (9) registered prompt authority plus field-guide archival, and (10) deliberately amended constitution with compiler/schema/source-profile footprint unchanged; proof surfaces: #118-#121 audit rows, approved PRD #117 amendment comment, final diff, focused tests, production browser, and canonical gates; sequence: #118 authority -> #119 copy and #120 import -> approved constitutional amendment -> #121 archival supersession.
- `Parent-Testing-Decisions`: atoms: external-behavior assertions; core public prompt/drift/parse/map/report seams with valid, partially valid, malformed, bad-enum, unregistered-key, empty, injected-link, and prose-wrapped fixtures; web editor copy/dialog/confirmation/report/entity/accessibility seam in standalone, linked, and edit modes; prior-art suite retention; real production browser; no server product seam; ratified core/web seam confirmation; proof surfaces: shared-boundary TDD table, `packages/core/test/cast-member-draft-template.test.ts`, `packages/core/test/cast-member-draft-import.test.ts`, `packages/web/src/records/CastMemberEditor.test.tsx`, 174-file root suite, and production browser evidence; sequence: preflight -> intended red at each changed seam -> focused green -> root gates -> production browser freshness.
- `Principles`: atoms: user-owned continuity with explicit Save/Create as the authority gate, static record-free copy, exact approved §26.2 unsaved-prefill exception, opt-in/provenance-visible/residue-free import, core purity, no compiler/schema/provider/server change, registered doc authority; proof surfaces: approved PRD #117 amendment comment, `docs/FOUNDATIONS.md`, component/browser behavior, final diff; sequence: exact wording and sign-off -> same-revision constitutional and dependent behavior change -> final conformance review.

### Child #118

- `AC1`: atoms: role framing, all per-field semantics, extraction/invention rules, strict output contract, one self-contained paste; proof surfaces: prompt authority document; sequence: N/A because the criterion is not sequence-sensitive.
- `AC2`: atoms: required core, optional extended, and sample-utterance fields `text`, `situation`, `speech_function`, `pressure_tags`, and `copy_policy`; proof surfaces: doc field reference plus schema-derived drift test; sequence: N/A because the criterion is not sequence-sensitive.
- `AC3`: atoms: invent when dossier evidence is absent, fit established character, increase memorability, list substantially invented fields; proof surfaces: doc/template assertions; sequence: N/A because the criterion is not sequence-sensitive.
- `AC4`: atoms: exactly one fenced JSON object, exact schema names, no `entity_id`, no unregistered keys, `uncertainties`, `invented_fields`; proof surfaces: doc/template assertions and parser contract; sequence: N/A because the criterion is not sequence-sensitive.
- `AC5`: atoms: zero project/story/user data and explicit static verbatim versioned copy statement; proof surfaces: doc inspection and clipboard exactness test; sequence: N/A because the criterion is not sequence-sensitive.
- `AC6`: atoms: same-change ACTIVE-DOCS row and domain-authority wording; proof surfaces: registry diff/static inspection; sequence: N/A because the criterion is not sequence-sensitive.
- `AC7`: atoms: lint, typecheck, test, build; proof surfaces: final command ledger; sequence: final tree -> four canonical commands.
- `Principles`: atoms: FOUNDATIONS §§9/9.1 and 26/26.1, ACTIVE-DOCS registration, no amendment/ADR exception; proof surfaces: final diff and authority audit; sequence: N/A because conformance is evaluated on the final tree.

### Child #119

- `AC1`: atoms: deterministic constant, independent version, public core exports, prose/compiler/contract versions unchanged; proof surfaces: core test and final diff; sequence: N/A because the criterion is not sequence-sensitive.
- `AC2`: atoms: schema-derived every-leaf inventory including five sample leaves, deliberate missing-leaf failure sensitivity, core/doc byte equality; proof surfaces: focused core test; sequence: remove/probe leaf -> failure, restore -> green (mutation sensitivity or equivalent deliberate negative probe recorded).
- `AC3`: atoms: create, linked create, edit availability whenever editor open; proof surfaces: component modes plus routed browser; sequence: navigate each mode -> observe same control.
- `AC4`: atoms: complete current bytes, one clipboard write, success feedback, no stale copy; proof surfaces: component clipboard mock and browser clipboard read; sequence: click -> write current constant -> success.
- `AC5`: atoms: named clipboard failure, success cleared on failure, retry path; proof surfaces: component failure/retry; sequence: success or failure -> failure alert with no success -> retry -> success.
- `AC6`: atoms: record-free template, template-not-canon/prose label, no `entity_id` or unregistered keys instruction; proof surfaces: doc/template static checks and UI copy-boundary text; sequence: N/A because the criterion is not sequence-sensitive.
- `AC7`: atoms: explicit user click, local clipboard only, no provider/network call; proof surfaces: component API mocks and browser request inventory; sequence: click -> clipboard write only.
- `AC8`: atoms: no project store, working set, prompt participation, browser storage, backup/migration/export/provenance copy state; proof surfaces: component storage/API spies and final source diff; sequence: copy -> inspect all write/storage spies unchanged.
- `AC9`: atoms: create/edit/linked modes, keyboard activation, accessible control and feedback names; proof surfaces: web component tests; sequence: focus/keyboard activate -> accessible feedback.
- `AC10`: atoms: lint, typecheck, test, build; proof surfaces: final command ledger; sequence: final tree -> four canonical commands.
- `Principles`: atoms: static record-free copy, opt-in, residue-free, platform-free core, no compatibility/server/schema path; proof surfaces: source audit/tests; sequence: N/A because conformance is evaluated on the final tree.

### Child #120

- `AC1`: atoms: three public pure functions, fenced JSON, prose-wrapped balanced object, deterministic parse failure; proof surfaces: core fixtures; sequence: extract -> parse -> map -> report.
- `AC2`: atoms: registered-schema leaf validation, valid sibling retention, invalid enum, unregistered key, empty value, malformed list entry, protected `entity_id`, exact skip paths/reasons; proof surfaces: core table fixtures; sequence: traverse each present path -> validate independently -> import or skip/report.
- `AC3`: atoms: create, linked create, edit control and accessible paste surface; proof surfaces: component modes and browser; sequence: open editor -> activate Import -> dialog.
- `AC4`: atoms: present-only deep merge, absent preservation, standalone and pre-bound `entity_id` preservation; proof surfaces: component tests; sequence: snapshot form -> import -> merge present leaves -> inspect retained values/link.
- `AC5`: atoms: detect exact differing non-empty targets, confirmation before overwrite, exact path list, cancel unchanged; proof surfaces: component/browser; sequence: parse/review -> alertdialog -> cancel unchanged or confirm then reset.
- `AC6`: atoms: filled band, per-skip path/reason band, needs-author `entity_id` plus uncertainties plus invented fields; proof surfaces: core report, component DOM, browser; sequence: apply import -> render all bands.
- `AC7`: atoms: parse error, zero imported fields, editable retry, named per-field warnings, cancel/failure unchanged; proof surfaces: core/component/browser; sequence: bad paste -> alert/no reset -> correct -> retry; cancel -> unchanged.
- `AC8`: atoms: unsaved/non-canon label, zero automatic create/update, explicit save only, uncertainty/invention review labels; proof surfaces: component API mocks and browser; sequence: import -> review -> no write -> explicit Save -> write.
- `AC9`: atoms: local import path and zero network/provider calls; proof surfaces: component API mocks/browser requests; sequence: import actions -> no request.
- `AC10`: atoms: no project-store/working-set/prompt/browser storage for paste/report/draft, discard/cancel/unmount cleanup, explicit saved record is sole durable result; proof surfaces: storage/API spies, unmount/remount component, browser; sequence: assistance state -> cancel/discard/navigate -> no retained scratch; alternate explicit Save -> canonical record only.
- `AC11`: atoms: both editor modes plus linked flow, entity preservation, overwrite alertdialog, accessible names, keyboard operation for dialog/report/confirmation; proof surfaces: component tests and browser snapshot; sequence: keyboard open -> paste/review -> keyboard cancel/confirm -> accessible report.
- `AC12`: atoms: lint, typecheck, test, build; proof surfaces: final command ledger; sequence: final tree -> four canonical commands.
- `Principles`: atoms: opt-in per invocation, exact §26.2 unsaved-prefill exception, provenance-visible, residue-free, explicit Save/Create as the sole record-write gate, platform-free core, no server/schema/compatibility path; proof surfaces: approved PRD #117 amendment comment, `docs/FOUNDATIONS.md`, final source audit, tests, browser; sequence: exact wording and sign-off -> same-revision amendment and behavior -> final conformance review.

### Child #121

- `AC1`: atoms: tracked report moved to `archive/reports/`, original absent, filename preserved; proof surfaces: git diff/path inspection; sequence: #119/#120 green -> archive move.
- `AC2`: atoms: no active operational reference to old guide, historical references either archived or path-correct, inventory manifests classified non-operational; proof surfaces: targeted `rg` and archive inspection; sequence: archive -> active-reference sweep.
- `AC3`: atoms: new doc is sole active operational authority and invention policy bytes unchanged through archival; proof surfaces: ACTIVE-DOCS/doc inspection and diff; sequence: doc ships -> guide archives -> authority recheck.
- `AC4`: atoms: lint, typecheck, test, build; proof surfaces: final command ledger; sequence: final tree -> four canonical commands.
- `Principles`: atoms: archival destination/post-archive rules and no duplicate authority path; proof surfaces: final path/reference audit; sequence: N/A because conformance is evaluated on the final tree.

### Sequence-sensitive checks

The explicit ordered proofs above govern #117 `Parent-Solution`, `US1`, `US5`, `US7`-`US19`, `Parent-Implementation-Decisions`, and `Parent-Testing-Decisions`; #118 `AC7`; #119 `AC2`-`AC5`, `AC7`-`AC10`; #120 `AC1`-`AC12`; and #121 `AC1`-`AC4`. Every other keyed check is non-sequence-sensitive for the stated reason.

## Review-fix preflight

- Initial Standards outcome: 0 findings; worst severity none; reviewer `/root/standards_review` (Nietzsche), terminal completed.
- Initial Spec outcome: 3 findings; worst severity high; reviewer `/root/spec_review` (Epicurus), terminal completed after bounded interrupt-and-synthesis recovery.
- `P1-spec-1` / `RF-1` / conformance-only: the prefill behavior contradicted then-current §§26.1 and 29.2. The user clarified that eliminating independent entry is the feature's purpose, then explicitly approved the exact narrow constitutional exception preserved on PRD #117. Red-first skipped because this is a conformance-only amendment that deliberately preserves the ratified behavior.
- `P1-spec-2` / `RF-2` / behavior: a second import can replace the provenance report and discard baseline while retaining values from the first import. Intended red seam: `packages/web/src/records/CastMemberEditor.test.tsx`, proving another import cannot begin while the current imported draft and provenance report remain active; green must also prove discard restores the original baseline and re-enables import.
- `P1-spec-3` / `RF-3` / behavior: overlapping clipboard writes can let an older settlement replace the newest attempt's feedback. Intended red seam: `packages/web/src/records/CastMemberEditor.test.tsx`, proving only the newest invocation owns visible feedback for both stale-success and stale-failure orderings.
- Review-fix pre-red evidence reference: this heading and the three keyed compact-table rows below precede the first RF-2/RF-3 red command; later chronology rows preserve exact command order.
- Existing expectation rewrite checkpoint: none; RF-2 and RF-3 add new external-behavior assertions without rewriting an existing expected value.

## Shared-boundary TDD table

| Issue | CONTEXT.md status | ADRs/principles/docs status | Seam | Red command/failure | Green command or evidence | Acceptance covered | Review fix / red-first skip reason |
|---|---|---|---|---|---|---|---|
| #117 | absent | authorities read; no ADR conflict | evidence-only aggregate | N/A because the parent PRD maps the independently tested and audited #118-#121 child seams rather than adding a separate runnable product seam | 61-row audit in this report passed implement review-entry validation; #118-#121 rows below are green; first root gates and production browser scenario passed; final child-state evidence remains a tracker-closeout action rather than implementation review evidence | Parent-Solution, US1-US20, Parent-Implementation-Decisions, Parent-Testing-Decisions, and Principles; atoms: every parent atom is expanded in the keyed acceptance map and exact 61-row audit; proof surfaces: `docs/cast-member-draft-prompt-template.md`, core template/import tests, `packages/web/src/records/CastMemberEditor.test.tsx`, production browser section, archive paths, root commands, and later tracker readback; sequence: authority #118 -> copy #119 and import #120 -> archive #121 -> root verification -> production proof -> later child-state readback before parent close | red-first N/A because parent aggregation has no independent runnable product seam |
| #118 | absent | authorities read; no ADR conflict | no-runnable doc conformance | N/A because authoring a domain-authority document and registry entry has no independent runnable product behavior seam | `packages/core/test/cast-member-draft-template.test.ts` passed 3 tests, including doc/core byte equality and schema-derived leaf coverage; the 61-row audit passed; first root gates passed | AC1-AC7 and Principles; atoms: complete self-contained static prompt authority, every schema leaf, invention/output/privacy rules, same-change registry, and four root gates; proof surfaces: `docs/cast-member-draft-prompt-template.md`, `docs/ACTIVE-DOCS.md`, `packages/core/test/cast-member-draft-template.test.ts`, and root commands; sequence: document and registry land together -> conformance test -> root gates | red-first N/A because the issue is document-authority authoring |
| #119 | absent | authorities read; no ADR conflict | red-first core template | `npx vitest run packages/core/test/cast-member-draft-template.test.ts` -> intended red, 1 file / 3 tests failed because both public exports were `undefined` before implementation | same command -> 1 file / 3 tests passed | AC1-AC2; atoms: independent version, deterministic bytes, schema leaves, doc equality; proof surfaces: core public test; sequence: missing public seam -> implementation -> green, with in-test missing-leaf sensitivity probe | N/A |
| #119 | absent | authorities read; no ADR conflict | red-first web copy | `npx vitest run packages/web/src/records/CastMemberEditor.test.tsx` -> intended red, 1 file / 3 new tests failed while 7 existing tests passed because the accessible copy control was absent | same command -> 1 file / 10 tests passed | AC3-AC9; atoms: all modes, exact clipboard, truthful feedback/retry, local/no-residue/a11y; proof surfaces: editor component; sequence: click/fail/retry and storage/API observations | N/A |
| #119 | absent | authorities read; no ADR conflict | evidence-only browser | N/A because real-browser proof supplements the red-first component seam rather than adding a separate production implementation seam | production `127.0.0.1:4173/records?type=CAST+MEMBER` edit route in clean Playwright session: keyboard-accessible control present; copy status truthful; clipboard length 12,861 and SHA-256 `4777c38553b711cdffb79ae5e40e419fb937d5a9d84b45b7a1219f0716ea8d97` exactly matched the built core constant; request list unchanged | AC3-AC9; atoms: routed editor copy action, exact current clipboard bytes, truthful status, zero provider/network/storage residue, accessible native control; proof surfaces: production DOM, clipboard digest matched to built core, before/after requests, empty browser storage, clean console; sequence: same active edit instance -> activate copy -> observe status -> hash clipboard -> compare built constant -> verify request/storage state | red-first N/A because real-browser proof supplements automated seams |
| #120 | absent | authorities read; no ADR conflict | red-first core import | `npx vitest run packages/core/test/cast-member-draft-import.test.ts` -> intended red, 1 file / 8 tests failed because all three public pure functions were absent | same command -> 1 file / 8 tests passed; `npm run typecheck --workspace @loom/core` passed after the Zod adapter correction | AC1-AC2, AC6 metadata; atoms: envelopes, tolerant registered-schema mapping, exact skips, three-band report data; proof surfaces: core public fixtures; sequence: parse -> map -> report | N/A |
| #120 | absent | authorities read; no ADR conflict | red-first web import | `npx vitest run packages/web/src/records/CastMemberEditor.test.tsx` -> intended red, 1 file / 4 new tests failed while 10 existing tests passed because the import control was absent | same command -> 1 file / 14 tests passed | AC3-AC11; atoms: modes, merge/link, confirm/cancel, report, error/retry, unsaved/no-network/no-residue/a11y; proof surfaces: editor component; sequence: paste -> review -> confirm/apply or cancel -> optional save/discard | N/A |
| #120 | absent | authorities read; no ADR conflict | evidence-only browser | N/A because real-browser proof supplements the red-first core/component seams rather than adding a separate production implementation seam | clean production session opened import by keyboard, kept invalid paste editable with form unchanged, retried valid adversarial paste, listed only `identity.one_line` before overwrite, cancelled by Escape unchanged, confirmed by Enter, rendered all three bands, preserved linked ENTITY and absent `identity.public_face`, added no request/storage, restored on discard, and remounted with no assistance state; console 0 errors / 0 warnings | AC3-AC11; atoms: routed create/edit-capable import surface, parse retry, exact pre-overwrite confirmation/cancel, present-only merge/link preservation, three-band provenance report, unsaved/no-request/no-storage boundary, discard/remount cleanup, accessible keyboard actions; proof surfaces: production DOM/value observations, loaded record response, identical request inventories, empty local/session storage, clean console, retained snapshot artifacts; sequence: same active edit instance -> keyboard open -> invalid review/no change -> corrected review -> alert before change -> Escape/no change -> retry -> Enter/apply -> inspect report/form/network/storage -> discard -> reload/remount -> inspect no residue | red-first N/A because real-browser proof supplements automated seams |
| #117/#120 | absent | exact user-approved §26.1/§26.2/§29.2 amendment recorded on PRD #117; sole ADR unrelated | no-runnable conformance review fix | N/A because RF-1 is a conformance-only constitutional amendment that deliberately preserves the ratified prefill behavior | TBD: exact approved wording in `docs/FOUNDATIONS.md`, same revision as dependent behavior, followed by final Spec re-review | #117 Parent-Implementation-Decisions and Principles; #120 Principles; atoms: narrow CAST MEMBER-only unsaved prefill, local user-invoked flow, visible field provenance, exact overwrite confirmation, residue-free discard, explicit Save/Create authority gate, all other independent-authorship boundaries retained; proof surfaces: PRD #117 amendment comment, `docs/FOUNDATIONS.md`, final diff, final Spec review; sequence: exact wording shown -> explicit user approval -> tracker record exact-read -> same-revision amendment -> final review | RF-1; red-first skipped because conformance-only fix did not change behavior |
| #120 | absent | approved §26.2 provenance-until-discard-or-save rule | red-first web import review fix | first red: `npx vitest run packages/web/src/records/CastMemberEditor.test.tsx` -> 1 of 15 failed because the active imported draft left Import enabled; second intended red after the first green: same command -> 1 of 16 failed because a successful Save left the provenance report mounted, while 15 passed | first command cycle -> 15 tests passed after guarding the import entry point through discard; final same command -> 1 file / 16 tests passed, including successful Save and Create clearing the session while discard restores baseline | #117 US11, US12, US18 and #120 AC6, AC10, Principles; atoms: current import provenance remains visible, no retained first-import field can outlive its report/discard baseline, discard restores the pre-import form, import becomes available after discard or successful Save/Create; proof surfaces: editor component and final production browser; sequence: first import -> report/form active -> second-import attempt blocked -> discard restores baseline or successful Save/Create clears provenance -> import enabled | RF-2 |
| #119 | absent | approved local clipboard boundary; no ADR conflict | red-first web copy review fix | after one unexpected green from an insufficient observer, tightened `npx vitest run packages/web/src/records/CastMemberEditor.test.tsx` -> intended red: 1 of 17 tests failed because older success removed the newer failure alert after its deferred settlement; the other 16 passed | same command -> 1 file / 17 tests passed; the retained test observes both older-success/newer-failure and older-failure/newer-success settlement orderings | #119 AC4-AC5; atoms: complete current clipboard write, newest-attempt-owned success/failure, older success cannot replace newer failure, older failure cannot replace newer success, retry remains available; proof surfaces: editor component and final production browser; sequence: attempt A remains in flight -> attempt B settles -> A settles -> feedback remains B | RF-3 |
| #121 | absent | authorities read; no ADR conflict | no-runnable archival conformance | N/A because moving superseded reports and classifying references has no independent runnable product behavior seam | tracked field guide and completed PRD-prep report moved unchanged to `archive/reports/`; original paths absent; targeted `rg` sweep leaves only dated repository inventory manifests, classified as non-operational historical path inventories; first root gates passed | AC1-AC4 and Principles; atoms: archive destination and original absence, active-reference cleanup with inventory classification, sole operational prompt authority with unchanged invention policy, and four root gates; proof surfaces: `archive/reports/cast-member-record-field-guide.md`, `archive/reports/cast-member-draft-import-prd-prep.md`, `docs/cast-member-draft-prompt-template.md`, `docs/ACTIVE-DOCS.md`, targeted `rg`, and root commands; sequence: #119/#120 green -> archive move -> active-reference audit -> root gates | red-first N/A because archival has no runnable behavior seam |

## Command chronology

1. #119 core-template RED: `npx vitest run packages/core/test/cast-member-draft-template.test.ts` -> intended failure, 1 test file failed and all 3 tests failed because `CAST_MEMBER_DRAFT_PROMPT` and `CAST_MEMBER_DRAFT_PROMPT_VERSION` were `undefined`; no setup or unrelated assertion failed.
2. #119 core-template GREEN: `npx vitest run packages/core/test/cast-member-draft-template.test.ts` -> 1 file passed, 3 tests passed after adding the platform-free static template/version and public exports.
3. #119 web-copy setup: `npm run build --workspace @loom/core` -> passed; consumer-resolved `@loom/core` output now includes the new public template/version exports before the web red.
4. #119 web-copy red planned: `npx vitest run packages/web/src/records/CastMemberEditor.test.tsx`; expected failure: create, linked-create, and edit editors do not yet expose the copy control or its feedback states.
5. #119 web-copy RED: `npx vitest run packages/web/src/records/CastMemberEditor.test.tsx` -> intended failure, 1 file failed; exactly 3 new tests failed because the accessible copy control was absent, while all 7 pre-existing editor tests passed.
6. #119 web-copy GREEN: `npx vitest run packages/web/src/records/CastMemberEditor.test.tsx` -> 1 file passed, all 10 tests passed after adding editor-local clipboard state/control.
7. #120 core-import red planned: `npx vitest run packages/core/test/cast-member-draft-import.test.ts`; expected failure: the public pure parse/map/report functions do not exist yet.
8. #120 core-import RED: `npx vitest run packages/core/test/cast-member-draft-import.test.ts` -> intended failure, 1 file failed and all 8 tests failed because the parse/map/report functions were absent; the registered-schema setup was available and no unrelated test ran.
9. #120 core-import behavior GREEN: `npx vitest run packages/core/test/cast-member-draft-import.test.ts` -> 1 file passed, all 8 tests passed after adding pure envelope parsing, registered-schema leaf mapping, and report construction.
10. #120 focused core typecheck: `npm run typecheck --workspace @loom/core` -> failed before test compilation with three Zod v4 type-adapter errors in `src/cast-member-draft/import.ts`; product tests had already executed green, so this was a compile-time implementation defect rather than behavior evidence.
11. #120 core-import corrected GREEN: `npx vitest run packages/core/test/cast-member-draft-import.test.ts` -> 1 file passed, all 8 tests passed after adapting traversal to the Zod v4 classic-schema surface.
12. #120 focused core typecheck corrected: `npm run typecheck --workspace @loom/core` -> passed with zero errors.
13. #120 web-import setup: `npm run build --workspace @loom/core` -> passed; consumer-resolved `@loom/core` output includes the public parse/map/report exports before the web red.
14. #120 web-import RED: `npx vitest run packages/web/src/records/CastMemberEditor.test.tsx` -> intended failure, 1 file failed; exactly 4 new tests failed because the accessible import control was absent, while all 10 pre-existing editor tests passed.
15. #120 web-import GREEN: `npx vitest run packages/web/src/records/CastMemberEditor.test.tsx` -> 1 file passed, all 14 tests passed after adding present-only merge, protected-link preservation, pre-overwrite confirmation, local retry/report/discard state, and explicit-save behavior.
16. #120 focused web typecheck: `npm run typecheck --workspace @loom/web` -> passed with zero errors.
17. Focused lint: `npx eslint packages/web/src/records/CastMemberEditor.tsx packages/web/src/records/CastMemberEditor.test.tsx packages/core/src/cast-member-draft/import.ts packages/core/src/cast-member-draft/template.ts packages/core/test/cast-member-draft-import.test.ts packages/core/test/cast-member-draft-template.test.ts` -> failed with three type-safety diagnostics: one unnecessary assertion and two unsafe assignments; no behavior expectation changed.
18. Focused lint correction verification: the same lint surfaces passed with zero diagnostics; `npx vitest run packages/core/test/cast-member-draft-import.test.ts packages/core/test/cast-member-draft-template.test.ts packages/web/src/records/CastMemberEditor.test.tsx` passed 3 files / 25 tests; corrected core and web typechecks passed.
19. #121 archive move and reference sweep: tracked `reports/cast-member-record-field-guide.md` and completed `reports/cast-member-draft-import-prd-prep.md` moved unchanged to `archive/reports/`; both original paths are absent. The only remaining old field-guide path outside archive is in `reports/manifest_2026-06-22_90d17f8.txt` and `reports/manifest_2026-06-24_c04bcf9.txt`, which are dated repository inventories rather than active operational guidance.
20. First complete-tree canonical gate: `npm run lint` and `npm run typecheck` passed; `npm test` passed 174 files / 1,914 tests; `npm run build` passed, with Vite's bundle-size advisory and no build error.
21. Initial production browser evidence: proof-owned `npm start` rebuilt the tree and served only `127.0.0.1:4173`; clean session `cl117-clean` completed the pre-review copy/import/discard/remount scenario with clipboard digest equality, unchanged request inventory for assistance actions, empty browser storage, and 0 console errors / 0 warnings. This evidence was later superseded by row 33 because RF-2 and RF-3 changed browser code.
22. Pre-review internal cleanup: removed the unused `mapping` snapshot property from `PreparedDraftImport`; this does not change merge, render, route, API, fixture, or action behavior. `npx vitest run packages/web/src/records/CastMemberEditor.test.tsx` passed 1 file / 14 tests, focused ESLint passed, and `npm run typecheck --workspace @loom/web` passed.
23. RF-2 web-import session RED: `npx vitest run packages/web/src/records/CastMemberEditor.test.tsx` -> intended failure, 1 file failed; 1 of 15 tests failed because the active imported draft left **Import Cast Member draft** enabled (`false` instead of `true`), while the other 14 tests passed.
24. RF-2 web-import session first GREEN: the same command -> 1 file passed / 15 tests passed after the active report disabled and guarded the import entry point until discard, and discard restored the original form baseline and re-enabled import.
25. RF-2 successful Save/Create resolution RED: the same command -> intended failure, 1 file failed; 1 of 16 tests failed because the report remained mounted after a successful Save response, while the other 15 passed. The Create half did not execute after that first intended assertion failure and remains part of the same retained tracer for green.
26. RF-2 successful Save/Create resolution GREEN: the same command -> 1 file passed / 16 tests passed after the import session clears only on successful persistence responses; the retained tracer observed both Save and Create, while failed persistence behavior remains governed by the unchanged error branch.
27. RF-3 first assertion run: `npx vitest run packages/web/src/records/CastMemberEditor.test.tsx` -> unexpected green, 1 file / 17 tests passed. This is not behavior red or coverage: the assertion observed the newest status before React processed the older deferred promise's settlement. The test must await that settlement inside React's update boundary before it can expose the stale-feedback defect.
28. RF-3 web-copy ordering RED: the tightened same command -> intended failure, 1 file failed; 1 of 17 tests failed because the older successful promise removed the newer failure alert after the test awaited React processing that settlement, while the other 16 passed.
29. RF-3 web-copy ordering GREEN: the same command -> 1 file passed / 17 tests passed after visible feedback became owned by a monotonically keyed newest clipboard attempt; the retained test exercised both stale-success and stale-failure settlement orderings.
30. Review-fix focused hygiene: `npx eslint packages/web/src/records/CastMemberEditor.tsx packages/web/src/records/CastMemberEditor.test.tsx` -> failed with one unused placeholder parameter in the deferred test helper; `git diff --check` -> failed on trailing Markdown whitespace in the new amendment-history line; parallel `npm run typecheck --workspace @loom/web` passed. Neither failure changed or contradicted the RF-2/RF-3 behavior results.
31. Review-fix focused correction: the same focused ESLint and `git diff --check` passed; `npm run typecheck --workspace @loom/web` passed; `npx vitest run packages/web/src/records/CastMemberEditor.test.tsx` passed 1 file / 17 tests.
32. Complete amended working-tree gates: `npm run lint` passed; `npm run typecheck` passed for all workspaces; `npm test` passed 174 files / 1,917 tests; `npm run build` passed with 463 Vite modules transformed and only the existing bundle-size advisory.
33. Final production browser evidence: proof-owned `npm start` rebuilt the amended tree and served only `127.0.0.1:4173`; clean in-memory session `cl117-reviewfix-clean` completed exact-copy, invalid retry, overwrite, active-session blocking, discard restoration, explicit Save, reopen, and reload proof. Assistance actions caused no record write; the explicit Save caused the sole record `PUT`; browser storage stayed empty; console totals were 0 errors / 0 warnings. The session and proof-owned server were closed, no server/browser process remained, and connection checks confirmed ports 4173/5173/5174 free.

## Verification command ledger

| Exact command | Observed result/counts | Run count | Represented SHA/tree |
|---|---|---:|---|
| `npx vitest run packages/core/test/cast-member-draft-template.test.ts` | expected RED: 1 file failed, 3 tests failed; public prompt/version absent | 1 | working tree after #118 doc and #119 red test, before core template implementation |
| `npx vitest run packages/core/test/cast-member-draft-template.test.ts` | GREEN: 1 file passed, 3 tests passed | 1 | working tree with #118 authority and #119 core template implementation |
| `npm run build --workspace @loom/core` | setup/build passed; TypeScript emitted current core public exports | 1 | working tree before #119 web-copy red |
| `npx vitest run packages/web/src/records/CastMemberEditor.test.tsx` | expected RED: 1 file failed, 3 new tests failed, 7 existing tests passed; copy control absent | 1 | working tree before #119 web-copy implementation |
| `npx vitest run packages/web/src/records/CastMemberEditor.test.tsx` | GREEN: 1 file passed, 10 tests passed | 1 | working tree with #119 web-copy implementation |
| `npx vitest run packages/core/test/cast-member-draft-import.test.ts` | expected RED: 1 file failed, 8 tests failed; public parse/map/report functions absent | 1 | working tree before #120 core-import implementation |
| `npx vitest run packages/core/test/cast-member-draft-import.test.ts` | behavior GREEN: 1 file passed, 8 tests passed | 1 | working tree with initial #120 core-import implementation |
| `npm run typecheck --workspace @loom/core` | FAILED: 3 TypeScript errors in the Zod v4 schema-node adapter | 1 | same initial #120 core-import working tree |
| `npx vitest run packages/core/test/cast-member-draft-import.test.ts` | corrected GREEN: 1 file passed, 8 tests passed | 1 | working tree after the Zod v4 traversal adapter correction |
| `npm run typecheck --workspace @loom/core` | passed with zero errors | 1 | same corrected #120 core-import working tree |
| `npm run build --workspace @loom/core` | setup/build passed; TypeScript emitted current core import exports | 1 | working tree before #120 web-import red |
| `npx vitest run packages/web/src/records/CastMemberEditor.test.tsx` | expected RED: 1 file failed, 4 new tests failed, 10 existing tests passed; import control absent | 1 | working tree before #120 web-import implementation |
| `npx vitest run packages/web/src/records/CastMemberEditor.test.tsx` | GREEN: 1 file passed, 14 tests passed | 1 | working tree with #120 editor-import implementation |
| `npm run typecheck --workspace @loom/web` | passed with zero errors | 1 | same #120 editor-import working tree |
| focused `npx eslint` over the six new/changed core and web implementation/test files | FAILED: 3 type-safety diagnostics, no behavior failures | 1 | initial #120 core/web implementation tree |
| same focused `npx eslint` surfaces after corrections | passed with zero diagnostics | 1 | corrected #120 core/web implementation tree |
| `npx vitest run packages/core/test/cast-member-draft-import.test.ts packages/core/test/cast-member-draft-template.test.ts packages/web/src/records/CastMemberEditor.test.tsx` | 3 files passed, 25 tests passed | 1 | corrected #118-#120 working tree |
| `npm run typecheck --workspace @loom/core && npm run typecheck --workspace @loom/web` | both workspaces passed with zero errors | 1 | corrected #118-#120 working tree |
| path existence and targeted `rg` sweep for archived report paths | original field-guide and prep paths absent; archive copies present; only two dated inventory-manifest references remain outside archive and are non-operational | 1 | #121 archival working tree |
| `npm run lint` | passed; root build prerequisite and all root/workspace ESLint invocations exited 0 | 1 | complete uncommitted implementation tree before review |
| `npm run typecheck` | passed for core, server, and web workspaces | 1 | same complete uncommitted implementation tree |
| `npm test` | passed: 174 test files and 1,914 tests | 1 | same complete uncommitted implementation tree |
| `npm run build` | passed for core, server, and web; Vite transformed 463 modules | 1 | same complete uncommitted implementation tree |
| `npx vitest run packages/web/src/records/CastMemberEditor.test.tsx` | passed: 1 file and 14 tests | 1 | pre-review tree after unused internal snapshot-property cleanup |
| `npx eslint packages/web/src/records/CastMemberEditor.tsx && npm run typecheck --workspace @loom/web` | focused ESLint and web typecheck passed | 1 | same pre-review cleanup tree |
| `npx vitest run packages/web/src/records/CastMemberEditor.test.tsx` | expected RF-2 RED: 1 file failed; 1 of 15 tests failed because a second import remained available during an active import session; 14 tests passed | 1 | review-fix working tree before RF-2 implementation |
| `npx vitest run packages/web/src/records/CastMemberEditor.test.tsx` | RF-2 first GREEN: 1 file and 15 tests passed; active import provenance/baseline blocks a second import until discard | 1 | review-fix working tree after RF-2 import-session guard |
| `npx vitest run packages/web/src/records/CastMemberEditor.test.tsx` | expected RF-2 second RED: 1 file failed; 1 of 16 tests failed because successful Save left the report mounted; 15 tests passed | 1 | review-fix working tree before successful Save/Create clears the import session |
| `npx vitest run packages/web/src/records/CastMemberEditor.test.tsx` | RF-2 final GREEN: 1 file and 16 tests passed; both successful Save and Create clear provenance and re-enable import | 1 | review-fix working tree after RF-2 complete implementation |
| `npx vitest run packages/web/src/records/CastMemberEditor.test.tsx` | RF-3 unexpected green: 1 file / 17 tests passed because the new assertion did not await React processing the stale deferred settlement; not behavior evidence | 1 | review-fix working tree before tightening the RF-3 observer |
| `npx vitest run packages/web/src/records/CastMemberEditor.test.tsx` | expected RF-3 RED: 1 file failed; 1 of 17 tests failed because older success replaced newer failure after stale settlement; 16 tests passed | 1 | review-fix working tree before RF-3 implementation |
| `npx vitest run packages/web/src/records/CastMemberEditor.test.tsx` | RF-3 GREEN: 1 file and 17 tests passed; newest-attempt feedback ownership held for stale-success and stale-failure orderings | 1 | review-fix working tree after RF-3 implementation |
| focused web ESLint | FAILED: 1 unused placeholder parameter in the deferred test helper | 1 | review-fix working tree after RF-3 behavior green, before hygiene correction |
| `git diff --check` | FAILED: trailing Markdown whitespace on the new FOUNDATIONS amendment-history line | 1 | same review-fix working tree before hygiene correction |
| `npm run typecheck --workspace @loom/web` | passed with zero errors | 1 | same review-fix behavior tree |
| focused web ESLint | passed with zero diagnostics | 1 | corrected review-fix working tree |
| `git diff --check` | passed with no whitespace errors | 1 | corrected review-fix working tree |
| `npm run typecheck --workspace @loom/web` | passed with zero errors | 1 | corrected review-fix working tree |
| `npx vitest run packages/web/src/records/CastMemberEditor.test.tsx` | passed: 1 file and 17 tests | 1 | corrected review-fix working tree |
| `npm run lint` | passed; core build prerequisite and every root/workspace ESLint invocation completed without diagnostics | 1 | complete amended review-fix working tree before commit amend |
| `npm run typecheck` | passed for core, server, and web workspaces | 1 | same complete amended review-fix working tree |
| `npm test` | passed: 174 test files and 1,917 tests | 1 | same complete amended review-fix working tree |
| `npm run build` | passed for core, server, and web; Vite transformed 463 modules with only its bundle-size advisory | 1 | same complete amended review-fix working tree |
| proof-owned `npm start`, Playwright session `cl117-reviewfix-clean`, and final cleanup probes | passed: current production build on localhost, exact clipboard digest, local import/discard/Save lifecycle, durable reload, empty browser storage, 0 console errors / 0 warnings, browser/server closed, and ports 4173/5173/5174 free | 1 | same complete amended review-fix working tree |

## Production browser evidence

Evidence-only proof server preflight: configured API/UI port 4173 plus dev ports 5173 and 5174; process-owner checks found no pre-existing server or Vite owner. The first default-sandbox start rebuilt successfully but the sandbox denied the localhost bind with `listen EPERM`; the approved proof-owned restart rebuilt the same tree and bound only `127.0.0.1:4173`. There was no separate proxy/API base because production UI and API share that port. Cleanup stopped only proof-owned session 43118 and browser `cl117-reviewfix-clean`; subsequent process checks returned no owner, browser listing returned `(no browsers)`, and connection checks for 4173/5173/5174 each returned curl exit 7.

Evidence-only backend process currentness: `CONTINUITY_LOOM_CONFIG_DIR=/tmp/continuity-loom-117-reviewfix-PMgiG3/config npm start` rebuilt all workspaces, transformed 463 Vite modules, then ran `node packages/server/dist/launch.js` without watch/reload mode. PID 12104 owned the current proof process on `127.0.0.1:4173`. The restart occurred after the amended working tree passed all four root gates; `GET /api/health` returned `{"status":"ok"}` before browser assertions. The current server created the disposable demo through the production UI under `/tmp/continuity-loom-117-reviewfix-PMgiG3/projects/letter-under-flour-bin-demo`; no stateful fixture was copied.

Evidence-only browser console state: final clean in-memory Chrome session `cl117-reviewfix-clean` granted clipboard read/write permission for the localhost origin before the first product action. Console reads after the assistance lifecycle and again after the durable reload both reported `Total messages: 0 (Errors: 0, Warnings: 0)`.

Evidence freshness: the final browser run occurred after the approved Foundations amendment, RF-2/RF-3 implementation, 17-test focused web green, and all four amended-tree root gates. It exercised the current production route, current generated fixture, current API, and both review-changed surfaces: newest copy feedback rendered truthfully in ordinary use, and one imported-draft provenance/baseline session persisted through review until Discard or successful Save. Overlapping clipboard settlement order remains covered by the retained component regression test because a real clipboard cannot deterministically expose both deferred settlement orders.

The clean production scenario observed:

1. Generated a disposable demo project under `/tmp/continuity-loom-117-reviewfix-PMgiG3/projects/letter-under-flour-bin-demo`, routed to Niko Bram's Cast Member, and opened edit mode on `127.0.0.1:4173/records?type=CAST+MEMBER`.
2. Activated **Copy Cast Member draft prompt**. The browser clipboard measured 12,861 UTF-16 code units and SHA-256 `4777c38553b711cdffb79ae5e40e419fb937d5a9d84b45b7a1219f0716ea8d97`; the independently read built core constant had the exact same length and digest. The truthful copied status appeared.
3. Opened **Import Cast Member draft**. Reviewing `This is not JSON.` produced the named parse alert, retained the paste for correction, and left the original `identity.one_line` unchanged.
4. Replaced the paste with a valid partial object containing a foreign `entity_id`, changed `identity.one_line`, unknown `identity.surprise_key`, one uncertainty, and one declared invented field. Review opened an alertdialog listing exactly `identity.one_line`; Escape canceled the overwrite while retaining the paste and original form value. Reviewing again and confirming applied the draft.
5. The form contained `Imported Niko proof draft.` while the absent fields and selected Niko ENTITY stayed unchanged. The report showed **Filled fields**, **Skipped fields**, and **Needs your attention**, naming the protected `entity_id`, unknown key, uncertainty, and declared invented field, alongside the unsaved/not-record/not-canon boundary.
6. During that active report, **Import Cast Member draft** was natively disabled with `aria-describedby` pointing to: `Create or save the record, or discard the current imported draft, before importing another response.` The paste dialog was absent, so the current report, provenance, and pre-import baseline remained the sole active import session.
7. The request inventory through assistance contained only app reads plus the earlier explicit demo-project POST: requests 4-20 ended with the loaded record GET and contained no record create/update or provider call. Both localStorage and sessionStorage were empty.
8. **Discard imported draft** restored the exact original one-line value, removed the report, and re-enabled Import. A second import changed the same field to `Saved Niko dossier draft.`; explicit **Save Record** emitted the sole record write, request 21 `PUT /api/records/019f7c92-563a-77d7-9940-189ad3a62c56` -> 200.
9. Save returned to the list with the new summary. Reopening the editor showed the durable saved value, enabled Import, no report, and no paste surface. Reloading the production route and selecting Niko again showed the same durable summary. Browser storage remained empty throughout.

Evidence identity refresh:
- Current evidence identities: fixture `/tmp/continuity-loom-117-reviewfix-PMgiG3/projects/letter-under-flour-bin-demo` generated through the current production UI; browser session `cl117-reviewfix-clean` closed after successful capture; proof server PID 12104/session 43118 closed; packet paths/hashes none; active revision complete amended working tree based on original fixed point `10e1c19f9cdfe4b460ac1c9949f033499bf7a720`; artifacts `.playwright-cli/page-2026-07-19T22-50-43-262Z.yml` SHA-256 `5b3a11ffe9c75769bbeb05bc1b2d2ff265d84052c74a793dffecf3d20140217a`, `.playwright-cli/page-2026-07-19T22-52-22-509Z.yml` SHA-256 `889f44e4c587c97bce505a9bfbc61592769cc4697e91c2a03dab2ac3bcac7a91`, `.playwright-cli/page-2026-07-19T22-53-00-582Z.yml` SHA-256 `b6f6d6c2e243a246ade291fc8cdfff503b421aebb911ef06d083b5f000f43527`, `.playwright-cli/page-2026-07-19T22-53-57-689Z.yml` SHA-256 `a8768dcec2e40c3647dcec912db77d7a6e60f7e6eb339747890d4909665a3b7c`, and `.playwright-cli/page-2026-07-19T22-55-01-944Z.yml` SHA-256 `396eace92703809b0c5c83c457ba9c47751521975f0d8390d553c9a7b1ecc4a0`.
- Historical red identities retained: #119 core-template and web-copy red commands at chronology rows 1 and 5; #120 core-import and web-import red commands at chronology rows 8 and 14.
- Superseded evidence identities: initial fixture `/tmp/continuity-loom-117-browser-0GevqR/letter-under-flour-bin-demo`; initial browser session `cl117-clean`; preliminary browser session `cl117-final`; packet paths/hashes none; initial snapshot artifacts recorded in the pre-review evidence history.
- Superseded-token sweep: current-proof text names only `cl117-reviewfix-clean`; `cl117-clean` and `cl117-final` occur only in the classified initial chronology/history and this superseded identity block; historical-red hits remain classified in the Historical red identities line.

TDD closeout preflight:
- Durable sink/body inspected: durable file-backed ledger file `archive/reports/cast-member-draft-import-implementation-evidence.md`, including the `TDD preflight`, `Acceptance atom and sequence map`, `Shared-boundary TDD table`, `Command chronology`, `Production browser evidence`, and `Working pre-close audit` headings.
- Compact table/header: present after structural check; the exact eight-column header is adjacent to rows for #117-#121 and every agreed seam.
- Rows accounted for: all in-scope issues and seams listed; parent aggregation, document conformance, core template, web copy, copy browser, core import, web import, import browser, and archival conformance are explicit.
- Pre-red recovery status: N/A - pre-red preflight/table was visible before first red.
- Pre-red evidence reference: durable file-backed ledger file `archive/reports/cast-member-draft-import-implementation-evidence.md` at the `TDD preflight` heading; file line order proves the preflight and compact table precede the first red command in `Command chronology` row 1.
- CONTEXT.md status: absent, with `docs/agents/domain.md` read and its proceed-silently rule applied.
- ADRs/principles/docs status: present; ACTIVE-DOCS, FOUNDATIONS, schema, archival workflow, user guide, domain guidance, the sole unrelated ADR, and both source reports were inspected.
- Acceptance atom map: all rows in the exact 61-row audit list authoritative atoms and proof surfaces, with the parent implementation and testing decisions expanded rather than referenced circularly.
- Acceptance sequence map: all rows list ordered proof or a justified sequence N/A; order-sensitive copy, retry, overwrite, merge, save, discard/remount, and archival rows carry ordered proof, with same-instance browser observers where required.
- Partial-red / red-first skip reasons: none partial; explicit skips are listed for parent aggregation, #118 document authoring, #119/#120 supplemental browser evidence, and #121 archival conformance.
- Evidence-only rows freshness: final browser smoke passed after every review fix on the complete behavior tree for current route/action/API/generated fixture: exact clipboard equality, local invalid retry and overwrite, active-session blocking, discard restoration, explicit Save resolution, durable reopen/reload, no assistance write, and no browser-storage residue.
- Evidence-only browser console state: listed with 0 errors and 0 warnings after the assistance lifecycle and again after reload; clipboard permission was granted before the first product action.
- Evidence-only proof server preflight: configured API/UI ports 4173 (single production port) plus dev 5173 and 5174; owner-check result all free; unrelated pre-existing owners none; configured ports verified free with no isolated proof-owned ports needed and no separate proxy/API base because production UI and API share `127.0.0.1:4173`; cleanup ownership stopped only the proof-owned `npm start` session and Playwright sessions, and all ports were free afterward.
- Evidence-only backend process currentness: the final proof-owned server rebuilt the amended tree immediately before launch, ran without watch mode as PID 12104, passed health, generated its fixture through the UI, and was closed afterward; no stateful fixture was copied.
- Evidence identity refresh: same-sink current, historical-red, and superseded identity block inspected; current fixture, clean session, server/session IDs, snapshot hashes, fixed point, historical red rows, and superseded initial runs are explicit.
- Existing-test contract-change rows: none; no existing expected value was rewritten.

TDD review-fix map:

| RF ID | Issue/seam | Finding | Red | Green | Regression durability | Browser freshness | Backend currentness | Identity refresh |
|---|---|---|---|---|---|---|---|---|
| RF-1 | #117/#120 constitutional conformance | P1-spec-1: prefill conflicted with then-current §§26.1 and 29.2 | red-first skipped because conformance-only fix did not change behavior | exact approved wording is tracked in `docs/FOUNDATIONS.md`; final Spec re-review remains the closeout confirmation | tracked `docs/FOUNDATIONS.md` amendment and exact-read PRD #117 approval comment | final production browser rerun covers the same-revision RF-2/RF-3 behavior dependent on the amendment | current proof-owned production server rebuilt the amended tree and passed health | current fixture/session/artifact block above; final Spec identity to be added after re-review |
| RF-2 | #120 web import session | P1-spec-2: a second import could sever retained values from provenance and the original discard baseline | first intended red: 1 of 15 failed because Import remained enabled; second intended red: 1 of 16 failed because successful Save left the report mounted | final same command -> 1 file / 16 tests passed, observing discard, successful Save, and successful Create session resolution | retained component regression tests in `packages/web/src/records/CastMemberEditor.test.tsx` | final production browser observed disabled/described second import, exact discard restoration, re-enable, successful Save clearing, durable reopen, and reload | current proof-owned production server rebuilt the amended tree and passed health | current `cl117-reviewfix-clean` fixture/session/artifact block above |
| RF-3 | #119 web copy ordering | P1-spec-3: older clipboard settlements could replace newer visible feedback | after an unexpected green from an insufficient observer, tightened command -> intended red, 1 of 17 failed because older success replaced newer failure | same command -> 1 file / 17 tests passed for both stale settlement directions | retained component regression test in `packages/web/src/records/CastMemberEditor.test.tsx` | final production browser observed exact clipboard equality and truthful ordinary success; both overlapping settlement orders are deterministic component-test evidence | current proof-owned production server rebuilt the amended tree and passed health | current `cl117-reviewfix-clean` fixture/session/artifact block above |

TDD evidence gate pre-review status: durable sink tracked implementation evidence report; compact table/header present after structural check; seams accounted for all listed; CONTEXT.md status absent; ADRs/principles/docs status present; sequence evidence present; evidence identities present; partial-red / red-first skip reasons listed; evidence-only rows listed and fresh; proof server preflight present; existing-test contract-change rows none. Final Spec re-review is the remaining non-TDD closeout confirmation.

## Working pre-close audit

Validator result: `node .claude/skills/implement/scripts/validate-closeout-body.mjs archive/reports/cast-member-draft-import-implementation-evidence.md --audit-only --review-entry --acceptance-manifest /tmp/continuity-loom-117-acceptance-manifest.json` passed with all 61 rows satisfied.

| Issue | Acceptance criterion or conformance check | Evidence | Status |
|---|---|---|---|
| #117 | Parent-Solution - Parent PRD ## Solution section | atoms: complete static copy prompt, tolerant local paste-back, partial prefill, three-band report, review-before-save, no provider call, no residue; proof surfaces: #118 doc, #119 core/web/browser, #120 core/web/browser, #121 archive; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.test.tsx`; `archive/reports/cast-member-record-field-guide.md`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: copy out -> external interaction outside app -> paste back -> review -> explicit save, observed in component and browser evidence. | satisfied |
| #117 | US1 - As an author, I want a copy button that puts a complete Cast Member draft prompt on my clipboard, so that I can brief an external LLM with a single paste instead of shuttling a separate field guide around | atoms: editor copy button, complete prompt, one clipboard write; proof surfaces: #119 component and browser; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.test.tsx`; `archive/reports/cast-member-record-field-guide.md`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: activate copy -> clipboard receives current constant -> success feedback. | satisfied |
| #117 | US2 - As an author, I want the copied prompt to explain the purpose of every Cast Member field, so that the external LLM drafts material a future prose writer can actually use | atoms: purpose of every schema leaf including five sample-utterance leaves; proof surfaces: #118 doc plus #119 drift/doc conformance; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.test.tsx`; `archive/reports/cast-member-record-field-guide.md`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: N/A because the criterion is not sequence-sensitive. | satisfied |
| #117 | US3 - As an author, I want the copied prompt to instruct the LLM to invent character-fitting, memorable material for fields my dossier does not cover, so that imported drafts arrive rich instead of full of holes | atoms: evidence-first drafting, character-fitting invention when evidence is absent, memorability aim, declaration of invented fields; proof surfaces: #118 doc/template tests; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.test.tsx`; `archive/reports/cast-member-record-field-guide.md`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: N/A because the criterion is not sequence-sensitive. | satisfied |
| #117 | US4 - As an author, I want the copied prompt to demand one fenced JSON object with exact field names plus declared uncertainties and inventions, so that the response can be imported mechanically without reformatting | atoms: exactly one fenced JSON object, exact payload names, uncertainties, invented-fields list; proof surfaces: #118 doc/template and #120 parser fixtures; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.test.tsx`; `archive/reports/cast-member-record-field-guide.md`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: N/A because the criterion is not sequence-sensitive. | satisfied |
| #117 | US5 - As an author, I want the copied prompt to forbid the LLM from choosing or emitting the linked entity, so that record linkage stays entirely under my control | atoms: no `entity_id` output, injected `entity_id` stripped/reported, form link preserved; proof surfaces: #118 contract, #120 core/component/browser; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.test.tsx`; `archive/reports/cast-member-record-field-guide.md`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: parse injected link -> skip/report -> merge while retaining current link. | satisfied |
| #117 | US6 - As an author, I want the copied prompt to contain no project records or story data, so that copying it never leaks anything from my project | atoms: no project records, story configuration, or user data in copied text; proof surfaces: #118 static doc/template plus #119 clipboard assertion; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.test.tsx`; `archive/reports/cast-member-record-field-guide.md`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: N/A because the criterion is not sequence-sensitive. | satisfied |
| #117 | US7 - As an author, I want an import affordance on the Cast Member editor that accepts a pasted response, so that I never retype structured output field by field | atoms: Cast Member editor import entry point and paste surface; proof surfaces: #120 component and browser; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.test.tsx`; `archive/reports/cast-member-record-field-guide.md`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: open editor -> open import surface -> paste. | satisfied |
| #117 | US8 - As an author, I want the importer to accept the JSON even when it is wrapped in code fences or surrounded by chat prose, so that a normal assistant reply imports without manual cleanup | atoms: fenced envelope and prose-wrapped object; proof surfaces: #120 core fixtures; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.test.tsx`; `archive/reports/cast-member-record-field-guide.md`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: input envelope -> deterministic extraction -> JSON parse. | satisfied |
| #117 | US9 - As an author, I want every valid field imported even when other fields are invalid, so that one bad enum value does not send me back to the external LLM for a full regeneration | atoms: valid siblings imported despite invalid field; proof surfaces: #120 core partial-valid fixture and web merge assertion; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.test.tsx`; `archive/reports/cast-member-record-field-guide.md`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: parse -> validate independently -> retain valid values. | satisfied |
| #117 | US10 - As an author, I want every skipped field named with its specific reason, so that nothing is ever silently dropped | atoms: every rejected payload path named with category-specific reason; proof surfaces: #120 core report fixtures and rendered skipped band; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.test.tsx`; `archive/reports/cast-member-record-field-guide.md`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: validate -> accumulate exact skips -> render. | satisfied |
| #117 | US11 - As an author, I want an import report that separates filled fields, skipped fields, and fields needing my attention, so that I know exactly where my review effort belongs | atoms: distinct filled, skipped, needs-author bands, with the active report retained as the only import provenance until discard or successful persistence; proof surfaces: #120 report function, component session-boundary test, browser; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.test.tsx`; `archive/reports/cast-member-record-field-guide.md`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: import -> apply -> render all three bands -> block another import until discard or successful Save/Create resolves the report. | satisfied |
| #117 | US12 - As an author, I want the report to surface the LLM's declared uncertainties and invented fields, so that I scrutinize invented material before it can become canon | atoms: uncertainties and invented field paths shown as review-only material and retained with their imported values for the whole active import session; proof surfaces: #120 report/component session-boundary test/browser; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.test.tsx`; `archive/reports/cast-member-record-field-guide.md`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: parse metadata -> exclude from payload -> render needs-author band -> retain until discard or successful Save/Create. | satisfied |
| #117 | US13 - As an author, I want the form's linked entity selection preserved on import, including the pre-bound entity from the linked creation flow, so that an import never breaks record linkage | atoms: standalone create link and linked-flow pre-bound link preserved; proof surfaces: #120 component tests and linked production route; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.test.tsx`; `archive/reports/cast-member-record-field-guide.md`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: pre-bind/select entity -> import -> same entity remains. | satisfied |
| #117 | US14 - As an author, I want the import affordance available while editing an existing dossier, so that a refreshed external draft can update a character without manual transcription | atoms: import available in edit mode and can update present fields; proof surfaces: #120 edit-mode component and browser; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.test.tsx`; `archive/reports/cast-member-record-field-guide.md`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: open saved dossier -> import -> confirm changes. | satisfied |
| #117 | US15 - As an author, I want a confirmation listing exactly which fields an import would overwrite when my form already has content, so that I never lose work unknowingly | atoms: detect only differing non-empty targets, list exact paths, block overwrite before confirmation, cancel unchanged; proof surfaces: #120 component and browser; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.test.tsx`; `archive/reports/cast-member-record-field-guide.md`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: review import -> confirmation appears before reset -> cancel unchanged or confirm applies. | satisfied |
| #117 | US16 - As an author, I want fields absent from the paste left untouched, so that partial drafts merge predictably with what I already wrote | atoms: paste-absent leaves and groups remain unchanged; proof surfaces: #120 merge component assertion; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.test.tsx`; `archive/reports/cast-member-record-field-guide.md`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: snapshot current form -> merge present paths only -> inspect absent path. | satisfied |
| #117 | US17 - As an author, I want nothing saved until I review the prefilled draft and save it myself, so that no external LLM output ever becomes a record automatically | atoms: import performs no create/update, imported state labeled unsaved, explicit Save is the only write; proof surfaces: #120 component API mocks and browser; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.test.tsx`; `archive/reports/cast-member-record-field-guide.md`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: import -> verify no write -> review -> explicit Save -> write. | satisfied |
| #117 | US18 - As an author, I want the paste, the report, and the unsaved draft to leave no stored residue when I cancel or navigate away, so that rejected assistance output disappears completely | atoms: paste/report/draft only mounted state, cancel unchanged, one active provenance/baseline session, discard restores the original pre-import state, successful Save/Create clears ephemeral import state, navigation/unmount leaves no browser/project residue; proof surfaces: #120 component storage spies, session-resolution tests, unmount, and browser; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.test.tsx`; `archive/reports/cast-member-record-field-guide.md`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: import -> block competing import -> cancel/discard, successful persistence, or navigate -> remount -> no assistance scratch. | satisfied |
| #117 | US19 - As an author, I want the app to make no network or provider call anywhere in this flow, so that my dossier and drafts stay on my machine | atoms: copy and import paths have no provider/network call; proof surfaces: #119/#120 component API assertions and browser request inventory; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.test.tsx`; `archive/reports/cast-member-record-field-guide.md`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: copy/import actions -> inspect requests -> no provider/server request from either action. | satisfied |
| #117 | US20 - As an author, I want the draft prompt versioned and mechanically kept in sync with the Cast Member schema, so that newly added schema fields are never silently missing from the prompt | atoms: independent version identity, schema-derived leaf inventory, missing-leaf failure, doc/core byte match; proof surfaces: #119 core tests; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.test.tsx`; `archive/reports/cast-member-record-field-guide.md`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: N/A because the criterion is not sequence-sensitive. | satisfied |
| #117 | Parent-Implementation-Decisions - Parent PRD ## Implementation Decisions section, as superseded only for its constitutional-footprint claim by the exact approved FOUNDATIONS amendment comment | atoms: (1) record-free clipboard round trip with no provider/server, (2) self-contained complete prompt and strict output contract, (3) character-fitting invention flagged for review, (4) independently versioned core ownership plus schema drift protection, (5) pure tolerant core parse/map/report, (6) create/linked/edit editor controls with present-only merge/link preservation/pre-overwrite confirmation, (7) ephemeral three-band report with every named skip and needs-author class retained through one active import session, (8) opt-in/provenance-visible/residue-free unsaved prefill under the exact §26.2 exception, (9) registered prompt authority plus field-guide archival, and (10) deliberately amended constitution with compiler/schema/source-profile footprint unchanged; proof surfaces: #118-#121 audit rows, PRD #117 amendment comment `https://github.com/joeloverbeck/continuity-loom/issues/117#issuecomment-5017616209`, `docs/FOUNDATIONS.md`, final diff, focused tests, production browser, and canonical gates; concrete anchors: `docs/FOUNDATIONS.md`; `docs/cast-member-draft-prompt-template.md`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.tsx`; `packages/web/src/records/CastMemberEditor.test.tsx`; `archive/reports/cast-member-record-field-guide.md`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: #118 authority -> #119 copy and #120 import -> exact amendment shown and approved -> same-revision constitutional and review-fix implementation -> #121 archival supersession. | satisfied |
| #117 | Parent-Testing-Decisions - Parent PRD ## Testing Decisions section | atoms: external-behavior assertions; core public prompt/drift/parse/map/report seams with valid, partially valid, malformed, bad-enum, unregistered-key, empty, injected-link, and prose-wrapped fixtures; web editor copy/dialog/confirmation/report/entity/accessibility seam in standalone, linked, and edit modes; prior-art suite retention; real production browser; no server product seam; ratified core/web seam confirmation; proof surfaces: shared-boundary TDD table, `packages/core/test/cast-member-draft-template.test.ts`, `packages/core/test/cast-member-draft-import.test.ts`, `packages/web/src/records/CastMemberEditor.test.tsx`, 174-file root suite, and production browser evidence; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.test.tsx`; `archive/reports/cast-member-record-field-guide.md`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: preflight -> intended red at each changed seam -> focused green -> root gates -> production browser freshness. | satisfied |
| #117 | Principles - Principles/ADR conformance for #117, including the exact approved FOUNDATIONS amendment | atoms: user-owned continuity with explicit Save/Create as the sole authority gate, static record-free copy, the CAST MEMBER-only §26.2 unsaved-prefill exception, opt-in/provenance-visible/residue-free import, all accepted-prose and other assistance independent-authorship rules retained, core purity, no compiler/schema/provider/server change, registered doc authority; proof surfaces: PRD #117 amendment comment `https://github.com/joeloverbeck/continuity-loom/issues/117#issuecomment-5017616209`, `docs/FOUNDATIONS.md`, source review, component/browser behavior, final diff; concrete anchors: `docs/FOUNDATIONS.md`; `docs/cast-member-draft-prompt-template.md`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.tsx`; `packages/web/src/records/CastMemberEditor.test.tsx`; `archive/reports/cast-member-record-field-guide.md`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: exact wording shown -> explicit approval -> exact tracker readback -> same-revision amendment and dependent behavior -> final conformance review. | satisfied |
| #118 | AC1 - `docs/cast-member-draft-prompt-template.md` exists and contains the complete copyable Cast Member draft prompt text: role framing, per-field semantics, extraction-and-invention rules, and the output contract, self-contained in one paste. | atoms: role framing, all per-field semantics, extraction/invention rules, strict output contract, one self-contained paste; proof surfaces: prompt authority document; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `docs/ACTIVE-DOCS.md`; `packages/core/test/cast-member-draft-template.test.ts`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: N/A because the criterion is not sequence-sensitive. | satisfied |
| #118 | AC2 - The per-field semantics cover every field of the CAST MEMBER schema in `docs/story-record-schema.md` section 5, including required core, optional extended, and every sample-utterance subfield. | atoms: required core, optional extended, and sample-utterance fields `text`, `situation`, `speech_function`, `pressure_tags`, and `copy_policy`; proof surfaces: doc field reference plus schema-derived drift test; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `docs/ACTIVE-DOCS.md`; `packages/core/test/cast-member-draft-template.test.ts`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: N/A because the criterion is not sequence-sensitive. | satisfied |
| #118 | AC3 - The invention policy is stated: fields without dossier evidence are invented in character to make the cast member more memorable rather than omitted, and the response must list the fields it substantially invented. | atoms: invent when dossier evidence is absent, fit established character, increase memorability, list substantially invented fields; proof surfaces: doc/template assertions; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `docs/ACTIVE-DOCS.md`; `packages/core/test/cast-member-draft-template.test.ts`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: N/A because the criterion is not sequence-sensitive. | satisfied |
| #118 | AC4 - The output contract demands exactly one fenced JSON object using exact schema field names, forbids emitting `entity_id` or unknown keys, and requires an `uncertainties` list alongside the invented-fields list. | atoms: exactly one fenced JSON object, exact schema names, no `entity_id`, no unregistered keys, `uncertainties`, `invented_fields`; proof surfaces: doc/template assertions and parser contract; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `docs/ACTIVE-DOCS.md`; `packages/core/test/cast-member-draft-template.test.ts`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: N/A because the criterion is not sequence-sensitive. | satisfied |
| #118 | AC5 - The prompt text contains zero project records, story configuration, or user data, and the document states that the app copies it verbatim as a static versioned template. | atoms: zero project/story/user data and explicit static verbatim versioned copy statement; proof surfaces: doc inspection and clipboard exactness test; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `docs/ACTIVE-DOCS.md`; `packages/core/test/cast-member-draft-template.test.ts`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: N/A because the criterion is not sequence-sensitive. | satisfied |
| #118 | AC6 - `docs/ACTIVE-DOCS.md` gains the registry row for the new document in the same change, naming it the domain authority for the Cast Member draft prompt template. | atoms: same-change ACTIVE-DOCS row and domain-authority wording; proof surfaces: registry diff/static inspection; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `docs/ACTIVE-DOCS.md`; `packages/core/test/cast-member-draft-template.test.ts`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: N/A because the criterion is not sequence-sensitive. | satisfied |
| #118 | AC7 - `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build` pass. | atoms: lint, typecheck, test, build; proof surfaces: final command ledger; concrete anchors: `docs/cast-member-draft-prompt-template.md`; `docs/ACTIVE-DOCS.md`; `packages/core/test/cast-member-draft-template.test.ts`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: final tree -> four canonical commands. | satisfied |
| #118 | Principles - Principles/ADR conformance for #118 | atoms: FOUNDATIONS §§9/9.1 and 26/26.1 for the static record-free prompt itself, ACTIVE-DOCS registration, no prompt-class/source-profile/ADR exception; the separate import behavior is governed by approved §26.2; proof surfaces: final diff, authority audit, and PRD #117 amendment comment; concrete anchors: `docs/FOUNDATIONS.md`; `docs/cast-member-draft-prompt-template.md`; `docs/ACTIVE-DOCS.md`; `packages/core/test/cast-member-draft-template.test.ts`; root `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; sequence: prompt authority remains constitutionally unchanged -> separate import exception approved and landed without altering prompt ownership. | satisfied |
| #119 | AC1 - Core exposes the Cast Member draft prompt as a deterministic versioned template with its own version identity; the prose template, compiler, and compiler-contract versions are unchanged. | atoms: deterministic constant, independent version, public core exports, prose/compiler/contract versions unchanged; proof surfaces: core test and final diff; concrete anchors: `packages/core/src/cast-member-draft/template.ts`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/web/src/records/CastMemberEditor.tsx`; `packages/web/src/records/CastMemberEditor.test.tsx`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root gates; sequence: N/A because the criterion is not sequence-sensitive. | satisfied |
| #119 | AC2 - A drift test fails when any CAST MEMBER schema field, including every sample-utterance subfield, is absent from the template, and asserts the core template text matches `docs/cast-member-draft-prompt-template.md`. | atoms: schema-derived every-leaf inventory including five sample leaves, deliberate missing-leaf failure sensitivity, core/doc byte equality; proof surfaces: focused core test; concrete anchors: `packages/core/src/cast-member-draft/template.ts`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/web/src/records/CastMemberEditor.tsx`; `packages/web/src/records/CastMemberEditor.test.tsx`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root gates; sequence: remove/probe leaf -> failure, restore -> green (mutation sensitivity or equivalent deliberate negative probe recorded). | satisfied |
| #119 | AC3 - The copy control's entry point is the Cast Member editor: it is available in create mode, including the linked ENTITY-to-CAST creation flow, and in edit mode, whenever the editor is open. | atoms: create, linked create, edit availability whenever editor open; proof surfaces: component modes plus routed browser; concrete anchors: `packages/core/src/cast-member-draft/template.ts`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/web/src/records/CastMemberEditor.tsx`; `packages/web/src/records/CastMemberEditor.test.tsx`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root gates; sequence: navigate each mode -> observe same control. | satisfied |
| #119 | AC4 - The control's user-visible states, actions, and outcomes are truthful: copying places the complete current template text on the clipboard verbatim with visible success feedback, and the copied prompt preview contents and freshness are guaranteed - the clipboard text is exactly the current core template version, never a stale copy. | atoms: complete current bytes, one clipboard write, success feedback, no stale copy; proof surfaces: component clipboard mock and browser clipboard read; concrete anchors: `packages/core/src/cast-member-draft/template.ts`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/web/src/records/CastMemberEditor.tsx`; `packages/web/src/records/CastMemberEditor.test.tsx`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root gates; sequence: click -> write current constant -> success. | satisfied |
| #119 | AC5 - The validation, warning, error, and recovery behavior is author-readable: a clipboard failure shows an error naming what failed, recovery is retrying the copy, and no failure path leaves stale success feedback. | atoms: named clipboard failure, success cleared on failure, retry path, newest-attempt feedback ownership under overlapping writes, older success cannot replace newer failure, older failure cannot replace newer success; proof surfaces: component failure/retry and deferred settlement-order test; concrete anchors: `packages/core/src/cast-member-draft/template.ts`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/web/src/records/CastMemberEditor.tsx`; `packages/web/src/records/CastMemberEditor.test.tsx`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root gates; sequence: attempt A remains in flight -> attempt B settles -> A settles -> visible state remains B; ordinary failure -> retry -> success. | satisfied |
| #119 | AC6 - The canon and prose boundary stays visible: the copied prompt contains zero project records, story configuration, or user data, is template text rather than canon or prose, and instructs the external LLM to never emit `entity_id` or unknown keys. | atoms: record-free template, template-not-canon/prose label, no `entity_id` or unregistered keys instruction; proof surfaces: doc/template static checks and UI copy-boundary text; concrete anchors: `packages/core/src/cast-member-draft/template.ts`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/web/src/records/CastMemberEditor.tsx`; `packages/web/src/records/CastMemberEditor.test.tsx`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root gates; sequence: N/A because the criterion is not sequence-sensitive. | satisfied |
| #119 | AC7 - The user-initiated external LLM boundary holds: copying is a local clipboard action and makes no network or provider call on any path; the external-model interaction happens entirely outside the app. | atoms: explicit user click, local clipboard only, no provider/network call; proof surfaces: component API mocks and browser request inventory; concrete anchors: `packages/core/src/cast-member-draft/template.ts`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/web/src/records/CastMemberEditor.tsx`; `packages/web/src/records/CastMemberEditor.test.tsx`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root gates; sequence: click -> clipboard write only. | satisfied |
| #119 | AC8 - Nothing is persisted by copying: no project-store write, no working-set change, no prompt participation, no browser storage, and no copy state in backup, migration, export, or provenance surfaces. | atoms: no project store, working set, prompt participation, browser storage, backup/migration/export/provenance copy state; proof surfaces: component storage/API spies and final source diff; concrete anchors: `packages/core/src/cast-member-draft/template.ts`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/web/src/records/CastMemberEditor.tsx`; `packages/web/src/records/CastMemberEditor.test.tsx`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root gates; sequence: copy -> inspect all write/storage spies unchanged. | satisfied |
| #119 | AC9 - The browser and accessibility regression scenario is covered by component tests spanning both editor modes, keyboard operation, and accessible naming of the copy control and its feedback states. | atoms: create/edit/linked modes, keyboard activation, accessible control and feedback names; proof surfaces: web component tests; concrete anchors: `packages/core/src/cast-member-draft/template.ts`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/web/src/records/CastMemberEditor.tsx`; `packages/web/src/records/CastMemberEditor.test.tsx`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root gates; sequence: focus/keyboard activate -> accessible feedback. | satisfied |
| #119 | AC10 - `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build` pass. | atoms: lint, typecheck, test, build; proof surfaces: final command ledger; concrete anchors: `packages/core/src/cast-member-draft/template.ts`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/web/src/records/CastMemberEditor.tsx`; `packages/web/src/records/CastMemberEditor.test.tsx`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root gates; sequence: final tree -> four canonical commands. | satisfied |
| #119 | Principles - Principles/ADR conformance for #119 | atoms: static record-free copy, opt-in, residue-free, platform-free core, no compatibility/server/schema path; proof surfaces: source audit/tests; concrete anchors: `packages/core/src/cast-member-draft/template.ts`; `packages/core/test/cast-member-draft-template.test.ts`; `packages/web/src/records/CastMemberEditor.tsx`; `packages/web/src/records/CastMemberEditor.test.tsx`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root gates; sequence: N/A because conformance is evaluated on the final tree. | satisfied |
| #120 | AC1 - Core exposes pure parse, map, and report functions with lenient envelope extraction that accepts a fenced JSON block or a JSON object surrounded by chat prose. | atoms: three public pure functions, fenced JSON, prose-wrapped balanced object, deterministic parse failure; proof surfaces: core fixtures; concrete anchors: `packages/core/src/cast-member-draft/import.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.tsx`; `packages/web/src/records/CastMemberEditor.test.tsx`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root gates; sequence: extract -> parse -> map -> report. | satisfied |
| #120 | AC2 - Per-field validation against the registered CAST MEMBER schema imports every valid field and skips only the specific fields that fail; `entity_id`, unknown keys, and empty values are stripped and reported, never imported. | atoms: registered-schema leaf validation, valid sibling retention, invalid enum, unregistered key, empty value, malformed list entry, protected `entity_id`, exact skip paths/reasons; proof surfaces: core table fixtures; concrete anchors: `packages/core/src/cast-member-draft/import.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.tsx`; `packages/web/src/records/CastMemberEditor.test.tsx`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root gates; sequence: traverse each present path -> validate independently -> import or skip/report. | satisfied |
| #120 | AC3 - The import control's entry point is the Cast Member editor: it is available in create mode, including the linked ENTITY-to-CAST creation flow, and in edit mode, opening a paste surface. | atoms: create, linked create, edit control and accessible paste surface; proof surfaces: component modes and browser; concrete anchors: `packages/core/src/cast-member-draft/import.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.tsx`; `packages/web/src/records/CastMemberEditor.test.tsx`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root gates; sequence: open editor -> activate Import -> dialog. | satisfied |
| #120 | AC4 - Importing prefills the draft form only with fields present in the paste; absent fields stay untouched, and the form's `entity_id`, including a linked-flow pre-bound value, is always preserved. | atoms: present-only deep merge, absent preservation, standalone and pre-bound `entity_id` preservation; proof surfaces: component tests; concrete anchors: `packages/core/src/cast-member-draft/import.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.tsx`; `packages/web/src/records/CastMemberEditor.test.tsx`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root gates; sequence: snapshot form -> import -> merge present leaves -> inspect retained values/link. | satisfied |
| #120 | AC5 - When the paste would replace non-empty form values, a confirmation lists the exact affected fields before anything is overwritten; cancelling the confirmation leaves the form unchanged. | atoms: detect exact differing non-empty targets, confirmation before overwrite, exact path list, cancel unchanged; proof surfaces: component/browser; concrete anchors: `packages/core/src/cast-member-draft/import.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.tsx`; `packages/web/src/records/CastMemberEditor.test.tsx`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root gates; sequence: parse/review -> alertdialog -> cancel unchanged or confirm then reset. | satisfied |
| #120 | AC6 - The report's user-visible states, actions, and outcomes are truthful: after import the editor renders an ephemeral three-band report - filled fields; skipped fields, each with its named reason; and needs-author items covering the `entity_id` selection, the response's declared uncertainties, and its declared invented fields. | atoms: filled band, per-skip path/reason band, needs-author `entity_id` plus uncertainties plus invented fields, one active import session retains the report with its values until discard or successful persistence; proof surfaces: core report, component DOM/session-boundary test, browser; concrete anchors: `packages/core/src/cast-member-draft/import.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.tsx`; `packages/web/src/records/CastMemberEditor.test.tsx`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root gates; sequence: apply import -> render all bands -> block another import -> discard restores baseline or successful Save/Create clears report. | satisfied |
| #120 | AC7 - The validation, warning, error, and recovery behavior is author-readable: an unparseable paste produces an error, imports nothing, and permits correcting the paste and retrying; per-field skips are warnings with named reasons; cancelled or failed imports recover with the form unchanged. | atoms: parse error, zero imported fields, editable retry, named per-field warnings, cancel/failure unchanged; proof surfaces: core/component/browser; concrete anchors: `packages/core/src/cast-member-draft/import.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.tsx`; `packages/web/src/records/CastMemberEditor.test.tsx`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root gates; sequence: bad paste -> alert/no reset -> correct -> retry; cancel -> unchanged. | satisfied |
| #120 | AC8 - The canon and prose boundary stays visible: the prefilled form is an unsaved draft, not a record and not canon; no record create or update occurs until the author reviews the prefilled draft and saves it themselves; declared uncertainties and invented fields are labeled review material, never prose. | atoms: unsaved/non-canon label, zero automatic create/update, explicit save only, uncertainty/invention review labels; proof surfaces: component API mocks and browser; concrete anchors: `packages/core/src/cast-member-draft/import.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.tsx`; `packages/web/src/records/CastMemberEditor.test.tsx`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root gates; sequence: import -> review -> no write -> explicit Save -> write. | satisfied |
| #120 | AC9 - The user-initiated external LLM boundary holds: the import flow makes no network or provider call on any import path; the external-model interaction happens entirely outside the app. | atoms: local import path and zero network/provider calls; proof surfaces: component API mocks/browser requests; concrete anchors: `packages/core/src/cast-member-draft/import.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.tsx`; `packages/web/src/records/CastMemberEditor.test.tsx`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root gates; sequence: import actions -> no request. | satisfied |
| #120 | AC10 - Rejected, cleared, or abandoned import output leaves no residue in persistence, migration, export, or provenance surfaces: no project-store write, no working-set change, no prompt participation, and no persisted browser storage for the paste, the report, or the unsaved draft; the only provenance-bearing output is the record the author explicitly saves. | atoms: no project-store/working-set/prompt/browser storage for paste/report/draft, one active provenance/baseline session, discard/cancel/unmount cleanup, successful Save and Create clear ephemeral state, explicit saved record is sole durable result; proof surfaces: storage/API spies, session-resolution tests, unmount/remount component, browser; concrete anchors: `packages/core/src/cast-member-draft/import.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.tsx`; `packages/web/src/records/CastMemberEditor.test.tsx`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root gates; sequence: assistance state -> competing import blocked -> cancel/discard/navigate -> no retained scratch; alternate successful Save/Create -> canonical record only and ephemeral report cleared. | satisfied |
| #120 | AC11 - The browser and accessibility regression scenario is covered by component tests spanning both editor modes, `entity_id` preservation, the overwrite confirmation, and accessible names and keyboard operation for the paste surface, report, and confirmation. | atoms: both editor modes plus linked flow, entity preservation, overwrite alertdialog, accessible names, keyboard operation for dialog/report/confirmation; proof surfaces: component tests and browser snapshot; concrete anchors: `packages/core/src/cast-member-draft/import.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.tsx`; `packages/web/src/records/CastMemberEditor.test.tsx`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root gates; sequence: keyboard open -> paste/review -> keyboard cancel/confirm -> accessible report. | satisfied |
| #120 | AC12 - `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build` pass. | atoms: lint, typecheck, test, build; proof surfaces: final command ledger; concrete anchors: `packages/core/src/cast-member-draft/import.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.tsx`; `packages/web/src/records/CastMemberEditor.test.tsx`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root gates; sequence: final tree -> four canonical commands. | satisfied |
| #120 | Principles - Principles/ADR conformance for #120, including the exact approved §26.2 exception | atoms: opt-in per invocation, CAST MEMBER-only unsaved prefill, local parsing, exact overwrite confirmation, provenance retained until discard or successful persistence, residue-free rejection/abandonment, explicit Save/Create as the sole record-write gate, all other assistance independent-authorship boundaries retained, platform-free core, no server/schema/compatibility path; proof surfaces: PRD #117 amendment comment, `docs/FOUNDATIONS.md`, final source audit, tests, browser; concrete anchors: `docs/FOUNDATIONS.md`; `packages/core/src/cast-member-draft/import.ts`; `packages/core/test/cast-member-draft-import.test.ts`; `packages/web/src/records/CastMemberEditor.tsx`; `packages/web/src/records/CastMemberEditor.test.tsx`; production route `127.0.0.1:4173/records?type=CAST+MEMBER`; root gates; sequence: exact amendment shown and approved -> amendment and behavior land together -> import -> explicit review/confirmation -> discard or explicit Save/Create -> final conformance review. | satisfied |
| #121 | AC1 - `reports/cast-member-record-field-guide.md` is relocated under the archive following the destinations and closeout steps in `docs/archival-workflow.md`. | atoms: tracked report moved to `archive/reports/`, original absent, filename preserved; proof surfaces: git diff/path inspection; concrete anchors: `archive/reports/cast-member-record-field-guide.md`; `archive/reports/cast-member-draft-import-prd-prep.md`; `docs/cast-member-draft-prompt-template.md`; `docs/ACTIVE-DOCS.md`; targeted `rg` path sweep; root gates; sequence: #119/#120 green -> archive move. | satisfied |
| #121 | AC2 - Active documents and reports no longer point to the field guide as an operational tool; historical references follow the post-archive reference rules. | atoms: no active operational reference to old guide, historical references either archived or path-correct, inventory manifests classified non-operational; proof surfaces: targeted `rg` and archive inspection; concrete anchors: `archive/reports/cast-member-record-field-guide.md`; `archive/reports/cast-member-draft-import-prd-prep.md`; `docs/cast-member-draft-prompt-template.md`; `docs/ACTIVE-DOCS.md`; targeted `rg` path sweep; root gates; sequence: archive -> active-reference sweep. | satisfied |
| #121 | AC3 - `docs/cast-member-draft-prompt-template.md` remains the sole operational authority for the Cast Member draft prompt, and its invention policy is unaffected by the archival. | atoms: new doc is sole active operational authority and invention policy bytes unchanged through archival; proof surfaces: ACTIVE-DOCS/doc inspection and diff; concrete anchors: `archive/reports/cast-member-record-field-guide.md`; `archive/reports/cast-member-draft-import-prd-prep.md`; `docs/cast-member-draft-prompt-template.md`; `docs/ACTIVE-DOCS.md`; targeted `rg` path sweep; root gates; sequence: doc ships -> guide archives -> authority recheck. | satisfied |
| #121 | AC4 - `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build` pass. | atoms: lint, typecheck, test, build; proof surfaces: final command ledger; concrete anchors: `archive/reports/cast-member-record-field-guide.md`; `archive/reports/cast-member-draft-import-prd-prep.md`; `docs/cast-member-draft-prompt-template.md`; `docs/ACTIVE-DOCS.md`; targeted `rg` path sweep; root gates; sequence: final tree -> four canonical commands. | satisfied |
| #121 | Principles - Principles/ADR conformance for #121 | atoms: archival destination/post-archive rules and no duplicate authority path; proof surfaces: final path/reference audit; concrete anchors: `archive/reports/cast-member-record-field-guide.md`; `archive/reports/cast-member-draft-import-prd-prep.md`; `docs/cast-member-draft-prompt-template.md`; `docs/ACTIVE-DOCS.md`; targeted `rg` path sweep; root gates; sequence: N/A because conformance is evaluated on the final tree. | satisfied |
