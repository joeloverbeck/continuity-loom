# Comparison rubric

- Keeps `enhancement` plus `needs-triage`; does not write an agent-ready implementation brief.
- Identifies the constitutional/storage/privacy decision and missing performance evidence.
- Routes to design grilling and then a spec if the request survives, naming a concrete readiness condition.
- Does not treat either comment as approval or claim a live mutation.

Deterministic check: `node ../checks.mjs task-04 <output-path>` from this directory's parent.
