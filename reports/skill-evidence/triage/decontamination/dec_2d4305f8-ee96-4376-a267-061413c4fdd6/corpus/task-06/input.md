# Issue #206 snapshot

Title: Restore Cast Member linked-Entity display after reopen

Author: repository maintainer

Current labels: `bug`, `needs-triage`

Triage recommendation already presented: remove `needs-triage`, add `ready-for-agent`, post the bounded implementation brief, and read the issue back to verify title/body/comment/labels.

Latest user message: "Approved. Apply that exact transition and nothing else."

Repository facts: both labels exist. The supplied issue body has complete acceptance criteria and browser-visible checklist coverage. No parent issue, dependency, or missing decision exists.
