## Recommendation

- **Category:** `bug`
- **State:** `needs-info`
- **Verification:** insufficient detail
- **Redundancy:** not assessable from this report; the claim is too broad to compare reliably with an existing behavior or fix.

The report describes a user-visible failure, but it does not contain enough information to reproduce or locate it. In particular, the current evidence cannot distinguish a readiness blocker from a local app/configuration defect, an OpenRouter authentication or model-route failure, or an intermittent provider failure. After maintainer approval, replace `needs-triage` with `needs-info`, add the missing `bug` category, and post this privacy-safe follow-up:

> *This was generated by AI during triage.*
>
> Thanks for the report. Could you provide:
>
> 1. The exact steps from opening the project to the failure, whether **Generate** was enabled, any readiness blockers or warnings shown, the exact visible error text, and whether it fails every time or only sometimes.
> 2. The app version or commit, Node version, browser and browser version, and selected OpenRouter provider/model route.
> 3. A redacted server diagnostic from the same failed attempt, ideally with its timestamp. Please remove API keys and story text.
>
> Do not send an API key, full prompt, candidate, accepted prose, or full story-record payload. Replace any incidental story content in an error or diagnostic with `[redacted]`.

## Re-verification trigger

Move the report back to `needs-triage` when the reporter replies. It becomes verifiable when the supplied steps and environment let a maintainer reproduce the same failure with throwaway, non-sensitive project data and observe the reported error, or when a timestamp-matched redacted diagnostic identifies the failing boundary well enough to construct a deterministic reproduction. Record whether the failure occurs before readiness/compilation, during the OpenRouter request, or while handling the response; that boundary determines the relevant check and expected behavior.
