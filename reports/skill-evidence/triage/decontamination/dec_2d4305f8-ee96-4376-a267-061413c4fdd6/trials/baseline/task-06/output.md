No GitHub operation was performed; this is the requested analysis-only execution plan.

The approved transition for issue #206 is:

1. Post exactly one comment containing the bounded implementation brief. Its first line must be:

   > *This was generated by AI during triage.*

   The remainder must use the Agent Brief contract: category `bug`; the Cast Member linked-Entity display regression as the summary; current and desired browser-visible behavior; durable interface-level guidance without file paths or line numbers; the issue body's complete acceptance criteria; and explicit out-of-scope boundaries. It must add no scope beyond the issue body.

2. Immediately verify that comment mutation before changing labels. Capture the returned comment URL or ID, fetch its raw JSON, assert that exactly one comment has that identity, extract its body byte-for-byte, and diff it against the staged comment source. Only an empty diff counts as an exact readback.

3. Perform one label edit on #206: remove `needs-triage` and add `ready-for-agent`. Preserve `bug`. Both target labels already exist, so no label creation is proposed.

4. Read #206 back after the label mutation and verify all of the following:

   - the issue is still open and its state reason is consistent with an open issue;
   - the title remains exactly `Restore Cast Member linked-Entity display after reopen`;
   - the issue body is byte-for-byte unchanged from the pre-mutation snapshot;
   - the final labels are exactly one category role, `bug`, and one state role, `ready-for-agent`;
   - `needs-triage` is absent;
   - the comment returned in step 1 is still present under the captured identity and still matches the staged source exactly.

The final handoff would report #206 as open, categorized as a bug, and ready for an AFK agent under the posted brief. No issue close or reopen, parent/dependency work, new issue, assignee or milestone change, title/body edit, extra comment, or other label change is part of this transition. If either readback fails, the transition must be reported as incomplete rather than handed off as ready.
