No GitHub operations were executed; this is the requested analysis-only transition plan.

The approval is sufficient to proceed without another checkpoint. The preconditions are satisfied: issue #206 is a `bug` in `needs-triage`, both target labels exist, the acceptance criteria and browser-visible checklist are complete, and there is no parent, dependency, missing decision, or commit-reachability gate.

Proposed operations, in order:

1. Immediately before mutation, read issue #206 with its title, full body, open/closed state and state reason, labels, comments, author, and dates. Confirm that the title and body still match the approved issue, that the maintainer approval is present, and that the issue still has exactly `bug` plus `needs-triage` with no conflicting category or state label.
2. Perform one label mutation that removes `needs-triage` and adds `ready-for-agent`. This keeps the approved remove/add transition bounded and avoids leaving the issue between state roles.
3. Read issue #206 back immediately. Verify that it remains open, its title and body are unchanged, and its labels are exactly one category role (`bug`) and one state role (`ready-for-agent`).
4. Post exactly one comment, beginning with the required disclaimer and containing the transition basis plus the bounded implementation brief below. Make no other comment, label, close, parent, dependency, or issue mutation.
5. Capture the comment URL/ID returned by the post, fetch that exact comment, and compare its body byte-for-byte with the staged text below. Then read issue #206 back again and verify the unchanged title and body, open state, exact `bug` and `ready-for-agent` labels, and presence of that exact comment.

Exact comment to post:

```markdown
> *This was generated by AI during triage.*

Readiness checks passed: the repository maintainer approved this exact transition; the issue body has complete acceptance criteria and browser-visible checklist coverage; both required labels exist; and there is no parent, dependency, missing product decision, or unpublished implementation prerequisite blocking delegation.

## Agent Brief

**Category:** bug
**Summary:** Restore the linked-Entity display for a Cast Member after that Cast Member is reopened.

**Current behavior:**
After a Cast Member with a linked Entity is reopened, the linked Entity is no longer displayed.

**Desired behavior:**
Reopening a Cast Member must preserve and display its existing linked-Entity association. Cast Members without a linked Entity must not acquire or display a stale association, and behavior outside the reopen path must remain unchanged.

**Key interfaces:**
- Cast Member reopen flow — reopening must retain the existing linked-Entity association.
- Cast Member linked-Entity display — it must reflect the Cast Member's current association after reopen.

**Acceptance criteria:**
- [ ] Reopening a Cast Member that has a linked Entity leaves that association intact and displays the linked Entity afterward.
- [ ] Reopening a Cast Member without a linked Entity does not display a fabricated or stale Entity.
- [ ] Existing linked-Entity display behavior outside the reopen path does not regress.
- [ ] The issue body's browser-visible regression scenario and all applicable project checks pass.

**Out of scope:**
- Redesigning Cast Member or Entity records or their relationship.
- Unrelated changes to Cast Member display or reopen behavior.
```

Final handoff after all readbacks pass: issue #206 is open and ready for an AFK implementation agent, categorized as `bug` and state-labeled `ready-for-agent`. The issue body remains the detailed acceptance authority, and the verified triage comment is the bounded implementation contract. No follow-up grooming or maintainer decision is required before implementation begins.
