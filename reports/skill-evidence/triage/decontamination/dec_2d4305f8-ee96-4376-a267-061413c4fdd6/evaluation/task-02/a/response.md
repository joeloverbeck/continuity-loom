> *This was generated by AI during triage.*

## Recommendation

- **Category:** retain `bug`.
- **State:** replace `needs-triage` with `ready-for-agent`.
- **Resulting label set:** exactly one category role (`bug`) and one state role (`ready-for-agent`).

The issue is specific, reproduced, and bounded enough for delegated implementation. The supplied current-`main` reproduction and failing focused component test confirm the defect: a saved CAST MEMBER still contains the correct `payload.entity_id`, but edit-mode form hydration reads the wrong source and leaves the Entity selector blank. The active schema requires `CAST MEMBER.entity_id` to resolve to an ENTITY, and the user guide treats that relationship as durable and preserved through editor workflows. The stored schema and API payload are already correct, so this is a browser editor regression rather than a persistence or data-model failure.

Repository inspection found the relevant seams in the CAST MEMBER editor, the shared form-default/reference-picker code, and the Records browser’s edit and linked-create entry points. There is no separate implementation that restores the saved relationship after the incorrect initialization. `.out-of-scope/` is absent or empty, so there is no resembling prior rejection. No dependency, design decision, migration, or human-only judgment blocks implementation.

The current worktree contains pre-existing changes in the focused CAST MEMBER test and shared record-editor code. Those changes concern nested-list removal and list-field markup, not this selector-hydration defect; an implementing agent must preserve them and avoid folding them into this issue. The browser-visible readiness checklist is covered by the supplied reopen/edit state and regression seam. Existing validation is unchanged, while prompt, network, canon, and migration checks are specifically not applicable.

## Agent Brief

**Category:** bug  
**Summary:** Preserve and display a saved Cast Member’s linked Entity when reopening the editor

**Current behavior:**  
When a saved CAST MEMBER is reopened for editing, its Entity selector appears blank even though the stored record and API payload still contain the correct `entity_id`. Create and linked-create flows are separate states and are not the reported failure.

**Desired behavior:**  
Edit mode must initialize the Entity selector from the saved CAST MEMBER payload. If that `entity_id` resolves to an available ENTITY option, the selector must visibly show that Entity. Saving without changing the selector must submit the same `entity_id`. Existing create-mode and linked-create defaults, unresolved-reference handling, validation behavior, and failed-save form retention must remain intact.

**Key interfaces:**

- `CastMemberEditor` edit-mode form initialization — use the saved record payload as the authority for `entity_id`.
- The form-default mapping for `entity_id` — preserve the saved scalar reference rather than reading record metadata or another field.
- The Entity reference selector — continue resolving the stored id to its human-readable ENTITY option without changing option eligibility or fallback behavior.
- The CAST MEMBER component regression seam — cover reopen/edit hydration and unchanged save submission.

**Acceptance criteria:**

- [ ] Opening a saved CAST MEMBER whose payload contains a valid `entity_id` selects and displays the corresponding ENTITY.
- [ ] Saving that record without changing the Entity selector submits the original `entity_id` unchanged.
- [ ] Creating an unlinked CAST MEMBER still starts with no Entity selected.
- [ ] Creating a linked CAST MEMBER from an ENTITY still starts with that ENTITY selected.
- [ ] Existing unresolved or archived reference presentation and existing client/server validation behavior remain unchanged.
- [ ] A focused component regression test fails before the fix and passes after it, covering both visible selection and the submitted payload.
- [ ] Relevant web tests, type checking, and linting pass.

**Out of scope:**

- Stored schema, API payload, persistence, migrations, or record backfills.
- Compiler, prompt, validation-rule, OpenRouter, or canon behavior.
- Changes to which records are eligible Entity references.
- Broad record-editor refactors or unrelated CAST MEMBER draft-import/list behavior.

## Next action

If the maintainer approves this recommendation, post the disclaimer-prefixed Agent Brief, replace `needs-triage` with `ready-for-agent`, and then read Issue #202 back to verify the open state, the exact two-role label set, and the exact posted brief body. No tracker mutation was performed in this analysis-only simulation.
