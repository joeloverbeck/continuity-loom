Recommendation: keep #510 as `enhancement` + `needs-triage`.

Its prerequisite is tracked by #511, “Migrate project metadata to provenance schema v2,” which is open and marked `ready-for-agent`. Although #510’s integration seam exists, it requires schema v2, so #510 is not ready for an agent.

Re-evaluate #510 only after #511 closes as completed. No label or tracker changes should be made now.
