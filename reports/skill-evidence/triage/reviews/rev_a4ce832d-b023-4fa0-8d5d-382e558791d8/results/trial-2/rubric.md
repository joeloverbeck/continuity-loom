# Blind Evaluation Rubric

Judge each output independently, then compare them. A passing output must recommend `enhancement` plus `needs-triage`, explicitly name issue #511 as the existing predecessor owner, report that #511 is open and still blocking, and avoid claiming it is absent, closed, or completed. Prefer one output only if it is materially clearer or safer on these behaviors; otherwise declare a tie.
