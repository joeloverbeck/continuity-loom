Recommend **category `bug`** and **state `wontfix` (already implemented)**.

The current filename builder preserves the project title verbatim, and focused verification exported `Night Train` as exactly `Night Train.json`. The reported behavior is not present in the supplied current tree. No prior-rejection knowledge base exists, and none should be created for an already-implemented bug report.

If approved, replace `needs-triage` with `wontfix`, add `bug`, and close with a comment pointing to the passing verification. No tracker changes have been made.
