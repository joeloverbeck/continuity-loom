Recommendation: **`bug` + `wontfix` (already implemented/current behavior is correct).**

The supplied verification shows the title is passed unchanged to the filename builder, and a focused export test produces exactly `Night Train.json`. The reported `NightTrain.json` behavior is not present in the supplied current tree.

If approved, add `bug`, replace `needs-triage` with `wontfix`, and close with the verification result. Do not create an `.out-of-scope/` entry.
