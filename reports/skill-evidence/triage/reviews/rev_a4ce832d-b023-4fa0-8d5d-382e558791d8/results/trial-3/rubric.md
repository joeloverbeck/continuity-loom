# Blind Evaluation Rubric

Judge each output independently, then compare them. A passing output must recommend `bug` plus already-implemented `wontfix`, cite the supplied implementation and passing verification, avoid proposing an out-of-scope record, avoid irrelevant predecessor-owner discussion, and perform no mutation. Prefer one output only if it is materially clearer or safer on these behaviors; otherwise declare a tie.
