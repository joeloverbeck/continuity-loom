# Legacy Skill Decontamination: playtest-prd-prep

## Eligibility

- Legacy basis: `owner-confirmed`; the target predates the repository's retired audit workflow and had no prior completed decontamination for the claimed version.
- Target before hash: `1a6984683fe63d6079f23dd712bac71536123643ee0ba0ad83bbcaeac9c85cfd` at repository HEAD `7e8a545860c0d70f25be429d0a02b37d44be8bbc`.
- Prior decontamination status: none. Preflight found no open incident and no competing review.
- Risk tier: high. Six trials were required because the skill governs a cross-skill custody artifact used before issue and PRD publication and has several stateful continuation branches.

## Current-skill baseline

- Core behaviors: validate one playtest report; preserve exact source, tracker, durability, privacy, and worktree boundaries; disposition every finding and strength; reconcile predecessor prep state; build the smallest governed PRD/non-PRD portfolio; validate one same-stem artifact; and hand it to `$playtest-to-issues` without implementation or publication.
- Runtime size and mandatory references: 1,864 words in `SKILL.md` plus 2,270 words in mandatory `references/prep-format.md`, for 4,134 target-owned normal-path words. The validator, validator tests, and agent metadata were part of the baseline but did not need simplification.
- Paired-trial corpus: six frozen historical intakes covering schema-v1 and schema-v2 new stories, two-generation and three-generation continuations, implemented predecessor findings, mixed no-create/method/future-coverage routing, ticket packets, and preservation constraints.

## Accretion analysis

- Preserved invariants: explicit-only invocation; one-artifact scope; source stop/continue rules; complete disposition and strength coverage; exact tracker and worktree boundaries; deterministic validation; producer-owned migration and predecessor consumption; ticket/PRD routing; privacy; no automatic mutation or publication; and the exact custody closeout.
- Removed or distilled audit-induced material: repeated phase-completion restatements, repeated first-action and custody explanations, duplicated migration and freshness narration, and qualifications whose ordering obscured the source stop/continue gate. Exact schemas and executable enforcement stayed in their canonical homes.
- Uncertain material retained: the retired `$skill-audit` route remains because the downstream custody skill still names and tests it. Reassigning that owner would be repair rather than decontamination, and the frozen corpus did not prove a replacement.

## Candidate

- Files changed: `SKILL.md` and `references/prep-format.md` only. The validator, its 26-test suite, and `agents/openai.yaml` are byte-identical to the baseline.
- Runtime size before/after: 4,134 to 2,745 mandatory target-owned words, a reduction of 1,389 words (33.6%, approximately 1,847 tokens). `SKILL.md` fell from 231 to 168 lines; the reference fell from 386 to 289 lines.
- Structural simplifications: retained all five phases and four executable validation commands, consolidated repeated gates into one final completion gate, kept uncommon browser work conditional, and shortened prose around schemas already enforced by the unchanged validator.

## Comparative validation

- Trial results: the candidate was noninferior on all six blind pairs. Evaluators preferred the candidate in T01, T02, T05, and T06; preferred the baseline narrowly in T03; and tied T04. No evaluator found a material or severe regression, lost domain rule, weakened safety boundary, or unsafe mutation.
- Deterministic checks: all retained baseline and candidate artifacts passed the assigned and frozen-current validators; all six candidate outputs passed an independent final revalidation; the unchanged target suite passed 26/26; and the decontamination helper suite passed 12/12.
- Regressions or uncertainty: none attributable to the candidate. One contaminated T03 attempt was interrupted and excluded before producing a retained result, then rerun fresh. Two independent continuation checks initially saw overwritten predecessor fixtures from sequential audit execution and passed after the frozen predecessor bytes were restored.

## Decision

- Outcome: `validated_simplification_landed`.
- Landed: yes. The helper verified that the live canonical target exactly matches the accepted candidate and recorded only `SKILL.md` and `references/prep-format.md` as modified.
- Target after hash: `b1a066e29020c0f09d5f7a68b7845e3f594b3c8bfb585a94aab9f635930cd474`.
- Host-adapter note: the landing event reports `mirror_status: broken` because the helper recognizes only `.agents` symlink mirrors. This repository intentionally uses a tracked, ordinary `.agents/skills/playtest-prd-prep` Codex host adapter whose sole procedural authority points to the canonical `.claude` target. The adapter remains valid and unmodified; broadening this run to repair the helper's mirror classifier would violate the no-same-run-expansion rule.
