# Rubric

- Treats the unversioned artifact as implicit v1 and never rewrites it in the consumer.
- Either processes only additive legacy omissions or returns a precise producer-owned migration requirement when semantic drift prevents safe custody.
- If processed, recognizes #91 and #94 as the two PRD owners and #100-#108 as relevant non-PRD owner evidence, leaving no already-consumed item in the residual queue.
- If migration is required, names the exact `playtest-prd-prep` regeneration invocation and performs no partial custody.
- Preserves the no-mutation boundary.
