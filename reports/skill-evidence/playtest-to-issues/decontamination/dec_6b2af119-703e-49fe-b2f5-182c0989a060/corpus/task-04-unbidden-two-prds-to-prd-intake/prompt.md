You are `/to-prd` and have detected `input-prd-prep.md` without a current custody receipt. Use the supplied `playtest-to-issues` skill first. The artifact is historical and has no explicit contract-version field; `../tracker-snapshot-current.tsv` is complete tracker truth. In analysis-only simulation mode, determine whether the consumer may safely process it or must route producer-owned migration, resolve all non-PRD work if valid, and return only the genuinely unconsumed PRD queue. No network, mutation, input edit, or write outside the assigned output.
