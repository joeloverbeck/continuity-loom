Use the supplied `playtest-to-issues` skill on `input-prd-prep.md`. Treat `../tracker-snapshot-pre-ash.tsv` as complete tracker truth at intake. The user explicitly approved the recommended four-ticket publication and non-PRD custody plan. Because this is an analysis-only simulation, do not access the network or actually mutate GitHub or the prep artifact; instead, produce the exact publication/readback/receipt manifest the workflow would execute, then return only the residual PRD queue. Write nowhere except the assigned output.
