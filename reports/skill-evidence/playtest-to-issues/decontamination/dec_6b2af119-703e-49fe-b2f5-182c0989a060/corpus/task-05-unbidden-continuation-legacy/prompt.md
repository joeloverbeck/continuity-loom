Use the supplied `playtest-to-issues` skill to assess `input-prd-prep.md`, an unversioned continuation-era prep artifact. Treat `../tracker-snapshot-current.tsv` as complete tracker truth. Work read-only: no network, tracker mutation, prep migration, or writes outside the assigned output. Apply the version contract, resolve non-PRD custody if the artifact is safely consumable, and return only the residual PRD queue or the exact producer route if it is not.
