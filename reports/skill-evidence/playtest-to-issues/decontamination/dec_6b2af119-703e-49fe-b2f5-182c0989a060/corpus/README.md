# Frozen paired-trial corpus

Frozen for run `dec_6b2af119-703e-49fe-b2f5-182c0989a060` before candidate construction.

Each task is executed twice from the same prompt and inputs: once with the hash-verified baseline snapshot and once with the isolated candidate. Performers must not read `rubric.md`, the other variant's output, or any evaluator notes. All trials are analysis-only simulations: they must not access the network, mutate GitHub, edit a prep artifact, or write outside their assigned trial directory. A frozen tracker snapshot stands in for live readback.

The corpus covers a current no-PRD portfolio, a current continuation, cross-report owner reconciliation, two legacy implicit-v1 portfolios, the publication approval boundary before and after approval, and an unknown future contract version.
