Use the supplied `playtest-to-issues` skill to take custody of this continuation artifact, `input-prd-prep.md`. Treat `../tracker-snapshot-current.tsv` as complete tracker truth. Work in analysis-only simulation mode: no network, GitHub mutation, prep rewrite, or writes outside the assigned output. Reconcile the prior-report carry-forward and every current non-PRD item, identify existing owners or explicit no-create routes, and return a receipt-equivalent ledger plus only the residual PRD queue.
