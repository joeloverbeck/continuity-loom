# Custody blocked: producer migration required

The missing contract-version field alone identifies the artifact as historical implicit v1 and is legacy-compatible. The artifact still cannot enter custody, however, because its `First operational action` delegates to `/to-prd` instead of naming substantive portfolio work. That defect is explicitly `migration-required` and belongs to the producer.

Run the validated producer route against the canonical source report so it rewrites the same-stem prep artifact at the current contract:

```text
$playtest-prd-prep "reports/playtest-the-unbidden-oath-2026-07-17T104952Z.md"
```

The complete tracker snapshot cannot cure this producer-contract defect. No non-PRD row was resolved, neither PRD candidate was classified as consumed or remaining, and `/to-prd` must not process either candidate from this artifact.

Unconsumed PRD queue: **blocked pending producer migration and a new passing `$playtest-to-issues` custody receipt; no `/to-prd` invocation is authorized.**
