1. Fetch the exact issue body, comments, labels, and current state; save its structured body snapshot under `/tmp`. Run the read-only scope checks: `git status --short`, the empty-diff check, current `HEAD`, and remote reachability. Post a one-row scope ledger naming every tracker-only acceptance item and its wording-packet proof, with artifact disposition N/A, ownership/placement N/A, no test seam, and no repository edits. Include the required `Scope ledger posted: yes; no edits started; ...` gate.

2. Make the commit decision explicit: no implementation commit is appropriate because the authorized work changes only tracker state; the unchanged current `HEAD` is the final repository identity. If it is not remote-reachable, use the full required `Local-only SHA: ... is not remote-reachable because ...; local-only closeout is acceptable because ...` sentence, citing the user's explicit tracker-closeout authorization.

3. Build the deterministic acceptance manifest and audit scaffold from the saved issue snapshot in `/tmp`. Run the precomposition size/headroom check before filling it. Preserve the approved wording packet verbatim, and fill one exact audit row per acceptance criterion or conformance check with status `satisfied`; every Evidence cell must independently name `atoms:`, concrete tracker `proof surfaces:`, and `sequence:` or a justified sequence N/A. Run the acceptance-exactness challenge against the issue text. Any mismatch changes the row to `blocked` and stops closeout.

4. Start the verification-command ledger for the unchanged `HEAD`. Record the exact no-repository-change check and the packet/audit validation results against that SHA. Mark TDD N/A because no runnable seam exists and the skill was not invoked; mark browser route, console, backend-currentness, and freshness evidence N/A because no browser-consumed surface or repository content changes. Canonical code gates are N/A for this tracker-only wording publication, not silently claimed as run.

5. Before closeout, read and invoke the repository `code-review` skill. Use the unchanged `HEAD` as both resolved fixed point and reviewed repository identity, explicitly record that the repository diff is empty, and review the external packet on both axes: Standards checks the tracker/closeout contract and no-file-change constraint; Spec checks the approved wording packet against every exact issue row. Preserve the resulting `Review:` evidence in the external closeout sink. If the skill requires fallback, use its full two-axis fallback block and validators rather than a one-line substitute. Any unhandled finding stops closeout.

6. Finalize the `/tmp` publishable body after review. It should contain the approved packet, the exact audit, final unchanged `HEAD` SHA, the verification ledger, TDD/browser/Principles N/A or applicable conformance wording, review evidence, current/historical-red/superseded identity inventories, and the superseded-token sweep. Keep repository files untouched and use a stable issue reference rather than publishing the local staging path.

7. Inspect the complete body without truncated output: byte size, bounded excerpts, placeholder/token sweeps, exact audit columns and literal statuses, and all required closeout labels. Run the applicable normal-review validator and the implement closing validator against the exact body and expected `HEAD`; include `--principles` or `--local-only` only if applicable. Immediately before mutation, rerun the implement validator with `--emit-preflight --mutation-ready` and copy its emitted `Closeout preflight:`, `Closeout gate passed:`, and `Post-comment verification next:` lines verbatim. Do not mutate the tracker unless every gate passes.

8. Post the exact body once using the body file and capture the returned comment URL. Before closing, verify the stored GitHub comment is an exact UTF-8 match for the inspected body. On mismatch, transport error, or ambiguous result, perform read-only recovery checks only; do not repost or close until exact storage is proven.

9. Close the issue as completed with a short inline pointer to the verified comment URL. Exact-read the issue number, state, state reason, and URL to prove closure. Finish with `git status --short` and confirm the repository remains unchanged; retain or remove `/tmp` artifacts only after ensuring the published evidence does not claim a removed local artifact remains inspectable.
