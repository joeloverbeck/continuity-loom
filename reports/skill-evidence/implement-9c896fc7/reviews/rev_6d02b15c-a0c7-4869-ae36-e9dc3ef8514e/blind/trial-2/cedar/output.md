1. Read the exact issue body and comments, capture its structured JSON in `/tmp`, and check `git status --short` and `git diff HEAD`. Post the scope ledger before any mutation: the single issue and every exact acceptance row, related-item classification, unrelated dirt, artifact disposition (publish the approved packet, then close the issue), ownership/placement `N/A`, no runnable test seam, and closeout state. If the issue is actually a parent or has blocking children, stop the single-issue path and resolve that scope first.

2. Freeze the repository identity without changing it: record the current `HEAD` as both fixed point and final repository SHA, confirm the diff is empty, check remote reachability, and record that no implementation commit, staging, push, code change, or docs change will occur. If the SHA is not remote-reachable, use the full `Local-only SHA:` sentence, grounded in the explicit tracker-only closeout authorization; block instead if repository policy does not permit that route.

3. Build the deterministic acceptance manifest and a pre-close audit in `/tmp`. Give every exact criterion its own row with literal status `satisfied` and non-circular `atoms:`, `proof surfaces:`, and `sequence:` evidence anchored to the approved packet and issue approval. Record canonical code gates as N/A because the repository tree is unchanged, `TDD evidence: N/A because no tdd skill was invoked and no runnable seam exists`, and browser/console/freshness as N/A because no browser-consumed surface changed. Apply any issue `## Principles` procedure if present; otherwise record `Principles/ADR conformance: N/A`.

4. Invoke the repository `code-review` workflow before closeout, using the current `HEAD` SHA as both resolved fixed point and reviewed HEAD and an explicitly empty `git diff` frame. The Standards axis reviews the no-repository-change/tracker procedure; the Spec axis compares the exact approved packet and audit against every issue criterion. Record the terminal `Review:` evidence. If the review workflow cannot accept the identical-SHA/empty-diff frame, follow its prescribed local two-axis fallback and retain the full fallback block and validator evidence; do not invent a code diff.

5. Freeze one publishable `/tmp` comment body containing the approved wording packet unchanged plus the required closeout envelope: final SHA and reachability, verification-command ledger, review result, TDD/browser N/A dispositions, Principles disposition, exact acceptance audit, and the current/historical-red/superseded evidence-identity inventory and sweep. Run the scaffold size/headroom check, placeholder and byte sweeps, bounded visual inspection, the applicable review validator, and the implement closing validator against the acceptance manifest. Immediately before mutation, rerun the implement validator with `--expected-final-sha`, `--emit-preflight`, and `--mutation-ready`, and copy its emitted preflight and `Closeout gate passed:` output verbatim into the conversation.

6. Post that body once with the issue-comment body-file path and capture the returned comment URL. Verify the exact stored UTF-8 body against the local body file before any close command. On mismatch or readback failure, preserve the URL and mutation result, retry only bounded read-only verification, and never repost unless readback proves the first mutation did not take effect.

7. After exact body verification passes, close the issue as completed without adding a second wording comment; the single verified comment is the durable closeout packet. Exact-read the issue number, state, state reason, and URL, then run a final status/diff check to prove the repository remains unchanged. Report the verified comment URL, closed issue state, unchanged final `HEAD`, reachability/local-only disposition, and any retained or removed `/tmp` evidence artifact.
