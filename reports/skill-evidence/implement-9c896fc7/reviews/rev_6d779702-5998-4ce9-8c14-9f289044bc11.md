# Skill Evolution Review: implement

## Authorization
- Gate rule: `ten_use_unresolved` — 23 qualifying uses on the unchanged target hash with open contemporaneous incidents.
- Trigger event IDs: `evt_482eb48e-9bb2-43e2-be3c-7fe9ec55b667` (threshold-crossing); second open incident adjudicated in the same review: `evt_3d45297f-111b-453c-844a-3788424240b2`.
- Target before hash: `b4857a8635d28a746cbb42f33a3713462b846d0591a71398b8eeabe4129e2f59`
- Fresh-session/cooldown proof: preflight passed the destination-session term against threshold session `dc2cc49b-f78d-47b8-bcea-9a42e6900391`; the claim re-evaluated every authorization term under the store lock and issued `rev_6d779702-5998-4ce9-8c14-9f289044bc11`.

## Evidence adjudication
- Independence result: confirmed. One trigger event, so no duplicate-account risk. The two open incidents come from distinct top-level sessions (`dc2cc49b…` for #151, `4e6bc226…` for #178), carry distinct task fingerprints and materially different tasks, and both are contemporaneous (`retrospective: false`). Both record the current target hash.
- Confirmed mechanism: two defects in the target's own `scripts/validate-closeout-body.mjs`, each reproduced against live code before any candidate existed.
  - **M-A** The satisfied-row unresolved-value check tested `\b(?:TODO|TBD|pending|unknown)\b` against the whole Evidence cell. Citing a real repository identifier whose name contains that vocabulary was therefore rejected as "an unresolved value". Probe: the literal test title `a pending-cooldown gate state never removes a ready target from the census` → flagged. Field consequence in `evt_3d45297f`: a repository test was renamed to `eligible_pending_cooldown …` purely to dodge `\bpending\b`, i.e. the validator coerced a substantive repository change for no substantive reason.
  - **M-B** `concreteProofAnchor` accepted a bare `notes.md` / `config.json` and any slash-bearing source path, but rejected a bare source filename. Probe: `evolution.mjs` → rejected, `status.mjs:226` → rejected, `notes.md` → accepted, `scripts/status.mjs` → accepted. This is the exact `evt_482eb48e` failure. Because slash-paths already admit any source file, the exclusion of bare source filenames carried no gate value — it was an accident of the alternation, not an intent, and it is documented nowhere.
- Target ownership: **target defect**. Both regexes live inside `.claude/skills/implement/scripts/`, and the target's own guidance (`references/closeout-templates.md`) described neither restriction.
- Routed outside target, not repaired here: `evt_3d45297f` also records the **tdd** closing validator rejecting verification-ledger rows reading `passed 21 of 21; failed 0`. That check is in `.claude/skills/tdd/scripts/validate-tdd-closeout-body.mjs`, whose line 927 carries the same class of whole-field placeholder scan. It is stated factually for that owner; no edit was made to it.

## Candidate
- Change hypothesis: a satisfied row should be rejected for an *unresolved value*, not for *citing* an artifact whose name contains gate vocabulary; and a named concrete source file is a concrete anchor on the same footing as the bare `.md`/`.json` filenames already accepted.
- Files changed in isolated candidate:
  - `scripts/validate-closeout-body.mjs` — exempt backtick-, quote-, and typographic-quote-delimited citations from the placeholder scan; name the offending token in the error and state the escape; fold source extensions (`[cm]?[jt]sx?`) into the bare-filename alternation and delete the now-subsumed `test|spec` alternation.
  - `scripts/validate-closeout-body.test.mjs` — three regression tests (quoted-identifier accept in both backtick and quote form, unbackticked prose placeholder still rejected with the new message, bare source filename accepted).
  - `references/closeout-templates.md` — one sentence: source or test file names count as anchors; quote a literal identifier containing the placeholder vocabulary; never rename a repository artifact to satisfy this validator.
- Runtime size before/after: `SKILL.md` + `references/` 171,573 → 171,881 bytes (+308, one sentence). Validator 33,119 → 33,340 bytes.
- Pre-behavioral mechanical correction (the single permitted one): the first candidate exempted only backticked spans, so frozen trial T1 — the field's plain-quoted citation form — still failed. The exemption was widened to quotation marks and the complete frozen suite was rerun from scratch.

## Frozen validation plan
- Risk tier: **high** — the change moves the acceptance boundary of a closeout validation gate, so unintended weakening is the primary risk. Plan frozen at `reviews/rev_6d779702-…/frozen-validation-plan.md` before the candidate existed.
- Paired trials: 6.
  - T1/T1b reproduction of M-A (quoted and backticked literal test title)
  - T2 reproduction of M-B (bare source filenames as the only anchors)
  - T3a–d safety: genuine unresolved values (`proof surfaces: TODO`, `sequence: pending`, `atoms: unknown`, prose `browser check pending`) must stay rejected
  - T4a–c safety: vacuous and circular proof surfaces must stay rejected
  - T5 unrelated core regression: full implement script suite, both versions, staged at equal depth so sibling-skill resolution works
  - T6 blind independent-agent authoring trial, two fresh agents, concealed version assignment
- Deterministic checks: `node --test` over all four implement script test files on both versions; project `npm run lint` and `npm run typecheck` after landing.

## Results
- Current version: T1 `exit 1` unresolved · T1b `exit 1` unresolved · T2 `exit 1` anchor · T3a–d `exit 1` · T4a–c `exit 1` · T5 78/78 pass · T6 2 validate rounds, final cell never quotes the test title (its only surviving trigger word is inside the underscored identifier `eligible_pending_cooldown`).
- Candidate version: T1 `exit 0` · T1b `exit 0` · T2 `exit 0` · T3a–d `exit 1` identical errors · T4a–c `exit 1` identical errors · T5 81/81 pass · T6 1 validate round, cites the artifact by its true name.
- Regressions: none. Every safety fixture rejects identically on both versions; all 78 pre-existing tests pass unchanged; the 3 new tests fail against the unchanged validator, confirming they bind the mechanism rather than the wording.
- T6 caveat: the first T6 execution was **voided** — my sandbox fixture was unrunnable (`status.mjs` called an undefined `projectState`; the test referenced undefined fixtures), and the current-version agent correctly refused to mark the row satisfied on evidence-integrity grounds, so the trial measured nothing about either version. The fixture was repaired until the test was green, both bodies were reset, and the trial was rerun with two fresh agents. Neither agent renamed a repository identifier, so T6 does not reproduce the most severe field consequence; with an author who declines to rename, the pressure surfaced instead as an extra round plus avoidance of the artifact's real name.
- Decision: **accepted**. Resolves both mechanisms on the reproduction cases, noninferior on every protected behavior, no gate weakening, growth minimal and confined to one guidance sentence.

## Landing
- Landed: yes.
- Target after hash: `021824fa09e12a131cc5877f3f4a376eca88dac93fa59e7befeac039781a6760` (from `b4857a86…`). Modified: `references/closeout-templates.md`, `scripts/validate-closeout-body.mjs`, `scripts/validate-closeout-body.test.mjs`. Baseline backed up at `reviews/rev_6d779702-…/pre-land-backup`.
- Post-land verification: landed suite 81/81 pass; live fixture trials match the candidate results exactly; `npm run lint` and `npm run typecheck` clean. `land` reported `mirror_status: broken`, which is pre-existing and by design — `.agents/skills/implement` is a real Codex adapter directory, not a symlink, as `build-closeout-body.test.mjs:1320` asserts; nothing under `.agents/` was touched.
- Final disposition: `resolved_by_change` (terminal outcome `resolved_by_validated_change`), adjudicating `evt_482eb48e-9bb2-43e2-be3c-7fe9ec55b667` and `evt_3d45297f-111b-453c-844a-3788424240b2`.
