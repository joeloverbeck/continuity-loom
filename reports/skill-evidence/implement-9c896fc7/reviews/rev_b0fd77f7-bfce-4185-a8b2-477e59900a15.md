# Skill Evolution Review: implement

## Authorization
- Gate rule: `ten_use_unresolved` (cost cluster; 19 qualifying uses on the current hash, 2 open cost incidents).
- Trigger event IDs: `evt_76a94d94-c636-48c5-a4f4-220a69fdafb0` (authorizing/threshold-crossing; task "implement #146 ratify PRD #145 amendments (tracker-only close)").
- Target before hash: `b4857a8635d28a746cbb42f33a3713462b846d0591a71398b8eeabe4129e2f59`.
- Fresh-session/cooldown proof: helper-recorded on `claim`; review claimed under the store lock, gate re-derived to `review_in_progress`, provisional risk tier.

## Evidence adjudication
- Independence result: confirmed. Two open cost incidents — `evt_76a94d94` (tracker-only #146 close, session `978a52e8`, fingerprint `66e135c6e92ac8a4`) and `evt_8fc672a4` (#149 ASCR-activate + close #145/#132, session `18e018d7`, fingerprint `485f154866ae6cba`) — are genuinely independent (distinct sessions, tasks, fingerprints, run-groups), both qualifying uses on the current hash, contemporaneous (2026-07-21/22).
- Confirmed mechanism: a **tracker-only / zero-code-diff close** has no first-class path. The deterministic scaffold builder needs a manifest + parent/issue-set scope and cannot serve a no-diff close, and the N/A dispositions for the code-implementation-shaped fields (verification ledger, TDD, review, browser, evidence-identity categories, freshness, body-check + gate lines, itemized `--emit-preflight` preflight block, satisfied audit-row proof surfaces) are scattered across SKILL.md, `tracker-closeout-gates.md`, and the parent-rollup template. The builder hand-synthesizes the minimal passing body and discovers requirements through repeated `validate-closeout-body.mjs` rejection rounds (incident 1: 3 rounds).
- Target ownership: **target defect** (missing consolidated zero-diff closeout recipe). `.claude/skills/implement` owns closeout-body guidance; no external contract needed to change. Scope note: incident 2 (`evt_8fc672a4`) is a mix of the structured-ordering mechanism already adjudicated-and-rejected in `rev_6d02b15c` and a squash-merge SHA re-anchoring that is environment novelty already covered by existing re-anchoring guidance; it was left out of scope and remains open.

## Candidate
- Change hypothesis: add one consolidated, hard-gated "Tracker-Only / Zero-Code-Diff Closeout" recipe (copy-ready minimal body with every code-shaped field pre-set to its validator-accepted N/A / git-only disposition) to `references/closeout-templates.md`, plus a one-line SKILL.md §4 pointer, so a zero-diff close is a copy-paste rather than an iterative discovery. Recipe hard-gated to zero-repository-file closes; forbidden once any tracked file changed.
- Files changed in isolated candidate: `references/closeout-templates.md` and `SKILL.md` only (no script changed; `diff -rq` isolated to these two).
- Runtime size before/after: `closeout-templates.md` 57,139 → 62,779 bytes (+5,640); `SKILL.md` 16,184 → 16,567 bytes (+383). Net +6 KB growth in a conditional reference.
- Candidate hash: `01a71dfe395b7a76ff9a7014009fe3f8b2b84efce5b14a3f51470ef694324dc6`.

## Frozen validation plan
- Risk tier: high (guidance governs external tracker actions and shared TDD/review closeout conventions).
- Paired trials: five — T1 tracker-only zero-diff reproduction, T2 docs-only boundary (no over-capture), T3 ordinary single-issue code close, T4 parent-rollup everything-active close, T5 one-line-code safety (SEVERE if failed). Run version-blind by independent agents.
- Deterministic checks: D1 candidate `build-closeout-body.test.mjs` (incl. the guidance-conformance test that killed `rev_6d02b15c`); D2 `validate-closeout-body.test.mjs`; D3 filled recipe body clears `--closing --emit-preflight --mutation-ready` one-shot; D4 isolated diff/size; D5 capture + verify test suites.
- Frozen plan: `reviews/rev_b0fd77f7-bfce-4185-a8b2-477e59900a15/frozen-validation-plan.md`.

## Results
- Current version (B):
  - T1 reproduction: passing mutation-ready body, **0 rejection rounds** (agent front-loaded by reading the 797-line validator + 2 long references; ~110k tok / 400s).
  - T2/T3/T4: correct normal / docs-only / parent-rollup closeouts, all real evidence present.
  - T5: real verification + real review, no shortcut.
- Candidate version (A):
  - T1 reproduction: passing mutation-ready body, **0 rejection rounds**; found and copied the recipe via the SKILL §4 pointer; effort LOW (one self-contained block, no field discovery; ~66k tok / 152s).
  - T2 docs-only: normal path with real 3-file verification, Review present; recipe correctly refused ("never applies once any tracked file changed"); flagged the clean-`git status` ≠ empty-diff trap.
  - T3 ordinary code: normal path, all four evidence fields present; recipe explicitly gated out.
  - T4 parent-rollup: correct child-family route, all three validators, all real evidence; recipe "fenced off."
  - T5 one-line-code safety: real verification + real review, NOT N/A; refused the recipe quoting its one-line-code guard and calling the recipe's clean-diff ledger line "factually false … explicitly forbidden here."
- Deterministic: D1 26/27 (sole failure is a locational adapter test — confirmed passes 27/27 at the live canonical path; the guidance-conformance test **passes**); D2 43/43; D3 one-shot pass (exit 0); D5 8/8; D4 isolated to the two intended files.
- Regressions: none. T2–T5 noninferior with no material or severe regression; the T5 safety invariant (no weakening of verification/review gates) was preserved — the recipe's explicit gating actively blocked misuse in every non-zero-diff trial. Deterministic checks show no script/template regression.
- Decision: **rejected.** The frozen primary reproduction gate — T1 *strictly fewer* rounds-to-pass — was **not met**: both the candidate and the current skill reached a first-pass mutation-ready tracker-only body (0 vs 0). The candidate's real advantage was lower effort (~40% fewer tokens, ~2.6× faster, copy-one-block vs reverse-engineering the validator), which was pre-registered as observational data, not the frozen pass gate; adopting it as the gate post-hoc would be goalpost-moving. The acceptance tie-breaker prefers the candidate only when "meaningfully smaller or clearer"; the candidate is +6 KB growth (larger). A capable executor already follows the current skill to the same result, so the head-to-head did not demonstrate the candidate materially better on the frozen metric.
- Raw outputs / blind evaluations: `reviews/rev_b0fd77f7-bfce-4185-a8b2-477e59900a15/blind/` (version-map, per-trial verdicts, results-matrix); deterministic evidence in `reviews/.../deterministic/`.
- Validation event: `evt_852c5096-bd1b-49d3-9b35-89189bb29a0e`.

## Landing
- Landed: no.
- Target after hash or unchanged hash: unchanged `b4857a8635d28a746cbb42f33a3713462b846d0591a71398b8eeabe4129e2f59` (git status of `.claude/skills/implement` clean; live skill never edited).
- Final disposition: `candidate_rejected_validation` (adjudicated `evt_76a94d94`; `evt_8fc672a4` left open as out of scope).
- Terminal outcome: `candidate_rejected_validation`.
- Landing step: not reached — `candidate_rejected_validation`.
