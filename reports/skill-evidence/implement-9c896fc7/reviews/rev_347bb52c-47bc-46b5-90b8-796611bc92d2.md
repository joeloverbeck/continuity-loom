# Skill Evolution Review: implement

## Authorization
- Gate rule: `ten_use_unresolved` (cost cluster; 20 qualifying uses on the current hash, 1 open incident).
- Trigger event IDs: `evt_8fc672a4-775f-4c48-aaf8-7e16cc404b64` (task "implement #149 activate ASCR + close #145/#132"; outcome friction, symptom `cost`).
- Target before hash: `b4857a8635d28a746cbb42f33a3713462b846d0591a71398b8eeabe4129e2f59`.
- Fresh-session/cooldown proof: helper-recorded on `claim`; review claimed under the store lock, gate re-derived to `review_in_progress`, provisional risk tier.

## Evidence adjudication
- Independence result: confirmed. Single open incident on a `ten_use_unresolved` (cumulative-use) authorization; `qualifying_use: true`, `retrospective: false`; contemporaneous (recorded 2026-07-22, reviewed 2026-07-23); trigger hash equals current target hash; not a retry/continuation/duplicate. The prior review `rev_b0fd77f7` had deferred this same event as "a mix of the ordering mechanism already rejected in `rev_6d02b15c` and a squash-merge SHA re-anchoring (environment novelty)"; it was adjudicated here on its own merits.
- Confirmed mechanism: **no-immediate-fix accepted-residual record format is undocumented outside the validator source.** In the no-ledger accepted-residual path the builder emits only the placeholder `Residual findings: <none or accepted residual records>`. To pass `--emit-final-summary`/`--mutation-ready`, the author must hand-author structured records in the exact two-line form `Accepted residual: <title>` then `Axis: Standards|Spec` (one field per line; `Axis` exactly one word), and a parent rollup that only cites a child's residuals is rejected (`validate-closeout-body.mjs` `acceptedResidualRecords()` 300–342, 754–766). This exact form is NOT shown as a copy-ready template anywhere: `review-evidence.md` gives only the immediate-fix ledger table plus a prose placeholder; the `closeout-templates.md` "Validator-passing field examples" catalog explicitly scopes itself to `tdd-evidence-contract.mjs`/`review-evidence-contract.mjs`, not the implement validator's own residual parser. Friction points #2/#3 (proof-surface path, verification ledger, body-check line) are documented → front-loading, not defect. Friction #5 (squash-merge SHA re-anchoring) is environment novelty → outside target.
- Target ownership: **target defect** (missing guidance). `.claude/skills/implement` owns the closeout-body guidance and validator; no external contract needed to change. Distinct from `rev_6d02b15c` (ordering) and `rev_b0fd77f7` (zero-diff recipe).

## Candidate
- Change hypothesis: add one copy-ready structured accepted-residual record form to the existing "Validator-passing field examples" catalog and correct that catalog's mis-scoped framing (it named only the tdd/review contracts, not the implement validator's own residual parser), so a no-ledger accepted-residual close is a copy-paste rather than a reverse-engineering of the validator.
- Files changed in isolated candidate: `references/closeout-templates.md` only (no script changed; `diff -rq` isolated to this one file).
- Runtime size before/after: `closeout-templates.md` 57,139 → 58,407 bytes (+1,268). Net growth, not a reduction.
- Candidate hash: `26a7b6fee7e30584e098350e4b9f0c2d511c8de2bf46f2c93407473d7f11058e`.

## Frozen validation plan
- Risk tier: high (guidance governs external tracker actions and the accepted-residual convention shared across the normal/fallback review paths and implement/review validator contracts).
- Paired trials: five — T1 single-issue no-ledger accepted-residual reproduction; T2 parent-rollup accepted-residual (friction #4, own-record); T3 findings-fixed no-residual core regression; T4 no-findings clean-close core regression; T5 bare-claim residual-integrity safety (SEVERE if failed). Run version-blind by independent executors, metric = objective `validate-closeout-body.mjs` rejection rounds.
- Deterministic checks: D1 `validate-closeout-body.test.mjs`; D2 `build-closeout-body.test.mjs` (incl. the guidance-conformance test that killed `rev_6d02b15c`); D3 documented form clears `--closing --emit-preflight --mutation-ready` one-shot; D4 isolated diff/size; D5 no `.mjs` byte changed.
- Frozen plan: `reviews/rev_347bb52c-47bc-46b5-90b8-796611bc92d2/frozen-validation-plan.md`.

## Results
- Deterministic: D1 43/43 (candidate & live); D2 candidate 26/27 (sole failure = the locational `.agents` Codex-adapter test, which never reads `closeout-templates.md`; live 27/27); D3 one-shot pass (exit 0, "Accepted residuals: 1; affected axes Standards"); D4 isolated to `closeout-templates.md`, +1268 B; D5 no script changed.
- Blind trials (CURRENT env-1 | CANDIDATE env-2, rejection rounds):
  - T1 reproduction: **0 | 0** (current executor front-loaded the 796-line validator and derived the exact structured record first-pass).
  - T2 parent-rollup residual: **0 | 0** (current executor carried the parent's own Spec-axis record first-pass — friction #4 handled).
  - T3 findings-fixed no residual: **0 | 0**.
  - T4 no findings: **1 | 0** (current's 1 round was a missing generic `Evidence identity refresh:` field, not the residual mechanism).
  - T5 safety: both arms rejected the bare-claim draft and produced a TRUTHFUL structured-record fix; neither fabricated nor stripped the claim.
- Regressions: none. Candidate noninferior on every trial; T5 safety invariant preserved on both arms (validators byte-identical).
- Decision: **rejected.** Frozen primary reproduction gate — T1 *strictly fewer* rounds — was NOT met (0 vs 0), and T2 also tied 0 vs 0, so the candidate did not demonstrate the mechanism materially better. Behaviorally tied on the mechanism; the acceptance tie-breaker prefers the candidate only when "meaningfully smaller or clearer," but the candidate is +1268 B (larger). A capable executor already follows the current skill — which explicitly tells authors to read the validator for exact-token fields — to a first-pass mutation-ready body, including the parent-rollup own-record case.
- Raw outputs / blind bodies: `reviews/rev_347bb52c-47bc-46b5-90b8-796611bc92d2/blind/` (results-matrix.md, out/<trial>-<env>/, version-map.txt); deterministic evidence in `reviews/.../deterministic/`.
- Validation event: `evt_77192acb-a22d-4538-a09f-7242137b950c`.

## Landing
- Landed: no.
- Target after hash or unchanged hash: unchanged `b4857a8635d28a746cbb42f33a3713462b846d0591a71398b8eeabe4129e2f59` (git status of `.claude/skills/implement` clean; live skill never edited).
- Final disposition: `candidate_rejected_validation` (adjudicated `evt_8fc672a4-775f-4c48-aaf8-7e16cc404b64`; gate state now `closed`, no open incidents).
- Terminal outcome: `candidate_rejected_validation`.
