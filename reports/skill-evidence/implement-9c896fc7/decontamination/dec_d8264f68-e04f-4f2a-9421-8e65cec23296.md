# Legacy Skill Decontamination: implement

## Eligibility
- Legacy basis: `owner-confirmed`; mechanical history confirmed that `.claude/skills/implement` predates retirement of the old audit workflow.
- Target before hash: `b4857a8635d28a746cbb42f33a3713462b846d0591a71398b8eeabe4129e2f59`.
- Prior decontamination status: eligible and not previously completed for this baseline.

## Current-skill baseline
- Core behaviors: exact live-scope intake; unrelated-dirt ownership; criterion-level TDD/verification; browser and stateful-fixture safety; fixed-point review with review-fix refresh; generated/validated closeout; exact comment/state/commit readback; child-before-parent ordering; and blocked handoff without speculative mutation.
- Runtime size and mandatory references: 23,304 Markdown words / 171,573 bytes across the entrypoint and six phase-selected references. Seven executable non-test helpers own manifest, scaffold, validation, split-core, capture, and exact-comment contracts.
- Paired-trial corpus: seven frozen trials covering a docs-only issue, a greenfield parent/child family, browser-visible UI, a direct resume boundary, review-fix closeout, a blocked prerequisite, and a tampered resume handoff.

## Accretion analysis
- Preserved invariants: live authority and dependency reads; dirty-tree ownership; proof preflight; exact acceptance atoms/proof surfaces/sequence; repository TDD and code-review delegation; final-tree verification; browser/backend freshness; two evidence sinks; mutation-ready closeout; exact readback; and family ordering.
- Removed or distilled audit-induced material: in the isolated candidate only, repeated literal gates, copied fallback templates, validator-regex repair lore, incident-specific examples, duplicated evidence schemas, and custody recitations were reduced to concise state transitions and canonical helper/skill pointers.
- Uncertain material retained: all live baseline material remains retained because the candidate failed noninferiority. The candidate also retained all seven executable helpers unchanged and kept conditional browser, split-body, private-fixture, and blocked-closeout branches.

## Candidate
- Files changed: isolated `SKILL.md`, six reference documents, and two prose-conformance test files; seven executable non-test helpers were byte-identical. Diff: 300 insertions, 1,015 deletions across nine files.
- Runtime size before/after: 23,304 to 4,004 Markdown words (-82.8%); 171,573 to 30,367 Markdown bytes (-82.3%).
- Structural simplifications: a four-phase state machine replaced repeated narrative gates; exact syntax was delegated to existing helpers and canonical `tdd`/`code-review` owners; rare browser, split-body, and blocked paths stayed conditional.

## Comparative validation
- Trial results: after randomized decoding, candidate 2 wins, 2 ties, and 3 losses. It won browser-visible UI and direct-resume trials, tied the greenfield-family and blocked-dependency trials, and lost docs-only, review-fix, and tampered-handoff trials.
- Deterministic checks: isolated candidate suite passed 78/78 tests; diff whitespace check was clean. Candidate hash: `44b644d10f2fb28ac5c7fcc8fb1686e89c985b798db751ce18f6fd88b62721c3`.
- Regressions or uncertainty: two material regressions. In trial 01 the candidate response could impose an unauthorized push/remote-reachability prerequisite. In trial 05 it remained procedural instead of resolving concrete final-tree and commit-visibility outcomes. No severe regression was found, but either material regression is sufficient to fail the landing gate. Full decoding is in the run's `validation-summary.md` and paired evaluator artifacts.

## Decision
- Outcome: `candidate_rejected_validation`.
- Landed: no.
- Target after hash or unchanged hash: unchanged at `b4857a8635d28a746cbb42f33a3713462b846d0591a71398b8eeabe4129e2f59`; the live target is byte-identical to the claimed baseline snapshot.
