# Trial 02: Greenfield issue family

You are operating in a nearly empty repository. Do not execute commands or edit files in this synthetic trial. Using the supplied implementation skill, write the ordered execution transcript and closeout ledger you would use.

User prompt: `Implement GitHub issues #2 to #9. They are children of PRD #1. Once they are finished and closed, close the PRD too.`

Fixture:

- The live issue bodies define a public rules-engine seam and dependency order.
- There is no package manifest or test runner yet.
- A checked-in Python harness is the named fidelity oracle; one child issue's prose conflicts with its refill timing.
- Work includes workspace bootstrap, deterministic fixture export, a TypeScript port, replay/parity tests, CI, and docs.
- Unrelated documentation edits already exist.
- The parent may close only after exact verification of children #2 through #9.

Output only the proposed execution/closeout transcript. Do not mention decontamination or compare skill versions.

