# Trial 03: Browser-visible TDD issue

Do not execute commands or edit files in this synthetic trial. Using the supplied implementation skill, write the ordered execution and evidence plan.

User prompt: `Implement issues #70 and #71, close them, then close parent #69.`

Fixture:

- #70 adds a shared React `Glyph` primitive with runtime validation.
- #71 retrofits a game's market cards, player mat, and legend; its acceptance criteria explicitly require a browser screenshot and preservation of enabled action anchors.
- The issue bodies reference one dirty ADR and one untracked context document as pending authority that must land with the work.
- The repository has focused package tests, parity tests, a Vite dev server, and a browser wrapper.
- The default dev ports may already be occupied.

Output only the proposed execution/evidence/closeout transcript. Do not mention decontamination or compare skill versions.

