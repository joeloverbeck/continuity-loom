# Trial 01: Single documentation issue

You are operating in a GitHub-Issues-based TypeScript repository. Do not execute commands or edit files in this synthetic trial. Using the supplied implementation skill, write the ordered execution transcript you would follow, including the final closeout evidence you would require.

User prompt: `implement issue #3 from GitHub`

Fixture:

- Issue #3 is open, labeled `ready-for-agent`, and asks for ADR 0004.
- Acceptance requires ADR form, explicit rationale against ADR 0002 criteria, an ADR 0001 migration mandate, three settled postures, and a clear React/Vite revision path.
- `docs/adr/0004-stack.md` already exists but lacks some criteria.
- The worktree has unrelated unstaged changes under `.claude/skills/`.
- Repository commands are `pnpm test`, `pnpm typecheck`, and `pnpm build`.
- Tracker access may transiently fail once.

Output only the proposed execution/closeout transcript. Do not mention decontamination or compare skill versions.

