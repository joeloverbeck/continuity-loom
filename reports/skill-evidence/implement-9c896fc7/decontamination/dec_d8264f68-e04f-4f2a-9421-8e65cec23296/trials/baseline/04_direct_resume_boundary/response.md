Decision recorded: use the single `BoardFacts` seam. That resolves the paused design choice, so I resume the existing implementation checklist at the first-edit boundary; I do not restart source intake or ask for the seam decision again. There is no PRD or implementation issue for this direct request, so issue discovery, an acceptance manifest, tracker comments, and issue closure are N/A. I do not create an issue merely to continue.

Before the first edit, I rerun `git status --short`, replace the old worktree snapshot with the refreshed one in the progress note, and continue to leave every unrelated dirty path untouched. I revalidate only an active server, browser session, in-flight command, or proof artifact that the next step depends on; if none exists after the decision pause, I record that no active proof state is being reused. I update the existing checklist with the approved seam, its owning module and placement rationale, the exact acceptance atoms and proof surfaces, the TDD seam, and the intended disposition of any active implementation artifact.

`Scope ledger posted: yes; no edits started; unrelated dirty files re-listed from the refreshed status or N/A if clean; in-scope issues none because this is a direct user-authorized implementation; related tracker classification N/A; artifact disposition listed or N/A; ownership/placement decisions single BoardFacts seam -> its existing board-state owner, with the governing principle/ADR basis or N/A and placement rationale recorded.`

Next exact evidence action: invoke the repository TDD workflow at the now-approved `BoardFacts` seam. The user's decision is the seam confirmation, so there is no second confirmation pause. I establish the pre-red baseline, add the smallest highest-practical-layer test that expresses the agreed BoardFacts contract, run it, and verify that it fails for the intended missing behavior. I then make the minimum implementation change at that seam, keep flow-specific behavior out of broader shared helpers, and rerun the exact focused command to green. Any unexpected or wrong-reason failure is recorded separately and repaired before it is used as red evidence.

I keep a verification-command ledger from the first result used as evidence, including each exact command, observed result and counts, run count, and represented working tree or SHA. I run focused tests and typechecks during the change. If the resulting diff changes UI, routes, a browser-consumed API, fixtures, or the reachable action path, I run a real final-tree browser smoke through the production route and record the route, action path, observed outcome, console state, proof-server ownership/currentness, and cleanup. Otherwise I record browser evidence N/A with the concrete unchanged-surface reason. I then run every canonical root gate required by the repository guidance on the final tree.

Before review, I draft the working pre-close audit against every item in the direct implementation checklist. Each row has `Acceptance criterion or conformance check` and `Status` columns; every satisfied Evidence cell names `atoms:`, `proof surfaces:`, and `sequence:` with ordered proof or a justified sequence N/A. Unsupported rows remain `blocked` or `not done` and are fixed before I call the implementation complete. I reconcile artifact disposition and rerun `git status --short`.

`Implementation pre-stage gate passed: working pre-close audit drafted in the visible implementation ledger; blocked/not done rows none; artifact disposition reconciled/N/A; ownership/placement decisions recorded; unrelated dirty files listed/N/A.`

If a local implementation commit is appropriate, I stage only implementation-owned paths, inspect `git diff --cached --name-only`, post the implementation commit gate with that exact staged list, and commit. If no commit was requested or repository policy makes a commit inappropriate, I leave the verified change in the working tree and explicitly use the permitted pre-commit review route rather than inventing a final SHA.

Once implementation is ready, I invoke the repository `code-review` workflow against a fixed point: the pre-implementation commit through the final implementation commit, or explicitly `git diff HEAD` when no committed fixed point exists. I handle Standards and Spec findings, use red-first evidence for behavior-changing fixes, rerun affected and canonical verification, and refresh browser/manual freshness when a fix touches a covered surface. Review does not become current until it covers the final tree. I then refresh current, historical-red, and superseded evidence identities and run the superseded-token sweep.

The closeout is repository-local, not tracker closeout. I do not run any tracker mutation or claim an issue was closed. My final report names the changed paths and behavior, the exact final verification results, the TDD evidence sink, the fixed-point review result, browser evidence or its justified N/A, the commit SHA if one was intentionally created or the explicit no-commit decision, and the final `git status --short` disposition, including untouched unrelated dirt and retained verification artifacts.
