# T02 — Continuation After Acceptance: Operator Packet

## Run charter

- Mode: `continuation`.
- Prior report: `reports/playtest-the-unbidden-oath-2026-07-17T104952Z.md`.
- Existing project locator: `/tmp/continuity-loom-playtest-projects/the-unbidden-oath-2026-07-17T104952Z`.
- Opening authority: visibly verify Accepted Segment 1 before editing. Its prose is reviewable evidence, not automatic continuity authority.
- Boundary: accept exactly one new local segment—expected sequence 2—or stop with an honest blocker.
- POV and portrayal: close-third Hina. Her obsessional OCD remains ego-dystonic distress and covert checking, never magic, intent, prophecy, violence propensity, power, or cure-by-transport/combat.
- Required action:
  - Begin immediately after Hina grips the shrine sword.
  - Preserve that Calen asked her to cover him, not replace him at the crossbar.
  - Hina draws the ordinary, still-dormant sword.
  - She moves behind and inward of injured Calen.
  - They withstand one sustained impact that does not breach the door.
  - After the outside scraping stops, they share several listening breaths.
- Exact endpoint: Calen warns Hina not to lower the sword. Stop before Hina responds, the door opens, the threat is identified, combat begins, the sword activates, or Veyr/the transfer is explained.
- Quantitative ledger: inactive; no quantitative comparison was requested.
- Method pilots: all authoritative states are `awaiting-disposition`, so no first-view witness, paired draw, independent claim challenge, or method-register row is run.

## Guarded setup

1. Run the preparation helper with title `The Unbidden Oath` and the exact prior-report path. Use its unique report stem, evidence directory, scratchpad, exchange directory, isolated settings directory, and existing project path.
2. If the prior report lacks the `/tmp` path, or that project no longer exists, produce a blocked report with `continuation-project-missing`; never reconstruct or replace the story.
3. Capture `git status --short` before any run-owned repository write and preserve unrelated dirt unchanged.
4. Seal the continuation intent, stop boundary, cast functions, knowledge locks, expected records, expected assistance, and mental model before launch.
5. Build the app, then start a fresh isolated production process:
   - bind only `127.0.0.1` on an ephemeral port;
   - set `OPENROUTER_API_KEY` explicitly blank;
   - use the run-specific settings directory.
6. Before first navigation, start a fresh 1440×900 guarded Chromium session. Capture console, page, network, provider-send, and external-origin diagnostics from pre-navigation onward.
7. Never click Generate, Ideate-send, Analyze, Review, model refresh, or another provider control. Any attempted provider request ends the run immediately as `blocked / provider-request-attempt`.

## Visible author journey

1. Open the existing project through the visible Open form. Confirm its title and exact path.
2. Visit Accepted Segments before changing state. Confirm sequence 1 and its hand-on-hilt endpoint. Record whether the continuation point is discoverable and distinguish its prose from Records and Generation Brief authority.
3. Inspect current canonical state through visible surfaces only:
   - Hina and Calen remain onstage.
   - Hina is POV and physically active.
   - Calen remains injured, at the crossbar, physically active, and the segment’s speaker.
   - The unidentified outside pressure remains offstage.
   - The door is barred and unbreached.
   - The sword is ordinary and dormant.
4. Curate only the working-set records required for this unit. Do not import accepted prose into prompt-facing fields.
5. If grounded alternatives for the outside-pressure change are genuinely useful, invoke Ideate’s prompt-inspection path. Otherwise skip it and record that the beat was already sufficiently determined.
6. Author the Generation Brief through visible controls. Before compilation, record the intended influence of each populated field, especially:
   - begin immediately after the grip;
   - current positions and Calen’s continued crossbar role;
   - sword custody and dormancy;
   - one sustained non-breaching impact;
   - cessation of scraping and several listening breaths;
   - Hina’s ego-dystonic checking pressure;
   - unknown threat, unexplained transfer, and locked sword capability;
   - exact warning endpoint and prohibited continuation.
7. Save, inspect readiness, and resolve blockers only through their canonical UI fixes. Do not weaken or bypass validation.
8. At safe points, perform a light keyboard-focus, naming, clipping, scrolling, and long-form readability check without derailing the journey.

## Cold prompt evaluations

For every naturally invoked prompt:

1. Predeclare the author need and expected useful output.
2. Open the exact visible prompt inspector and record displayed kind, fingerprint, versions, scope, counts, and readiness/source disclosures.
3. Search the visible prose prompt for every distinctive populated Generation Brief value and record `present`, `absent`, or `unclear`.
4. Extract the exact visible prompt with `text-file` into the run’s `/tmp` exchange directory without printing it.
5. Give a genuinely fresh, non-forked context only the prompt-file instruction. Do not provide the prior report, story summary, desired answer, suspected defect, or earlier response.
6. Inspect the untouched response before editing. Record contract compliance, grounding, unsupported invention, omissions, immediate utility, adoption, burden, likely layer, verdict, and confidence.

Specific handling:

- Ideate, if naturally invoked: require grounded, locally usable, non-prose options for the outside-pressure change. Adopt nothing automatically; manually re-author only a chosen option.
- Prose: allow one initial cold response and one unchanged-prompt retry only if the first is materially poor or malformed. No third attempt.
- Segment Reconciliation: invoke after acceptance because explicit durable changes are expected. It may identify only movement, sword custody, bar/door state, outside pressure, and handoff deltas.
- Record Hygiene: do not invoke before acceptance. Invoke only after the accepted segment has caused deliberate CLOCK, LOCATION, and OBJECT edits.
- No paired draw is collected because that pilot is awaiting disposition.
- Provider parsing and result cards remain untested; cold outputs never enter provider-result surfaces.

## Acceptance gate

A prose response is eligible for the candidate editor only if it:

- starts after Segment 1’s grip without replaying the opening;
- keeps Hina’s OCD intrusive, unwanted, non-causal, and non-supernatural;
- preserves Calen at the crossbar and Hina as cover;
- has Hina draw and hold an ordinary dormant sword;
- places her behind and inward of Calen;
- renders exactly one sustained impact without breach;
- allows several listening breaths after scraping ceases;
- preserves the unidentified threat and all knowledge/reveal locks;
- ends on Calen’s warning before any prohibited next event.

Then:

1. Preserve the raw verdict.
2. Use **Write or paste candidate** and `fill-file`.
3. Make only ordinary visible author edits; record intervention as `none`, `light`, `substantial`, or `rewrite`.
4. If neither allowed prose attempt can pass without replacement-level rewriting, stop with `prose-quality-blocker`.
5. Accept exactly once.
6. Confirm visibly in Accepted Segments that sequence 2 exists and has the required endpoint. Without that proof, do not claim completion.

## Post-acceptance continuity gate

Accepted Segment 2 remains evidence, not canon. After inspecting the durable-change reminder:

1. Evaluate Segment Reconciliation cold.
2. Manually apply only explicitly supported changes through canonical editors:
   - movement: Hina behind/inward of Calen; Calen remains at the bar;
   - object custody: the sword is drawn and held by Hina but remains ordinary and dormant;
   - bar/location: door remains closed, barred, and unbreached;
   - pressure/CLOCK: one sustained impact occurred and scraping stopped, only to the extent explicitly rendered;
   - handoff: both listen; Calen has warned her not to lower the sword; Hina has not answered;
   - knowledge: threat, sword capability, Veyr, and transfer remain unresolved.
3. Do not infer combat readiness, magical activation, prophecy, intent, or new threat identity.
4. After CLOCK, LOCATION, and OBJECT changes are saved, inspect Record Hygiene’s exact visible prompt cold. Manually adopt only safe, concrete corrections.
5. Leave the Generation Brief and working set ready for a later continuation; do not begin another prose unit.

## Evidence packet

Maintain contemporaneous scratchpad entries separating:

- visible fact;
- author interpretation;
- expected versus actual;
- classification, severity, confidence, and frequency;
- recovery and intervention cost;
- prompt and field-influence verdicts;
- acceptance and reconciliation milestones.

Retain only finding-specific, privacy-safe evidence, such as:

- project title/path confirmation;
- tightly cropped sequence-1 and sequence-2 metadata/boundary proof;
- readiness or prompt metadata without prompt bodies;
- durable-change reminder and canonical-update confirmation;
- relevant console/network/provider-safety entries.

Do not retain full prompts, record payloads, raw responses, candidate or accepted prose, project files, browser traces, keys, routine screenshots, or uncited artifacts.

Carry forward inherited findings `F001`–`F021`. Allocate new findings from `F022`. Mark inherited items resolved only after explicit current-run retesting; otherwise use `open`, `repeated`, `not-retested`, or `preserve-strength`.

## Blocker handling

For a probable blocker:

1. Stop and record the visible state and last successful action.
2. Capture privacy-safe evidence.
3. Inspect only existing diagnostic streams and narrowly relevant visible-tree/geometry/DOM evidence.
4. Try one safe visible recovery, one reload if persistence should be safe, and at most one fresh guarded browser against the same app and project.
5. Never restart into a replacement project, use APIs/files to advance state, or conceal the initial failure.
6. If still blocked, preserve the project and produce the complete blocked report with unreached sections explicitly stubbed.

## Report and terminal handling

Produce a new cumulative schema-v2 report at `reports/<new-playtest-stem>.md` with:

- `run_mode: continuation`;
- the exact prior report and unchanged project path;
- `accepted_segment_sequence: 2` only after visible proof;
- truthful provider, cold-attempt, counterfactual, and intervention counters;
- all method-pilot counters at `0`;
- all sixteen required sections;
- the inherited cumulative ledger plus current findings;
- a continuation handoff naming sequence 2 and the unresolved moment immediately after Calen’s warning.

Only after the source-and-doc-blind journey ends, consult repository artifact-routing instructions solely for closeout; do not revise sealed expectations or observations from them.

Close the guarded browser via its shutdown request, then the isolated app. Preserve safety counts first. Remove session plumbing, empty streams, routine screenshots, and forbidden evidence. Validate the report, retain scratch/exchange files until the first pass, delete them, then validate again.

Because all method pilots await disposition, do not update the method register or pilot-state table. Compare final worktree status with the baseline; only the new report and its declared evidence directory are permitted run-owned deltas.

After successful validation, remove the exact run root under `/tmp/continuity-loom-playtest/` while preserving the separate story project. The terminal response must name the validated report, state `completed` or `blocked`, confirm provider-request counts, identify the latest visibly verified sequence, and report custody status.
