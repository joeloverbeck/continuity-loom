# T04 Operator Packet — Continuation Specificity Test

## Packet status

This is an instruction-level run packet, not a completed playtest report. No app
or browser has been launched, no project data has been opened or changed, no
prompt has been dispatched, and no provider request has been made. Every path,
sequence, prompt fingerprint, response verdict, and finding described below must
be observed in the future guarded run rather than inferred here.

## Run charter

- **Mode:** continuation.
- **Story:** *The Winter Letter*.
- **Prior report:**
  `reports/playtest-the-winter-letter-2026-07-19T022000Z.md`.
- **Project locator:** use only the prior report's exact `project_path`. It must
  be under `/tmp` and still exist. Do not derive another path from the title,
  search for a substitute, or reconstruct the project from report prose. If the
  locator is absent, unsafe, or missing, terminate with
  `continuation-project-missing` and write a complete blocked report.
- **Completion boundary:** visibly accept exactly one new local prose segment.
  Because the supplied task identifies the existing accepted endpoint as
  sequence 1, the expected new sequence is 2, but completion may claim sequence
  2 only after the archive visibly proves it. Perform deliberate continuity
  review after acceptance, then stop; do not compile or accept another segment.

At the start of the actual run, read the named prior report completely and no
unrelated report. Copy its exact project locator, established story contract,
voice/POV constraints, continuation handoff, and entire cumulative finding
ledger into the scratchpad. Treat that material as returning-author context,
never as context for a cold evaluator.

Before app launch, seal this creative contract in the prepared scratchpad:

- The segment remains in Clara's sanatorium room on Christmas Eve 1972.
- Clara and Tomás are the onstage participants and speakers, subject to any
  stricter presence or POV facts in the prior handoff. Daniel is physically
  offstage and comatose in 1972. Approaching staff are an offstage pressure, not
  an invitation to stage a staff entrance or move the scene.
- Segment 1's required visible endpoint is that Clara postponed action until the
  next staff round and asked Tomás for three Daniel-specific facts. Verify that
  endpoint in **Accepted Segments** before changing any state.
- Clara lends Tomás a pencil. Tomás writes exactly three facts on his own
  paperwork. Clara keeps the letter and controls its distance, the evidence
  test, and the extent of any commitment.
- The author chooses the three facts before prose compilation. For each, record
  in the scratchpad: the exact fact, why it is Daniel-specific, a plausible
  independent source path, who currently knows it, and what it cannot establish.
  Author those decisions through the appropriate visible canonical record or
  Generation Brief control supported by the UI. Do not ask the prose evaluator
  to invent them and do not silently elevate suggestions into story authority.
- The facts can furnish a credible path to Daniel or a way to test Tomás's
  specificity. They cannot prove time travel, establish Daniel's eventual
  recovery, supply a waking date, or force Clara to believe Tomás.
- Preserve the physical constraints: no one leaves the room, Daniel does not
  appear or communicate, the letter does not leave Clara's control, and no
  rescue logistics begin.
- Preserve Clara's knowledge and agency: she may question provenance,
  specificity, and testability, but she neither accepts the extraordinary claim
  as proven nor commits beyond this local exchange.
- **Exact stop:** after Clara has tested all three facts and their possible source
  paths, end with her retaining the letter and deciding whether to permit one
  further source-path clarification before the next staff round. Leave that
  choice unresolved. Staff pressure may be perceptible, but it must not displace
  the conversation before the stop.
- Remain within the prior report's established voice, prose mode, maturity
  envelope, content constraints, and reveal locks. Any conflict between this
  packet and a stricter inherited constraint stops preparation for an explicit
  resolution; do not improvise around it.

The first-time/returning-author mental model to test is: visible accepted prose
shows where the story stopped; canonical records and the Generation Brief
control what the next prompt knows; candidate prose remains a reviewable draft;
and acceptance does not automatically convert prose into continuity authority.
Non-goals are exploration of other scenes, systematic feature coverage,
provider-backed generation, accessibility auditing, record cleanup unrelated to
this segment, or planning past the reversible stop.

## Guarded setup and custody

1. From the repository root, allocate unique paths with:

   ```bash
   node .claude/skills/playtest/scripts/prepare-run.mjs \
     --story-title "The Winter Letter" \
     --prior-report reports/playtest-the-winter-letter-2026-07-19T022000Z.md
   ```

   Use every path in its receipt exactly. Do not substitute a repository path
   for project, scratch, exchange, browser, config, or routine screenshots.

2. Before any run-owned repository write other than the prepared evidence
   directory, append the exact `git status --short` baseline to the scratchpad.
   Existing changes remain user-owned and uninspected.

3. Verify from the prior report, without inspecting the project filesystem, that
   `project_path` is an absolute `/tmp` path and exists. The UI is the only means
   of opening or modifying it.

4. Resolve the repository root once with `pwd -P`, run `npm run build`, and start
   a fresh isolated app using literal absolute paths:

   ```bash
   node /absolute/repository/root/.claude/skills/playtest/scripts/safe-app-session.mjs \
     --repo-root /absolute/repository/root \
     --session-dir /tmp/continuity-loom-playtest/<stem>/app \
     --config-dir /tmp/continuity-loom-playtest/<stem>/config
   ```

   Wait for `app-session.json` and use its exact `baseUrl`. The holder must bind
   only to `127.0.0.1` on its ephemeral port, use isolated settings, and have a
   blank `OPENROUTER_API_KEY`. Never reuse another app process.

5. Before any navigation, start the guarded 1440×900 browser:

   ```bash
   node /absolute/repository/root/.claude/skills/playtest/scripts/browser-session.mjs \
     --evidence-dir /absolute/repository/root/reports/assets/<stem> \
     --scratch-dir /tmp/continuity-loom-playtest/<stem>/browser \
     --base-url <baseUrl> \
     --port 0
   ```

   Wait for `session.json`. The guard must block provider-send routes and reject
   all origins except the exact app origin. A guard event is a defect and a
   terminal send attempt, not permission to continue.

6. Drive only the visible UI with `browser-act.mjs`, one visible action per
   command. Use roles, names, labels, and visible text; use CSS only if semantic
   locators fail. Never use hidden controls, runtime evaluation, source-derived
   selectors, direct API calls, saved profiles, or DOM mutation. After every
   consequential action, wait for a settled UI and inspect the new visible
   state.

If Chromium cannot start, follow the one permitted install/retry path. If the
exact loopback bind/connect is denied with `EPERM`, retry that command once
through narrow approval without changing the security boundary. Otherwise use
only the bounded recovery in the terminal section.

## Visible author journey

### 1. Re-enter and verify continuity

- Open the supplied `project_path` through the visible **Open** form.
- Go to **Accepted Segments** before changing state. Confirm that the latest
  accepted item is sequence 1 and visibly ends with Clara deferring action until
  the next staff round and requesting three Daniel-specific facts from Tomás.
- Record whether the endpoint is discoverable, what durable-change reminder or
  unreconciled work is visible, and any mismatch with the prior handoff.
- If the project cannot be opened, the archive cannot prove the endpoint, or
  state is too ambiguous to continue safely after bounded recovery, stop with a
  specific blocked reason. Do not manufacture the continuation state.

### 2. Prepare only the next local segment

Use the visible surfaces in the order that feels natural:

1. Review **Story Configuration** only enough to confirm inherited story
   contract, prose mode, content envelope, and constraints. Do not broaden them.
2. In **Records**, inspect and edit only the entities, facts, knowledge/reveal
   boundaries, physical state, pressures, relationships, objects, locations,
   and affordances necessary for this room-bound exchange. Keep new records
   atomic unless the visible domain clearly calls for a richer cast dossier.
3. Use **Private Notes** only for sincere inert scratch. Any adopted fact,
   knowledge boundary, or directive must also be independently authored into an
   appropriate canonical record or brief field.
4. In **Active Working Set**, include Clara and Tomás in their actual visible
   functions, include Daniel only as an offstage pressure/reference if the UI
   supports that role, and represent approaching staff as offstage pressure.
   Do not place Daniel or staff onstage merely to make them visible to the
   prompt.
5. In **Generation Brief**, fill the universal minimum plus only the fields
   needed to preserve the three exact author-chosen facts, their epistemic
   limits, Clara's agency, the room and time, the pencil/paper/letter blocking,
   and the reversible stop. Before compilation, log each populated
   prompt-facing field's author need and intended observable influence.
6. Save and use visible readiness. Distinguish blockers from warnings and cure
   blockers only through canonical visible controls. Do not bypass validation.

Do not create irrelevant records or populate optional fields for coverage.
Record any imposed work whose purpose the UI does not explain.

### 3. Assistance only when sincere

- **Ideate:** invoke only if the author genuinely needs grounded categories,
  questions, or source-path options before choosing the three facts. It may
  offer possibilities, but the author must independently select and re-author
  any adopted detail. If the facts are already confidently chosen, skip Ideate
  and report that no live author need arose.
- **Record Hygiene:** invoke only if the visible records create plausible
  overlap, staleness, or uncertainty about what should change. Otherwise skip it
  and say why.
- **Segment Reconciliation at entry:** invoke only if the prior report or visible
  continuation reminder says sequence 1 still needs reconciliation.
- **Segment Reconciliation after acceptance:** the new facts, Clara's evidence
  stance, or object/knowledge state may plausibly create durable deltas, so
  inspect this surface if that need actually arises. Adopt nothing automatically;
  manually re-author only correct deltas into canonical visible surfaces.

Never click Generate, any assistance send control, Analyze, Review, or a model
refresh. A single click or recorded provider-request block ends the run.

## Cold prompt evaluations

For the prose prompt and every assistance prompt naturally opened:

1. Before opening the inspector, record the author need and expected useful
   result. Once open, record the visible prompt kind, version, scope, counts,
   readiness, source disclosure, and SHA-256 fingerprint.
2. Use `browser-act.mjs text-file` only while the exact prompt is visibly open,
   writing its complete text to the allocated `/tmp/.../exchange/` path without
   printing it.
3. Dispatch one fresh no-parent-context evaluator with only:

   ```text
   Read exactly the complete prompt in <absolute-prompt-file> and perform it as the sole task.
   Do not inspect the repository, any other file, the parent conversation, or prior responses.
   Return only the output requested by that prompt.
   ```

   If file custody is required, add only the supplied response-file instruction.
   Never send the premise, prior report, desired answer, suspected weakness,
   earlier response, or evaluator expectations. One fresh evaluator serves one
   attempt. If true cold isolation is unavailable, stop with
   `cold-subagent-unavailable`; do not simulate the result in the parent.
4. Assess the untouched output before any manual re-authoring. For prose, judge
   room/time scope, exact three-fact directive, factual specificity and source
   paths, Clara's POV/knowledge/agency, Daniel's offstage coma, presence roles,
   physical handling of pencil/paper/letter, voice/content constraints, the
   reversible stop, unsupported durable change, genericity, and prose quality.
   Treat any claim that the facts prove time travel, recovery, or a waking date
   as a substantive failure. Treat leaving the room, initiating rescue, forcing
   belief, or extending Clara's commitment as boundary failures.
5. Record contract compliance, grounding, unsupported invention, omissions,
   contradictions, adoption/rejection, repair burden, likely layer, verdict,
   and bounded confidence. Keep a prompt-contract failure distinct from a model
   execution failure.
6. If the first prose response is materially poor or malformed, permit exactly
   one retry with the unchanged exact prompt in a different fresh context and
   no critique. Choose the response an author would prefer to edit. If neither
   can become acceptable without replacement-level rewriting, stop with
   `prose-quality-blocker`.

For every deliberately populated prompt-facing Generation Brief field, search
the visible inspector for its distinctive saved value and label. Log `present`,
`absent`, or `unclear`, placement/prominence, response evidence, and one of
`used`, `partly used`, `not compiled`, `not observably used`, `contradicted`, or
`not assessable`. Do not count blank controls, IDs, validation-only settings, or
author-only metadata as ignored prompt content. A one-field counterfactual is
allowed only if an important visibly compiled field appears ignored and the
answer could change a product decision; it stays diagnostic and never enters
the app.

For assistance outputs, count actionable versus no-change/low-value items and
record adoption explicitly. Reconciliation proposals must identify real durable
deltas against current state, paraphrase evidence, use valid targets and shapes,
avoid invented canon, and make the required manual work clear.

## Candidate and acceptance gate

Acceptance is permitted only when all of the following are true:

- the exact visible prose prompt has been cold-evaluated with one or, after a
  material failure, two attempts;
- the selected raw response has been assessed before editing;
- all three facts are the author's choices and remain specificity/source-path
  evidence rather than proof of time travel, recovery, or waking date;
- the prose stays in Clara's room on Christmas Eve 1972, keeps Daniel comatose
  and offstage, and keeps the letter, distance, and commitment under Clara's
  control;
- no rescue logistics begin, belief is not forced, staff do not displace the
  exchange, and the scene ends at the sealed reversible choice;
- the response can be made coherent with no more than ordinary visible editing;
  and
- there have been zero send-control clicks, provider attempts, or guard blocks.

Only then click **Write or paste candidate**, fill it from the temporary response
file, and make ordinary edits through visible controls. Record intervention as
`none`, `light`, `substantial`, or `rewrite`, along with what the author had to
repair. Explicitly accept once.

Immediately visit **Accepted Segments** and visibly verify one new item with
sequence 2 and the intended endpoint. If sequence proof is missing or ambiguous,
do not claim completion; use bounded recovery and then block if necessary.
Inspect the durable-change reminder and perform only the canonical record or
brief updates the author chooses. Accepted prose is bounded evidence, never
canon or prompt authority by itself. Do not begin another generation.

## Evidence plan

- Keep the full prior-report notes, charter, action notes, exact prompts, raw
  responses, candidate prose, settings, routine screenshots, and diagnostic
  scratch under the allocated `/tmp` run root.
- Record visible fact separately from interpretation. For each current finding,
  preserve visible reproduction, expected versus actual, author impact,
  workaround/burden, likely layer, desired visible outcome, confidence, and
  limits.
- Promote only tightly cropped, privacy-safe evidence needed to establish a
  finding or important strength. Useful candidates include the accepted archive
  sequence proof, a readiness state, or a narrowly scoped visible problem. Do
  not retain full prompts, raw responses, candidate prose, a trace, private
  project content, or routine screenshots as durable evidence.
- At safe points, make a light visible check of focus, accessible names,
  label/help association, color-only state, clipping, scrolling, long-form
  readability, and the 1440×900 layout. This is not a systematic WCAG audit.
- Read only the guard's privacy-safe console/page/network failure streams. After
  first recording the visible issue, diagnostics may use a relevant
  accessibility tree, geometry, or DOM fragment. Never inspect request bodies,
  application stores, React state, SQLite, project files, hidden prompt data,
  `.env`, or source.

The durable report retains only privacy-safe counts, prompt fingerprints,
verdicts, adoption/rejection totals, intervention burden, minimal excerpts, and
selected evidence paths. Use only evidence tags actually earned during the run;
do not claim reproduction, independence, pairing, or source confirmation that
did not occur.

## Blockers and bounded recovery

For a probable blocker, stop and record the visible state, intended action,
expectation, and last successful action; capture privacy-safe evidence; inspect
existing guard/console/network streams; then try one safe recovery through a
visible control. Reload once only if state should persist. If still blocked,
make one fresh guarded-browser attempt against the same app and project. Never
restart the app, replace the project, weaken validation, use a direct API/file
edit, or conceal the original failure. Recurrence or unsafe ambiguity ends the
run with a truthful blocked report.

Any send-control click or any entry in `provider-request-blocks.jsonl` ends the
run immediately. Confirm the guard abort and blank credential, set
`status: blocked` and `completion_reason: provider-request-attempt`, record only
the visible action and endpoint, and never retry.

Other terminal blockers include an unopenable/missing continuation project, an
unverifiable accepted endpoint, no visible path to required canonical state,
readiness, prompt inspection, local candidate, acceptance, or sequence proof;
repeated crash/stall/state loss; unavailable cold execution; two unusable prose
attempts; or state too unsafe to continue.

## Report and terminal handling

After ending the source-and-doc-blind browser journey, read root `AGENTS.md` and
`docs/ACTIVE-DOCS.md` only to route the repository report write; do not revise
the sealed charter, expectations, or observations from those documents. Write
the unique prepared `reports/<report-stem>.md` as a cumulative, standalone
report in the exact Final Report Format. It must include all 16 required sections
and all validator-owned table headers, even when a phase says
`Not reached — <specific blocker>`.

Set the report frontmatter from observed receipt/runtime values, with:

- `run_mode: continuation`;
- `prior_report: reports/playtest-the-winter-letter-2026-07-19T022000Z.md`;
- the exact `/tmp` `project_path` and whether it exists at close;
- `status: completed` and `completion_reason: accepted-one-segment` only after
  visible sequence-2 proof, otherwise the specific truthful blocked reason;
- the actual browser, base URL, 1440x900 viewport, prompt-attempt counts, and
  intervention burden;
- `openrouter_send_controls_clicked: 0`, `provider_request_attempts: 0`, and
  `provider_requests_blocked: 0` for a completed run; and
- `cold_first_view_witnesses: 0`, `independent_claim_challenges: 0`, and
  `paired_draw_checks: 0`, because this task requests no method pilot.

Use one stable `F###` namespace. Carry every prior ledger row forward with its
ID and an allowed current status; mark `resolved` only after explicit visible
retest. Add current strengths and problems with evidence basis, and distinguish
observation from interpretation, prompt-contract mismatch from model-output
failure, and product friction from a terminal blocker. The continuation handoff
must make the preserved project locator, latest verified sequence, accepted
endpoint, manual continuity work, unresolved story choices, and next safe local
boundary usable without the scratchpad or raw prose.

Before shutdown, copy only safety counts and app/browser metadata needed by the
report. Request browser-holder shutdown first and wait for exit 0, then request
app-holder shutdown and wait. Record any abnormal exit. Stop only run-owned
processes. Remove session files, empty streams, routine screenshots, and all
uncited or forbidden evidence.

Validate the report once, then delete the temporary prompt/response exchange and
scratchpad as instructed, and validate again. Compare final `git status --short`
with the baseline. Beyond the baseline, the run may leave only its new report
and cited evidence directory; preserve the `/tmp` continuation project for the
next run. Completion requires a passing validator, stopped run-owned processes,
zero forbidden durable material, and status that exactly matches the visible
outcome.
