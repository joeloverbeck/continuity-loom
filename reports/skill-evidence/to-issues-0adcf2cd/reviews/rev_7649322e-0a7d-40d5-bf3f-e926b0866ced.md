# Skill Evolution Review: to-issues

## Authorization
- Gate rule: `friction_recurrence:tool-compatibility` — three independent friction incidents in one symptom cluster
- Trigger event IDs: `evt_44c0ab5a-7ec7-448d-8f6c-93d25e816b67`, `evt_bdb7352e-680d-4568-bdc2-d3bbdd2470a7`, `evt_d2af415e-3ac6-4d90-a8e1-f0fcb09606cf`
- Target before hash: `23d6d5a8e62c9b559bfc09200d555a9fe9dc49801d7e908cadfecb892ecb1003`
- Fresh-session/cooldown proof: threshold session `019f91f1-e67f-75c2-a1c0-881bd9133841`; this review ran in a different top-level session, re-verified under the store lock at `claim`

## Evidence adjudication
- Independence result: confirmed. Three distinct top-level sessions (`237ab214…`, `unavailable`, `019f91f1…`), three distinct task fingerprints, three distinct run groups, three separate days, all non-retrospective, all recorded against the current target hash.
- Confirmed mechanism: the skill's own staged-artifact documentation states shapes that the skill's own validators reject. An author following `references/publication-protocol.md` produces artifacts that fail the gate on first submission, and recovery requires reading validator source. Five divergence sites are implicated:
  1. run-sheet rows shown without the leading/trailing pipes `parseChecklistRows` requires (`/^\|.*\|$/`);
  2. `blockedBySlices` documented as "exact earlier slice titles" while the validator matches `entry.slice`;
  3. `## Blocked by` bullets documented as free prose without stating that every `#N` in a bullet is parsed as a declared tracker blocker;
  4. posted-ledger contents listed without the literal `Breakdown decisions` heading `validateLedger` requires;
  5. manifest fields listed without value shapes (`checklistMapped` must be `"yes"`/`"N/A - <reason>"`; `parent.ledger` must be `{status, commentUrl, bodyFile}`).
- Target ownership: **target defect**. Both the documentation and the validators live inside the skill; the validators are tested and correct, and the documentation understates or contradicts them. No external contract owns these shapes — only the browser-visible checklist item list is external (`docs/agents/issue-tracker.md`) and it is correctly referenced.

## Candidate
- Change hypothesis: stating the enforced shape at each divergence site lets an author produce a gate-passing artifact from the documentation alone, without weakening any gate.
- Files changed in isolated candidate: `references/publication-protocol.md` only. `SKILL.md` untouched; `scripts/` byte-identical.
- Runtime size before/after: 13,125 → 13,677 bytes (+552, +4.2%) on the changed file; skill total 10,532-byte `SKILL.md` unchanged.

## Frozen validation plan
- Risk tier: **high** — the edited reference governs external tracker writes (issue creation, parent label mutation, ledger comments). Plan frozen at `reviews/<id>/frozen-plan.md` before the candidate existed.
- Paired trials: 9 (5 reproduction, 1 adjacent, 2 core regression, 1 safety invariant), each run against the unchanged skill and the candidate by independent agents given neutral doc packs, no diagnosis, no version label, and barred from reading `.claude/skills/`.
- Deterministic checks: exported validators (`validateRunSheet`, `validateChild`, `validateLedger`, `validateWorkingPublicationState`, `validateManifest`) via `grade.mjs`, self-checked against known-good fixtures before any trial ran; plus `node --test` across all three script test files.

## Results
- Current version: T1 FAIL (off-mechanism: backticked item names), T2 PASS, T3 PASS, T4 PASS, **T5 FAIL** (`parent.ledger.status must be posted or skipped`), T6 PASS, T7 PASS, T8 12/12, T9 70/70.
- Candidate version: T1 PASS, T2 PASS, T3 PASS, T4 PASS, **T5 PASS**, T6 PASS, T7 PASS, T8 11/12, T9 70/70.
- Regressions: none material. T8's 1-point delta sits on criterion 6 (trailing content after the authorization sentence) in a section governed by a byte-identical `SKILL.md`; single-sample run variance, not a candidate effect. No gate weakened; `scripts/` byte-identical.
- Decision: **accepted**, with the mixed evidence stated plainly. Only 1 of 5 divergence sites (site 5) reproduced on a fresh case; the candidate resolves it. Sites 1–4 did not reproduce in these synthetic runs, but each is verifiably false or incomplete against validator source and each is named in a trigger event as real-run friction. Under the strictest reading of the frozen rule ("T1–T5 fail on current") the candidate would not pass; it was accepted on the step-7 gate because the reproduced mechanism is resolved, nothing regressed, and no landed statement is false. Growth is outcome-supported at site 5 and correctness-supported at sites 1–4.

## Landing
- Landed: **yes**
- Target after hash: `1b9d808fb81f0a5ec2c6e8bc812a53f842e823744dfe0112ce475966a8451416` (modified: `references/publication-protocol.md`; baseline backed up to `reviews/<id>/pre-land-backup`)
- Final disposition: `resolved_by_change` — terminal outcome `resolved_by_validated_change`

## Notes left for a later evidence cycle
- The composite-checklist component vocabulary friction in `evt_44c0ab5a` was **not** repaired: the protocol already directs the author to let the validator report missing components, which is a designed recovery loop rather than a doc-vs-gate contradiction.
- `land` reported `mirror_status: "broken"`. `.agents/skills/to-issues` is a tracked real directory containing its own divergent `SKILL.md`, not a symlink like its siblings, and has been since commit `6645191`. It predates this review, landing did not touch it, and it was left alone under the no-same-review-expansion rule.
