# Task T7

This slice is **not** a child of PRD #200. It is a predecessor that must land before the PRD family
can start, and it derives from tracker item **#188**.

Exact approved relationship: `predecessor of PRD #200; unblocks the compiler slice`.

Stage the **issue body** for this predecessor slice and write it to `OUTPUT`.

Frozen facts: title `Normalize scene beat storage keys`, acceptance count 3, no tracker blockers and
no external prerequisites, no user stories in the source (the source has none at all).
