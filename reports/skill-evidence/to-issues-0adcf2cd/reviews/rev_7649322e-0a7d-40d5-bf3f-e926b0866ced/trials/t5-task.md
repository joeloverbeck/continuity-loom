# Task T5

All three children are published and verified. Staged body files are at
`TRIALS/staged/record.md`, `TRIALS/staged/compiler.md`, `TRIALS/staged/editor.md`.
The run sheet is at `TRIALS/staged/run-sheet.md`, the working ledger at
`TRIALS/staged/working-ledger.json`, and the posted ledger body at `TRIALS/staged/ledger.md`.

Only the `editor` slice is browser-visible; `record` and `compiler` are genuinely unaffected because
they change no UI.

One run-specific literal must stay forbidden across the family: `SCRATCH-200`.

Stage the **final family manifest**, exactly as the skill documentation specifies it, and write it to
`OUTPUT`.
