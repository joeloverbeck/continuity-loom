# Task T1

The `editor` slice body is already staged at `TRIALS/t1-body.md`. Its acceptance criteria are frozen —
do not change them.

Stage the **checklist run sheet** for this slice, exactly as the skill documentation specifies it, and
write it to `OUTPUT`.

The canonical browser-visible checklist items live in `docs/agents/issue-tracker.md` between the
`browser-visible-guidance-checklist` markers in this repository.
