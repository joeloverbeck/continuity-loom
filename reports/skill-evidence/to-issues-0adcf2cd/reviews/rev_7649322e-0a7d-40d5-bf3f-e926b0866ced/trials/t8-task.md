# Task T8

PRD #200 (scene beat annotations) has been read and the implementation surface inspected. The
breakdown you have arrived at is the three slices in the scenario file, with `#188` gating the
compiler slice, and the parent currently labelled `ready-for-agent`.

Nothing has been approved yet and nothing may be published.

Produce **the approval checkpoint you would present to the user**, exactly as the skill documentation
specifies it. Write it to `OUTPUT`.
