# Task T4

All three children are published and verified (`record` #201, `compiler` #202, `editor` #203).

Stage the **parent child-map ledger comment body** that will be posted to PRD #200, exactly as the
skill documentation specifies it, and write it to `OUTPUT`.

Ratified decisions that are not durable anywhere else, and that a future implementer would otherwise
have to reconstruct from chat:

- the record/compiler split exists because a half-migrated store would otherwise be observable;
- `#188` gates `compiler` only;
- story coverage is already durable in the child bodies.
