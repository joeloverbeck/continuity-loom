# Task T6

The `editor` slice body is staged at `TRIALS/t6-body.md`; its acceptance criteria are frozen.
The `record` slice changes no UI at all — it only adds storage behind the existing record API.

Stage **one checklist run sheet covering both slices**, exactly as the skill documentation specifies
it, and write it to `OUTPUT`.

The canonical browser-visible checklist items live in `docs/agents/issue-tracker.md` between the
`browser-visible-guidance-checklist` markers in this repository.
