# Task T3

Stage the **issue body for the `editor` slice** and write it to `OUTPUT`.

The `compiler` slice has already been published and verified as issue **#202**, so the backward
reference is resolvable now. The `editor` slice has no external prerequisite.

The approving user asked specifically that the dependency be readable to a future implementer: the
`Blocked by` entry should make clear *why* the compiler work has to land first, not just name a number.

Frozen facts for this body: acceptance count 8, story `US3 - As an author I want to annotate scene
beats while editing a scene.`, principles section may be brief. Do not invent extra sections.
