## Source and coordination

#188
predecessor of PRD #200; unblocks the compiler slice

This slice is not a child of PRD #200. It lands before the PRD #200 family starts; the slice titled `Compile scene beat annotations into the prose prompt` consumes the canonical keys established here.

## What to build

One canonical storage key form for scene beats on the story record, applied end to end through the record store and through projects already saved on disk.

Settle the key shape once, before any annotation payload is stored against it: normalize on write, migrate existing local projects on load, and leave a single authority path — no alias table, no dual-read fallback.

Scope stops at key shape. Annotation storage, prompt compilation, and the editor surface are handoffs to the PRD #200 family slices `Store scene beat annotations on the story record`, `Compile scene beat annotations into the prose prompt`, and `Edit scene beat annotations in the scene editor`. No browser-visible surface changes here: no entry point, control, label, or user-visible state moves.

## User stories covered

N/A - source item #188 defines no user stories, and this predecessor delivers no author-facing behavior of its own.

## Acceptance criteria

- [ ] Scene beat keys are written and read in one canonical form through the record store, and `docs/specs/story-record-schema.md` names that form as the authority.
- [ ] A project saved with pre-normalization keys loads, migrates once, and re-saves canonically with no beat data lost or reordered; a legacy fixture test covers the round trip.
- [ ] No alias, shim, or dual-read path for the legacy key shape survives the migration, and the existing golden prompt tests stay green and unedited.

## Blocked by

None - can start immediately

## Principles

`docs/principles/FOUNDATIONS.md` §4.10 and §24 (local-first, user-owned data): migration runs against the author's own project files and loses nothing. §4.4 (deterministic compilation): normalizing keys must not change compiled prompt output. Repository convention: no backwards-compatibility aliases or duplicate authority paths — one key shape wins.
