Understood. I’m rejecting that recommendation and won’t implement anything. The duplication is load-bearing because the offline importer is maintained and released independently from the server, so moving normalization into a server-owned module would put the seam in the wrong place.

Want me to record this as an ADR so future architecture reviews don't re-suggest it?
