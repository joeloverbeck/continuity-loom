# Raw prompt

The core package contains two strict model-output parsers plus shared primitives.
Design a deeper parser module interface that improves locality and testability while
keeping each assistance profile's schema and semantic checks explicit. It is valid
to recommend retaining the current structure if a new interface would be shallow.

Return: a diagnosis of the current module depth, two plausible interface shapes,
a recommendation, an example TypeScript signature, ownership of syntax versus
domain validation, and the tests that should target the chosen interface. Do not
edit files.
