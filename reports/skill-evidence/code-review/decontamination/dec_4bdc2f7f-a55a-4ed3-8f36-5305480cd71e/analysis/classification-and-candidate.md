# Classification decision and candidate hypothesis

The exhaustive one-category classification is retained in `structure-classification-independent.md`; its provenance is independently checked in `provenance-independent.md`, and canonical-owner seams are checked in `ownership-and-duplication-independent.md`.

## Preserved core

- resolved fixed-point and reviewed-HEAD identity, three-dot diff, commit list, and explicit dirty/WIP scope;
- concrete pre-dispatch Standards and Spec authorities, repository-selected domain/ADR routing, confirmed no-spec behavior, and the full judgement-only smell baseline;
- independent Standards and Spec reviewer packets, exact-acceptance/sequence/production-route/nonlocal-proof checks, and the zero-residual output hard stop;
- truthful reviewer terminal/cleanup/recovery custody and the rule that main-agent synthesis becomes fallback;
- visible pre-fix axis reports, no cross-axis reranking, original-fixed-point re-review after fixes, immutable finding identity, accepted-residual honesty, and final-tree proof freshness.

## Distilled or routed material

- Moved the normal `implement` handoff from the always-loaded entrypoint to one conditional `implementation-closeout.md` reference.
- Replaced copied TDD field inventories with a trigger and the canonical `tdd/closeout-evidence.md` owner.
- Kept exact fallback output in its existing conditional owner instead of repeating it in the entrypoint.
- Kept the shared evidence-identity schema in its existing owner and made the entrypoint state only when it is required.
- Kept body generation, manifests, splitting, publication, and exact readback with `implement`; code-review retains only the review overlay and validator route.
- Condensed repeated browser/currentness exceptions into one conditional closeout section without changing the safety rule.
- Updated only the structural contract test so it reads the entrypoint plus its new routed closeout reference; validator behavior and executable helpers are unchanged.

## Uncertain material retained

- The non-empty-diff hard stop is retained. One usage event reports friction for a tracker-only review, but changing that rule would be functional evolution, not simplification of demonstrated behavior.
- Host-specific cleanup dispositions, exact validator literals, the current body-size ceiling, and SQLite snapshot safety remain reachable through their conditional owners because the frozen corpus exercises those branches.
- Fallback and evidence-identity references remain byte-identical in the candidate; deeper cross-skill consolidation is outside this one-target run.

## Single hypothesis

The skill can reduce instruction competition by keeping the two-axis method in `SKILL.md` and routing implementation-closeout schema to one explicit conditional reference. The candidate is accepted only if all seven frozen tasks remain noninferior, deterministic validators stay green, and blind evaluation finds no material regression in core review, exact acceptance, custody, TDD routing, fallback labeling, or browser/backend currentness.

