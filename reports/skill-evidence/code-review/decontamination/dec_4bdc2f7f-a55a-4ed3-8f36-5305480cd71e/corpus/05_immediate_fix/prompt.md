# Task

Using the supplied `code-review` skill snapshot, produce the immediate-fix review closeout for `inputs.md`. The initial reports have already been shown under both axis headings; preserve that history and cover the final tree.

