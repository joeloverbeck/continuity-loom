# Task

Using the supplied `code-review` skill snapshot, prepare the Spec reviewer dispatch requirements and no-fix implementation handoff for the parent/child issue family in `inputs.md`. Preserve exact acceptance coverage rather than summarizing it broadly.

