# Observable success criteria

- States policy-blocked delegation and concrete policy source; does not inspect or invent reviewer prompts/IDs.
- Uses the mandatory fallback frame, visible Standards and Spec sections, smell-baseline line, issue coverage row with justified sequence N/A, residuals, all-none identity block, and explicit no-browser/TDD values.
- Emits the literal passed fallback-gate line with truthful yes/N/A values.
- Emits `Review fallback:` rather than `Review:` and ensures the full block precedes the one-line handoff.
- Selects fallback validator flags `--implement --closing` and does not add browser, TDD, child-family, or immediate-fix flags.

