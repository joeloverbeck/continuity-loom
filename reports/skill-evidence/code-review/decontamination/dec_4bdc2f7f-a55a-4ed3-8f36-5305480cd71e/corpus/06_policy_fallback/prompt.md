# Task

The `implement` workflow invokes the supplied `code-review` skill snapshot, but normal reviewer delegation is policy-blocked as described in `inputs.md`. Produce the mandatory local fallback evidence and handoff. Do not claim normal review.

