# Observable success criteria

- Records the exact structured recovery: interrupted reviewer, partial-output state and sink, fresh completing reviewer/status, and passed output gate.
- Names all three reviewer IDs/statuses and gives unavailable-close proof for each.
- Records final-tree browser freshness for the production route, clean console state, server command/mode, port/PID ownership, restart, API probe, SQLite `.backup`, snapshot source, and expected-state probe.
- Produces the complete current identity inventory with no superseded sweep required beyond the all-none N/A and does not misclassify the interrupted output as main-agent synthesis.
- Selects normal validator browser mode and preserves separate zero-finding axes and the normal `Review:` handoff.
