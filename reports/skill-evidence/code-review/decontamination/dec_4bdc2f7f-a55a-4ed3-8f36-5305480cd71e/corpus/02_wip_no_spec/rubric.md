# Observable success criteria

- Includes committed diff, `git diff --cached`, and `git diff` in the Standards input and explicitly excludes the untracked note.
- Uses the exact `no spec available` route and does not invent or dispatch a Spec review.
- Distinguishes the localhost breach as hard from the smell as judgement-call.
- Preserves fixed-point/dirty-scope facts and reports separate visible axis sections, with the Spec axis explicitly skipped.
- Does not fix the findings or add implementation-closeout machinery.

