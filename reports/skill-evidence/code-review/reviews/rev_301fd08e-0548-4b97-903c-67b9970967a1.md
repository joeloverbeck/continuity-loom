# Skill Evolution Review: code-review

## Authorization
- Gate rule: `ten_use_unresolved` — 15 qualifying uses on the unchanged target with one open contemporaneous incident.
- Trigger event IDs: `evt_40796ead-c1ae-477f-85af-b4972f18ec9e` (`cost` cluster, friction, "Issues 180-183 Standards and Spec review", `retrospective: false`).
- Target before hash: `2967265361ebe8d7d3d27aed4f89d32e7489548b4441dd562d0bf2619ddab3c7`
- Fresh-session/cooldown proof: preflight and claim both evaluated the destination session against threshold session `019f9573-7d0b-7df0-be01-c73e7b97dc71` and authorized; review ID `rev_301fd08e-0548-4b97-903c-67b9970967a1`.

## Evidence adjudication
- Independence result: single trigger event; the rule is use-count based, so no multi-event independence claim was made. The event is a qualifying, contemporaneous, current-hash use. Premises confirmed.
- Confirmed mechanism: the normal-review closeout validators reject field values that already satisfy the skill's stated requirement, and their messages do not name the accepted forms, so the author revises by guessing across repeated validator rounds. Reproduced deterministically:
  - `review-evidence-contract.mjs:validateSequenceSource` gated on a closed, inflection-brittle proof-token alternation. `test` passed but `tests` did not; `proved` passed but `proof` did not; `asserted` passed but `assertion` did not. Cells that plainly named their observing proof — "covered by the four settings tests", "exercised by the presenter regression tests", "proven by the workflow tests", "traced in the request logs", "shown by the sequence proof in the presenter suite" — were all rejected. Its one message named a single example and no vocabulary.
  - `validate-review-normal-body.mjs:concreteSpecSource` rejected `issue #N comment ID <digits>`, the exact form `SKILL.md:51` instructs the author to write, while accepting the strictly less identifying `issue #N comment <digits>`.
- Target ownership: **target defect**, inside the skill directory, in code the skill itself owns. The sequence contract has one canonical home shared by the normal and fallback validators; `.claude/skills/tdd`'s separate sequence-map check was left untouched.
- Not owned by this review: the incident's other half — later-pass sampling-recovery findings and repeated final re-reviews — maps to a rule `SKILL.md:113` already states at its point of use ("keep the original fixed point and re-review each affected axis through final `HEAD`"). No target defect was demonstrated there, so no change was made.

## Candidate
- Change hypothesis: accepting the compliant values and naming the accepted forms in the rejection messages removes the guess-and-revise rounds without loosening what the gates protect.
- Files changed in isolated candidate: `scripts/review-evidence-contract.mjs` (proof-token alternation made inflection-tolerant; single failure message split into an order message and a proof message that enumerates the accepted vocabulary), `scripts/validate-review-normal-body.mjs` (`comment ID` form accepted; Spec-entry message enumerates accepted forms). No prose or workflow file changed.
- Runtime size before/after: 212,656 → 213,437 bytes (+781). All growth is validator logic and the accepted-form enumerations that constitute the diagnosability fix.
- Vacuity guard: only proof-asserting past participles were added (`covered`, `exercised`, `confirmed`, `demonstrated`, `shown`), never bare event verbs such as `check`/`confirm`/`cover`, so a UI step named "confirm" still cannot satisfy the proof half. A sequence with no proof language at all remains rejected.

## Frozen validation plan
- Risk tier: **high** — two widened gates in a closeout contract shared by the normal and fallback validators. Plan frozen at `reviews/<id>/plan.md` before the candidate existed.
- Paired trials: T1 sequence-authoring reproduction, T2 inventory-authoring adjacent case, T3 normal-path core regression, T4 fallback-path core regression, T5 vacuity/safety.
- Deterministic checks: D1 existing suites in a sibling-complete tree; D2 a 20-case frozen verdict matrix (7 flip, 9 safety, 4 preserved); D3 message-diagnosability assertions.

## Results
- Current version: D2 — 6 of the 7 flip cases rejected (F3 already passed incidentally through the substring `test suite`); D3 both false. D1 44/44 code-review, 57/58 tdd (one known pre-existing failure). T1 0 rounds, T2 1 round with doc-hunting recovery.
- Candidate version: D2 — every flip case accepts, all 9 safety cases still reject, all 4 preserved cases still accept; D3 both true. D1 44/44 code-review, 57/58 tdd with the identical pre-existing failure. T1 0 rounds, T2 1 round recovered from the error message itself.
- Regressions: none. T3 and T4 outputs are byte-identical between versions; T5's two vacuity bodies are rejected by both, the candidate differing only by naming which half failed. Post-land verification at the canonical path reproduces 44/44 and the unchanged 57/58 tdd baseline.
- Blind round counts tied on both trials, as the front-loading effect predicts; per the frozen plan the deterministic matrix and the diagnosability pairing were the decisive instruments.
- Recorded honestly: no blind executor happened to attempt the `comment ID` form, so that flip is proven by the matrix rather than by a blind rejection. Both T2 executors instead lost their round to Standards-axis separator and literal-token rules whose messages name no accepted forms — real friction on a surface outside this candidate, left for a later evidence cycle rather than folded in.
- Decision: **accepted**. Validation event `evt_12b6f3ff-2d3f-4ead-b343-6f9071e4909c`, candidate hash `4664a842e65b7c8d6d6b8d6b68e480721be4c27375ec6524774771625b3beec4`.

## Landing
- Landed: yes. Modified `scripts/review-evidence-contract.mjs` and `scripts/validate-review-normal-body.mjs`; `.agents` mirror `ok`; baseline backed up at `reviews/<id>/pre-land-backup`.
- Target after hash: `4664a842e65b7c8d6d6b8d6b68e480721be4c27375ec6524774771625b3beec4`
- Final disposition: `resolved_by_change` (terminal outcome `resolved_by_validated_change`), adjudicating `evt_40796ead-c1ae-477f-85af-b4972f18ec9e`.
