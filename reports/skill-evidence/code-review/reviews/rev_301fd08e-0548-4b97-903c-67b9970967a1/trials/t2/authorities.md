# Authorities this review actually used

**Standards axis.** The repository agent guide `AGENTS.md`, the repository instruction file
`CLAUDE.md`, and the fixed smell baseline that the code-review skill defines.

**Spec axis.** Sibling issues #369, #370 and #371, plus one more authority: the acceptance decision
for #370 was not written in the issue body — it lives in a tracker comment on issue #370 whose
comment ID is 5052198448. That comment is the authority the Spec reviewer actually read, so it
belongs in the inventory alongside the three issues.
