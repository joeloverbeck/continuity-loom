# Skill Evolution Review: code-review

## Authorization
- Gate rule: `ten_use_unresolved` (11 qualifying uses on the current hash with one unresolved open incident).
- Trigger event IDs: `evt_543ac1c9-760f-4216-a626-a4921bd1be0f`.
- Target before hash: `2967265361ebe8d7d3d27aed4f89d32e7489548b4441dd562d0bf2619ddab3c7`.
- Fresh-session/cooldown proof: preflight + `claim` re-derived the gate under the store lock in a
  top-level session distinct from the trigger session (`18e018d7…`) and the threshold session
  (`6b948cda…`); helper appended `review_started`. No child-agent/continuation shortcut used.

## Evidence adjudication
- Independence result: single open friction incident; premises confirmed — qualifying use
  (`qualifying_use: true`, non-retrospective), contemporaneous (recorded 2026-07-22, ~1 day before
  review), hash matches current target, plausible recurring "output" symptom.
- Confirmed mechanism: the normal-body validator extracts each accepted-residual field with a
  line-anchored regex (`fieldPattern` in `review-evidence-contract.mjs`: `^…<label>:…$`, flags
  `im`/`gim`, no `s`), so `Axis`/`Source`/`Rationale`/`Revisit trigger` are only captured when each
  label begins its own line. The per-line constraint (plus a "single-line record is rejected"
  warning and a copy-paste example) exists **only** in `evidence-identities.md` §"Validator-passing
  field examples" — not at any of the three points of use (`SKILL.md:108`,
  `implementation-closeout.md:29`, `fallback-evidence.md:38`), whose inline field enumeration
  ("record each **with** `Accepted residual:`, `Axis:`, …") suggests an inline layout. A
  first-attempt inline record is rejected → reformatting round (the #149 friction).
- Target ownership: **Target compliance defect** (placement/salience) for symptom #1 → proceeds.
  Symptom #2 (post-squash-merge Review-SHA re-anchor) is an inherent Git squash-merge fact owned by
  the publish/merge step, **outside** code-review guidance — noted, not repaired here.

## Candidate
- Change hypothesis: raising point-of-use salience at `implementation-closeout.md:29`
  ("one field per line", replacing the misleading inline "with") reduces first-attempt inline
  formatting without duplicating the canonical example.
- Files changed in isolated candidate: `implementation-closeout.md` only (one sentence). No
  script/validator bytes changed.
- Runtime size before/after: `implementation-closeout.md` 7530 → 7547 bytes (**+17 bytes**, larger).

## Frozen validation plan
- Risk tier: ordinary (doc-only, one file, one sentence). Frozen before the candidate existed at
  `reviews/rev_88e8ca8e-…/frozen-validation-plan.md`.
- Paired trials (each on current + candidate; blind executors given the identical real reading path,
  differing only in `implementation-closeout.md` bytes; not told to run the validator — first
  attempt measured):
  - T1 repro — one Standards accepted residual (2 executors/version).
  - T2 adjacent — two accepted residuals, Standards + Spec (1/version).
  - T3 core regression — no-residual normal path (1/version).
- Deterministic checks: D1 only `implementation-closeout.md` differs (validator byte-identical);
  D2 candidate-formatted residual passes `validateAcceptedResiduals`; validator test suite spot-run.

## Results
- Current version: T1 2/2 first-attempt validator PASS; T2 PASS; T3 says "none", no invented block.
- Candidate version: T1 2/2 first-attempt validator PASS; T2 PASS; T3 says "none", no invented block.
- Deterministic: D1 pass (only the doc differs); D2 pass; `validate-review-normal-body.test.mjs`
  29 pass / 0 fail.
- Regressions: none.
- Decision: **REJECTED.** Behaviorally tied on the reproduction case — the existing
  `evidence-identities.md` example already yields first-attempt compliance for a fresh blind
  population, so the candidate has no mechanism to improve. Being +17 bytes larger and not
  meaningfully clearer, the size/clarity tie-breaker keeps the current skill.

## Landing
- Landed: no.
- Target after hash or unchanged hash: unchanged
  `2967265361ebe8d7d3d27aed4f89d32e7489548b4441dd562d0bf2619ddab3c7` (git shows no change under
  `.claude/skills/code-review/`).
- Final disposition: `candidate_rejected_validation` (terminal outcome
  `candidate_rejected_validation`); trigger `evt_543ac1c9` adjudicated and retired from the active
  set. Validation event `evt_8519e305-973a-4bf7-854f-908536ef9eea`.
