# Frozen paired-trial corpus

Frozen before candidate construction for run `dec_3d978253-2447-44c6-92d9-8b2f16d78014`.

The six tasks are drawn from closed Continuity Loom issues implemented during the TDD skill's active period. Each task preserves the issue's problem shape and governing constraints while omitting audit commentary, prior implementation details, and expected answers.

All trials use the same instruction: load only the assigned skill variant plus the task-local prompt, input, and rubric; do not edit the repository or use the network; return an execution-ready response that can be evaluated from retained text.

Coverage:

1. Common UI/accessibility feature with unchanged domain behavior (#114).
2. Core validation change with fail-closed and recovery invariants (#130).
3. Security/privacy-sensitive cross-package bug fix (#131).
4. Bounded UI guidance change with prompt-freshness constraints (#124).
5. Unusual evidence-only issue where applicability must be judged (#126).
6. Broad regression case spanning core, server, web, persistence, and docs (#96).
