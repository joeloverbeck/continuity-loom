# Round 4 blind evaluation — task 02

## Output A

Output A is substantively strong. It starts at the public compiler/validation seam, specifies the exact right-reason red for an existing but unselected ENTITY, preserves dangling and wrong-kind precedence, covers all three canonical bands, blocks prompt bytes and raw identifiers, exercises recovery through public state seams, verifies rendered send gating and zero provider calls, inventories all other reference roles, keeps storage unchanged, limits production work to the existing reference policy, synchronizes the two named authorities after green, and defines focused plus canonical verification. Its uncertainties are explicit and it refuses to invent band names, diagnostics, fixtures, or policy.

Two shortcomings keep it from being the stronger answer. It does not explicitly record the supplied workflow's pre-command `CONTEXT.md` gate. In addition, its recovery slice permits distributing selecting, repointing, and correcting across the three bands instead of proving each recovery form in every band. That still demonstrates each transition and gives every band full blocked-path coverage, so it is not a severe fail-closed or safety omission, but it is a material reduction in regression confidence relative to B.

## Output B

Output B fully specifies the behavior and preservation matrix. It records `CONTEXT.md` status and active authorities before the first red; proves fixture preconditions; distinguishes a missing-diagnostic red from setup failures; exercises separately named cases for all three bands; preserves exact dangling and wrong-kind diagnostics and precedence; and requires selecting, dangling-reference correction, and wrong-kind correction on an active fixture for every band. It also exhaustively freezes other reference-role posture, verifies no prompt bytes or raw identifiers escape, proves zero provider calls at the public send boundary, checks recovery on the same project state, synchronizes only the two relevant documents, and confines both production and refactor work to the declarative cast-reference policy.

The public send/API tracer is described less explicitly as rendered UI behavior than A's UI-focused slice, but it still verifies the user-visible validation response, absence of leaked prompt/identifier data, provider isolation, and a successful retry through the public boundary. That is not a material omission under the stated task.

No material or severe regression is present in B. No safety omission is apparent.

## Comparison and verdict

**Preferred: B.** B is more faithful to the supplied TDD startup process and gives the more rigorous all-band recovery matrix while preserving the same minimal policy change and fail-closed safety properties.

- **A noninferior to B:** No. A omits the explicit `CONTEXT.md` pre-command gate and permits a materially thinner recovery cross-product.
- **B noninferior to A:** Yes. B retains A's core, send-boundary, confidentiality, provider-isolation, documentation, and non-CAST preservation obligations while strengthening startup and recovery evidence.
