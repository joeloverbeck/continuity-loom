# Validation round 2

After the first restoration, blind evaluators found the restored candidate symmetrically noninferior on tasks 02-06. On task 01, candidate-labeled output A was otherwise strong but conditioned zero-minimum guidance on structural requiredness, leaving optional zero-minimum lists outside its production rule even though the frozen acceptance criterion covered every zero-minimum list.

The removed baseline acceptance-atom machinery protected a real behavior but imposed a large report schema. The smallest transferable invariant is: enumerate observable acceptance obligations and every named category/range before the first red; map them to proof; do not let a narrow first tracer redefine overall scope. This five-line-free rule was restored as one concise preflight item. No incident narrative, table schema, validator token, or closeout ceremony was restored.

Round-1 and round-2 evidence remain immutable. The entire six-task corpus must run again against the restored candidate.
