# Skill Evolution Review: tdd

## Authorization
- Gate rule: `ten_use_unresolved` — ten qualifying uses on the unchanged target with open contemporaneous incidents
- Trigger event IDs: `evt_00025bf4-e9d8-457a-8320-6e065feb672c` (symptom `tool-compatibility`, #178 TDD evidence), `evt_aa234a60-a55f-4b18-976b-4b130944776d` (symptom `cost`, issues #180-183)
- Target before hash: `810a4c9e7311539dce37634ded37e73dfb61e6fb78aabd266b896914b9611395`
- Fresh-session/cooldown proof: threshold session `019f9573-7d0b-7df0-be01-c73e7b97dc71`; this review ran in a different top-level session, verified by the helper under the store lock at claim time

## Evidence adjudication
- Independence result: confirmed. Distinct top-level sessions (`4e6bc226-…` vs `019f9573-…`), materially different tasks (#178 vs #180-183), distinct task fingerprints and run groups, `retrospective: false` on both, both hashes equal to the live target.
- Confirmed mechanism: `validate-tdd-closeout-body.mjs:350-357` gated each ledger row on `hasObservedOutcome && hasOutputDetail`, where `hasOutputDetail` accepted only `exit <digits>` or `<digits> <unit-noun>`. Counts in the shapes real runners print were therefore rejected despite being output-derived — `passed 21 of 21; failed 0` (the exact incident string), `21 passed, 0 failed`, `passed 21/21`, `3 passing`. The single emitted message named neither which of the two conjuncts failed nor any accepted form, so the author had to read validator source to recover. Independently reproduced in blind trial r5.
- Target ownership: **target defect**. The failing script ships inside `.claude/skills/tdd/`. The second trigger's diffuse field friction (compact rows, pre-red status, external-boundary proof, proof-server preflight) was not traced to a specific wrong behavior and was not repaired here.

## Candidate
- Change hypothesis: the check enforces a narrower rule than it advertises; accepting the count shapes runners actually emit, and naming the accepted forms in the message, removes the false rejection and the forced source read without loosening what the gate exists to block.
- Files changed in isolated candidate: `scripts/validate-tdd-closeout-body.mjs` (one hunk)
- Runtime size before/after: 210,397 → 211,088 bytes (+691)
- One mechanical candidate defect was found before any behavioral trial and corrected once under the step 7 exception: Mocha's `3 passing` failed the *outcome-word* conjunct (`passing` was absent from `hasObservedOutcome`), which the first candidate had not touched. The complete frozen suite was rerun afterwards.

## Frozen validation plan
- Risk tier: high (the change alters a validation gate protecting durable verification evidence) — escalated to five paired trials
- Paired trials: T1 fresh reproduction (five-command transcript, blind); T2 adjacent runner vocabulary (Mocha + silent tsc, blind); T3 core regression, prose claims must still reject; T4 core regression, documented happy path and unrelated row checks; T5 safety boundary, widening must open no hole
- Deterministic checks: D1 full validator suite on both versions in sibling-complete trees; D2 20-fixture accept/reject matrix; D3 message must name accepted forms while keeping the substring pinned by two existing tests

## Results
- Current version: D1 58 tests / 57 pass / 1 fail. D2: rejects `passed 21 of 21; failed 0`, `21 passed, 0 failed`, `3 passing, 0 failing`, `passed 21/21`. D3 fails — message names no accepted form. Blind T2 (r5): 1 rejection round and a forced read of validator source lines 350-356; verbatim `3 passing` had to be rewritten to `3 tests`. Blind T1 (r1, r2): 0 rounds.
- Candidate version: D1 58 / 57 / 1 — identical, same single pre-existing failure (`guidance carries sink, snapshot, exactness, and shared closeout contracts`, which expects `docs/robustness-testing.md` in SKILL.md; unrelated to this mechanism and deliberately left alone). D2: all four shapes accepted; T3/T4/T5 verdicts byte-identical to current across 16 fixtures. D3 passes. Blind T2 (r6): 0 rounds. Blind T1 (r3, r4): 0 and 1 rounds.
- Regressions: none. The change is strictly additive on the accept-set — no input the current version accepted is now rejected. Prose claims (`focused tests passed`, `all good`, bare `passed`) still reject; `21 tests` without an outcome word, `exit code` without a digit, `<n> tests passed`, bare `not applicable`, and vacuous `passed 0 of 0` all still reject.
- Known imprecision left unrepaired: the new message lists the five original outcome words and does not mention the newly accepted `passing`/`failing`. It under-advertises rather than misleads — a body using `passing` passes — and correcting it after behavioral trials would mean improvising a second candidate, which the protocol forbids.
- Decision: **accepted**. The blind round-count metric tied overall (current 1 failing round across 3 runs, candidate 1 across 3), because capable executors front-load: r3 and r6 state they read the validator source before drafting, which collapses that metric on either version. The decisive evidence is elsewhere and is not a rewording: the candidate changes verdicts on four canonical runner-output shapes the current version wrongly rejects, with gate integrity provably unchanged; and the directly paired diagnosability case shows r5 on current forced into a source read where r4 on candidate recovered in one round from the message's examples alone.

## Landing
- Landed: yes
- Target after hash: `ffcebc988a4b83eec8a90456886feadce857d3ebc7715c01a262fe312e43ee93` (mirror `ok`; baseline backed up at `reviews/rev_82a89933-…/pre-land-backup`)
- Post-land verification on the live target: suite 58 / 57 / 1 with the same single pre-existing failure; fixture matrix 0 mismatches
- Final disposition: `resolved_by_change` (terminal outcome `resolved_by_validated_change`)

## Out of scope, recorded not acted on
Blind executors repeatedly surfaced adjacent issues that are not the implicated mechanism and were not repaired: `closeout-evidence.md` states no accepted-form vocabulary and its single example conflates count with exit code; `Run count` is unverifiable from the evidence artifact and any positive integer passes; nothing cross-checks the ledger against the compact table's green command, so an incomplete ledger still exits 0; the executable-command allowlist and whole-cell backtick rule are undocumented. These become actionable only if a real skill use records them as evidence.
