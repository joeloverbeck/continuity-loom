# Skill Evolution Review: tdd

## Authorization
- Gate rule: `friction_recurrence:cost` — three independent `friction` incidents on symptom key `cost`, all on the live target hash.
- Trigger event IDs: `evt_b70c9daa-6f04-4c8f-80ea-706adf4ef85c` (#186), `evt_8045b842-88a8-4694-968b-41ebc9ed6387` (#190), `evt_59fefaa9-90b4-4ece-bd86-e33232687000` (#196).
- Target before hash: `ffcebc988a4b83eec8a90456886feadce857d3ebc7715c01a262fe312e43ee93`
- Fresh-session/cooldown proof: verified by the helper under the store lock at `claim`; threshold session `019f9f97-3784-7580-b405-06c1eab0bea9` is not the review session, and the preflight evaluated every authorization term before any semantic read of the target.

## Evidence adjudication
- Independence result: confirmed. Three distinct top-level sessions (`019f968d…`, `019f9b2e…`, `019f9f97…`), three distinct task fingerprints and run groups; none is a retry, continuation, or duplicate account. All three carry `qualifying_use: true`, `retrospective: false`. The threshold-crossing event is contemporaneous (2026-07-26). All three trigger hashes equal the live target hash.
- Confirmed mechanism: the TDD closeout validator's acceptance predicates gate on brittle literal token sequences and whole-field keyword scans rather than on the requirement they enforce, so semantically-correct, fully-conforming evidence is rejected; and several rejection messages name no accepted form or name the wrong one, so the author cannot recover from the message and must reread validator source or guess-and-rerun. Four instances, each named by trigger `evt_59fefaa9` and corroborated by the other two, were reproduced against the live target before any candidate existed (`raw/pre-freeze-repro.txt`):
  - **D-A** `ADRs/principles/docs status` — an unconditional whole-field `conflict|blocked|unresolved` scan ran before every disposition branch, so a fully resolved `aligned because …` or `N/A because …` that merely mentions those words was rejected with a message asserting a conflict that did not exist. Acute in this repo, where *blocker* and *conflict* are domain nouns.
  - **D-B** `Acceptance atom map` / `Acceptance sequence map` — required the literal bigram `all row(s)`; `every row`, `each row`, and `all acceptance-audit rows` were rejected. The skill's own guidance had documented the brittleness as a workaround rather than fixing it.
  - **D-C** evidence-identity block and TDD review-fix map cells — a whole-field `TODO|TBD|pending|unknown|<…>` scan rejected legitimate citations containing those tokens, and the message named no offending token.
  - **D-D** compact-row coverage-only red evidence — required a contiguous literal phrase, and on failure emitted a message about the unrelated expectation-rewrite case.
- Target ownership: **target defect**. All four predicates live in `.claude/skills/tdd/scripts/`, which the target owns; the sibling skills consume this contract rather than defining it. No other owner was edited.

## Candidate
- Change hypothesis: replace each brittle literal gate with one that tests the requirement it enforces, and make the three misleading messages name the accepted form — without loosening what the gate refuses.
- Files changed in isolated candidate: `scripts/validate-tdd-closeout-body.mjs` (D-A restructure, D-B universal-quantifier predicate, D-C token-naming errors, D-D targeted message), `scripts/tdd-evidence-contract.mjs` (new `unresolvedEvidenceToken` citation-aware scan, `hasCoverageOnlyExistingBehaviorEvidence`), `scripts/validate-tdd-closeout-body.test.mjs` (8 regression tests), `closeout-evidence.md` (one sentence: the documented `all rows` workaround replaced by the now-true rule).
- Runtime size before/after: runtime guidance 94,972 → 94,979 bytes (+7, a corrected sentence). Scripts 69,543 → 72,710 bytes (+3,167) for four behavior repairs plus the shared citation-aware helper and the improved messages.

## Frozen validation plan
- Risk tier: **high** — the change edits the target's own gate script, which enforces a convention consumed by `implement` and `code-review`, and whose loosening could admit unresolved authority conflicts or unresolved placeholders into a tracker closeout. Escalated to five paired trials.
- Paired trials: T1 fresh reproduction (resolved-conflict authority + coverage-only row); T2 adjacent case (acceptance-audit map phrasing + placeholder-like artifact name); T3 unrelated core regression (ordinary red-first body); T4 second core regression (browser evidence-only + review-fix map); T5 safety (genuinely unresolved authority conflict + genuinely undetermined identity). Executors were independent agents holding one unlabeled skill copy with test files stripped, given only the raw authoring facts — no diagnosis, no intended repair, no version label.
- Deterministic checks: **D1** a 30-case verdict matrix frozen before the candidate existed (10 defect-flip, 10 safety, 10 preserved-pass), run against both versions; **D2** the tdd script suite in a sibling-complete temp tree, both versions; **D3** `npm run lint` after landing. Per this store's recorded lesson, D1 was designated the decisive instrument and blind round-counts the diagnosability comparison.

## Results
- Current version: D1 — 10 defect cases REJECT, 10 safety REJECT, 10 preserved-pass ACCEPT. D2 — 58 tests, 57 pass, 1 pre-existing failure (`guidance carries sink, snapshot, exactness…`, which expects `docs/robustness-testing.md`; not a regression and deliberately not touched). Blind: T1 1 round; T2 **2 rounds, executor forced into validator source**, independently identifying the same `pending` collision; T3 0 rounds; T4 0 rounds; T5 REFUSED.
- Candidate version: D1 — all 10 defect cases flip to ACCEPT, all 10 safety cases still REJECT, all 10 preserved-pass still ACCEPT. D2 — 66 tests, 65 pass, the same single pre-existing failure; 8 added tests all pass. Blind: T1 1 round; T2 **0 rounds**; T3 0 rounds; T4 1 round; T5 REFUSED. D3 lint passed after landing.
- Regressions: none. Cross-validating all eight passing blind bodies on both validators shows every current-authored body also passes on the candidate; the sole divergence is `t2-tern`, the truthful first-attempt body citing the real committed file `reports/pending-review-notes.md`, which fails on the current validator and passes on the candidate. The T4 candidate-side round was spent on an external/cold-proof check the diff does not touch, and that body passes on the current validator too — authoring variance, not a version effect.
- Decision: **accepted**. The candidate resolves the implicated mechanism on the reproduction cases, is noninferior on every protected behavior, changes no safety verdict, and is materially better rather than merely reworded. Left unrepaired and recorded as future evidence: the authority check still treats a past-tense `open question` narrative as an unresolved state, which cost both T1 sides one round; it is outside the four demonstrated defects and was not expanded into this candidate.

## Landing
- Landed: yes
- Target after hash: `f477e3d312f6f3234b4c5ef44b175584a1b61bbd8651142d3c88473adc2ad5cb` (from `ffcebc98…`; `.agents` mirror verified; baseline backed up at `reviews/rev_c08a5b3e-c82c-42b6-8958-8c636dba5e3c/pre-land-backup`). The landed target reproduces the validated candidate's verdicts on all 30 frozen cases and the same 66/65/1 suite result. Not committed to Git.
- Final disposition: `resolved_by_change` — terminal outcome `resolved_by_validated_change`.
