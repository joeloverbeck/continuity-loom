Source: `reports/readiness-audit.md`

Selected section: `Eligibility routing`

Verdict: The omission claim was valid: the status skill lacked one blocked-state routing bullet.

Context: The audit of the durable readiness report shifted into the explicitly requested narrow repair of `.claude/skills/status/SKILL.md`.

Finding: Fixture `blocked-active` was valid, but the routing table had no matching row.

Evidence: The local helper omitted `blocked-active`; inspection confirmed the valid fixture and missing routing row. After repair, `rg -n blocked-active .claude/skills/status/SKILL.md` returned line 44 and the local status fixture reported one blocked entry.

Inspected authorities: `docs/process.md`; `.claude/skills/status/SKILL.md`.

Tracker overlap: None.

Existing prep artifact status: N/A — none found.

Rejected/no-op alternatives: Treating the omission as expected behavior.

Rejected operations: Rewriting the helper; the routing-only edit was sufficient.

Baseline: moved - `.claude/skills/status/SKILL.md` had no `blocked-active` match (`rg -n blocked-active` returned none) -> `blocked-active` appeared at line 44 and the fixture reported one blocked entry; reconciliation: both file and fixture were reread after the edit; rollback: restore the prior file from the review backup.

Touched resources and read-back: Added one routing bullet to `.claude/skills/status/SKILL.md`; exact `rg` read-back returned line 44, and the local status fixture reported one blocked entry.

Recommendation: Retain the narrow repair and monitor the next census.

Out of scope: Helper implementation and evidence-store contents.

External research: Skipped because repository-local evidence was sufficient.

Supporting skill result: N/A.

Freshness: Helper and file read-back were taken after the mutation against the same working tree.

Freshness/external research: Post-mutation helper and file read-backs are current to the same working tree; external research was skipped because repository-local evidence was sufficient.
