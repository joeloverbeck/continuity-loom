Context: Audit of `reports/readiness-audit.md` shifted into the explicitly authorized narrow repair of the status skill’s blocked-state routing.

Source: `reports/readiness-audit.md`

Selected section: `Eligibility routing`

Verdict: The report’s omission claim was valid: the status skill lacked one routing bullet for the valid `blocked-active` state, and the authorized routing-only repair corrected it.

Evidence: Before the repair, the local helper omitted fixture `blocked-active`, inspection confirmed the fixture was valid, and `rg -n blocked-active` found no routing-table match. After the repair, `rg -n blocked-active .claude/skills/status/SKILL.md` returned line 44 and the local status fixture reported one blocked entry.

Inspected authorities: `docs/process.md`; `.claude/skills/status/SKILL.md`.

Tracker overlap: None.

Existing prep artifact status: N/A; none existed.

Rejected/no-op alternatives: Treating the omission as expected behavior was rejected by the valid fixture and missing route. Rewriting the helper was rejected because the routing-only edit was sufficient.

Mutation and read-back: Added one routing bullet to `.claude/skills/status/SKILL.md`; the exact `rg` read-back found `blocked-active` at line 44, and the local status fixture reported one blocked entry.

Baseline: moved - resource `.claude/skills/status/SKILL.md`, state `blocked-active` absent with proof `rg -n blocked-active` returning no match -> `blocked-active` present at line 44 with one blocked fixture entry; reconciliation: the file and helper were read back after the mutation in the same working tree, with rollback available by restoring the prior file from the review backup.

Recommendation: Retain the narrow repair and monitor the next census.

Out of scope: Helper implementation and evidence-store contents.

Supporting skill result: N/A.

Freshness/external research: The helper and file read-back were taken after the mutation against the same working tree. External research was skipped because repository-local evidence was sufficient.
