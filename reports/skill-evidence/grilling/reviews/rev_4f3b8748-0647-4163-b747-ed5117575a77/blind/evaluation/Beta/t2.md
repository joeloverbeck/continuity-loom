Decision: RATIFIED label taxonomy -> use existing label `workflow` rather than create `workflow-state`; rationale: it matches the repository vocabulary and avoids a duplicate taxonomy.

Decision: RATIFIED update scope -> update only issue #42; rationale: issue #43 concerns a different queue.

Context: GitHub issue-label cleanup for issue #42.

Finding: Issue #42 lacked the existing `workflow` label.

Evidence: The pre-write issue read showed labels `bug` and `triage`; the label inventory showed that `workflow` exists.

Rejected operations: Creating `workflow-state` or editing issue #43.

Baseline: moved - issue #42 labels `bug, triage` -> `bug, triage, workflow`; reconciliation: an exact post-mutation issue read verified the final labels; rollback: remove `workflow` if read-back failed.

Touched resources and read-back: Added `workflow` to issue #42; the exact issue read then showed `bug, triage, workflow`.

Recommendation: No further label changes.

Out of scope: Issue body, milestone, assignee, and issue #43.

Freshness/external research: GitHub state was refreshed immediately before and after mutation; external research was skipped because live tracker state was authoritative.
