Decision: RATIFIED label choice -> use existing label `workflow` rather than create `workflow-state`; rationale: it matches the repository vocabulary and avoids a duplicate taxonomy.

Decision: RATIFIED issue scope -> update only issue #42; rationale: issue #43 concerns a different queue.

Context: GitHub issue-label cleanup for issue #42.

Source: GitHub issue #42.

Finding: Issue #42 lacked the existing `workflow` label.

Evidence: The pre-mutation issue read showed labels `bug` and `triage`; the label inventory showed that `workflow` already existed. The post-mutation exact issue read showed `bug`, `triage`, and `workflow`.

Inspected authorities: GitHub issue #42, issue #43, and the repository label inventory.

Tracker overlap: Issue #43 was checked and excluded because it concerns a different queue.

Rejected operations: Creating `workflow-state` and editing issue #43.

Mutation and read-back: Added `workflow` to issue #42; the exact issue read then showed labels `bug, triage, workflow`.

Baseline: moved - resource issue #42, labels `bug, triage` by exact issue read -> labels `bug, triage, workflow`; reconciliation: the exact affected issue was read back after mutation, with rollback available by removing `workflow` if verification failed.

Recommendation: Make no further label changes.

Out of scope: Issue body, milestone, assignee, and issue #43.

Freshness/external research: GitHub state was refreshed immediately before and after mutation. External research was skipped because live tracker state was authoritative.
