Context: Explicitly requested rename of local branch `wip/cache` to `fix/cache-order`.

Finding: The working tree was clean and the destination branch name did not exist locally.

Evidence: `git status --short` was empty; `git branch --list fix/cache-order` returned no match; the current branch was `wip/cache`. After the rename, `git branch --show-current` returned `fix/cache-order` and the working tree remained clean.

Rejected operations: Force-deleting another ref and pushing remotely; neither was requested.

Mutation and read-back: Renamed the local branch to `fix/cache-order`; `git branch --show-current` returned `fix/cache-order`, and the working tree remained clean.

Baseline: moved - resource local branch ref, current branch `wip/cache` and destination absent by exact Git reads -> current branch `fix/cache-order` with a clean working tree; reconciliation: the branch and worktree were read back after mutation, with rollback available by renaming `fix/cache-order` back to `wip/cache`.

Recommendation: Push only if separately requested.

Out of scope: Remote refs, commits, and pull requests.

Freshness/external research: Local refs and the worktree were read immediately before and after mutation. External research was skipped as irrelevant.
