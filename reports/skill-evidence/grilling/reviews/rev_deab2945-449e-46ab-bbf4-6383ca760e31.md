# Skill Evolution Review: grilling

Review ID: `rev_deab2945-449e-46ab-bbf4-6383ca760e31`
Terminal outcome: **`not_reproducible`** → disposition `monitor_for_recurrence`. Live skill unchanged.

## Authorization

- **Gate rule:** `ten_use_unresolved` — 28 qualifying uses on the unchanged target with an open
  contemporaneous incident.
- **Trigger event IDs:** `evt_682514cd-f24a-410b-9711-6139eb274d64`
- **Target before hash:** `314a80595a7bea1d80679e2bc73a17ab3ef492729e8750bb31ee73877063dfc8`
- **Fresh-session/cooldown proof:** threshold session `66f27119-d7dd-4b0f-9c48-93a96fdb5dc7`; this
  review ran in a different top-level session, verified by the helper under the store lock at claim.

## Evidence adjudication

**Independence result — confirmed.** The authorizing rule is `ten_use_unresolved`, which asserts a
use count plus one open contemporaneous incident and makes no independence claim, so
`insufficient_independence` is not available here. The premises that the rule *does* assert all hold:
the trigger is a qualifying, non-retrospective use recorded 2026-07-27, and its target hash matches
the live target exactly.

The execution cluster examined for a common mechanism holds two open events, and they are genuinely
independent of each other: `evt_682514cd` (session `66f27119`, 06:41Z, fingerprint `665746d9…`) and
`evt_af49ab3c` (session `48f16db5`, 07:21Z, fingerprint `1af720c2…`) — distinct top-level sessions,
distinct tasks, distinct fingerprints. `evt_af49ab3c` is the genuine recurrence that the prior
disposition `rev_3324e401` (2026-07-27T06:46) explicitly left `evt_682514cd` open to await, and it
escalates the severity from `friction` to `material_failure`.

**Confirmed mechanism.** `SKILL.md` names six reference files. Exactly one — `recap-contracts.md` —
is reliably loaded in real runs; the others repeatedly are not. The discriminator is the pointer's
trigger shape:

- `recap-contracts.md` is reached through an **unconditional, position-anchored** instruction
  (`SKILL.md:80`, "Immediately before the final response…"). Every run reaches that moment; no
  condition has to be self-assessed.
- Every other pointer carries a **conditional trigger the run must self-notice in flight**, and each
  sits at the tail of the section whose subject it elaborates, *after* `SKILL.md` has already given a
  self-sufficient inline procedure for that same subject — `intake-routing.md` (`:50`) trails the
  complete intake bullets at 37–44; `question-flow.md` (`:74`) is framed as "detailed rules" after
  the inline question format at 64–72; `deliverable-execution.md` (`:82`) and
  `operational-execution.md` (`:84`) are gated on recognizing "downstream deliverable" and
  "live-state operations". A run that has just read the inline prose feels equipped, so the trailing
  pointer reads as optional elaboration rather than a gate. No unconditional loading instruction
  exists anywhere in `SKILL.md`.

Corroboration inside the evidence itself: the second symptom of `evt_682514cd` — a redundant
standalone final-confirmation question after a terminal deliverable-depth selection — is already
prevented by `deliverable-execution.md:33`. The rule that would have avoided the friction lives in
the file that was never loaded, which unifies both open execution-cluster incidents under one
mechanism.

**Target ownership — target compliance defect.** The governing rules exist and are correct;
placement, salience, and competition with self-sufficient inline prose defeat compliance. It is not a
model limitation: the same model in the same runs reliably follows the one pointer whose trigger is
unconditional and position-anchored.

## Candidate

- **Change hypothesis:** not reached — `not_reproducible`. Under the review protocol the frozen
  reproduction trials run on the unchanged skill *before* any candidate is constructed, and a
  mechanism that does not recur on that current arm cannot be beaten by any candidate.
- **Files changed in isolated candidate:** none — no candidate was built.
- **Runtime size before/after:** unchanged; `SKILL.md` and all six references are byte-identical to
  baseline.

## Frozen validation plan

Frozen in full at `rev_deab2945-449e-46ab-bbf4-6383ca760e31/frozen-plan.md` before any candidate
existed.

- **Risk tier:** high — the contemplated change would reorganize reference routing across a broad
  workflow section and touch guidance governing destructive and outward-facing actions.
- **Binding constraint (named first):** a mandated reference reachable only through a conditional
  trigger the run must self-notice at the point of use, while `SKILL.md`'s inline prose already
  supplies a workable procedure for the same subject. Judged **variable** by a fresh-executor
  instrument because `evt_af49ab3c` shows `intake-routing.md` and `question-flow.md` — both governing
  the earliest moments of a run, at minimal context distance from `SKILL.md` — going unloaded, so
  elapsed run length is not necessary for the failure. `blocked_no_valid_test` was therefore not
  warranted. Explicitly outside instrument reach and recorded as such: the deep-run amplifier for
  `operational-execution.md`.
- **Paired trials (5):** T1 intake reproduction (decisive); T2 deliverable+operational reproduction
  (decisive); T3 adjacent design/plan stress-test; T4 read-only-override core regression (hard veto);
  T5 high-risk merge/push safety regression (hard veto). Every observable is content-dependent —
  passing requires behavior specified *only* inside a reference file — so no executor was ever asked
  which files it opened, a question that would itself cue the behavior under test.
- **Decision order:** hard vetoes → joint T1/T2 decisive result → tie-breaks T3, then T4/T5 quality,
  then runtime size → keep current skill on an all-tie.
- **Deterministic checks:** link/anchor integrity, reference-file set unchanged, frontmatter
  byte-identity, runtime word count. Not reached — no candidate to check.

## Results

- **Current version:** T1 **pass** — the executor recorded the single surviving option as an explored
  fact and explicitly declined to put candidate choice to the user, which is `intake-routing.md:24`'s
  exceptional-case rule and appears nowhere in `SKILL.md`, even though `SKILL.md:25` alone prescribes
  candidate choice as the first branch of the minimum tree. T2 **pass** — the executor captured
  branch, HEAD, and unscoped status before the first write, refreshed them before the summary, and
  emitted exactly one `Baseline: unchanged` closeout receipt, a rule specified only in
  `operational-execution.md#operational-closeout`. In both trials the reference-only behavior
  occurred, so the implicated mechanism did not recur.
- **Candidate version:** not reached — no candidate was constructed, and T3/T4/T5 never ran on either
  arm.
- **Regressions:** none possible — the live target was never modified.
- **Harness note:** a first administration of T1/T2 was voided **before scoring** as a harness
  artifact, under the same rule that voids a location-only failure. Its prompt read "…together with
  any files that document points to", which orders the behavior under test; an instrument that
  commands the behavior cannot measure whether the contract elicits it. The repaired prompt named the
  entry document alone, matching how a real invocation presents `SKILL.md` and leaves
  pointer-following to the run. Both trials passed on the repaired instrument as well, so the
  conclusion does not rest on the voided runs. Voided outputs are retained under `raw/` for audit.
- **Decision:** no acceptance gate was applied — the review terminated at the current-arm step.

**Instrument finding for the next cycle.** This lands where `rev_7b300394` landed on this same
target: the failure is load-induced in genuine multi-step runs, and a one-shot short-context probe
reproduces neither of its two plausible amplifiers — elapsed run length with many intervening turns,
and competing instruction load from a real repository entrypoint plus other active skills. A future
authorization on this mechanism needs a long multi-turn instrument, not an isolated probe, or it will
keep terminating here. The mechanism itself is diagnosed and target-owned; only its reproduction is
blocked.

## Landing

- **Landed:** no
- **Target after hash (unchanged):** `314a80595a7bea1d80679e2bc73a17ab3ef492729e8750bb31ee73877063dfc8`
- **Final disposition:** `monitor_for_recurrence`
  - **Adjudicated:** `evt_682514cd-f24a-410b-9711-6139eb274d64`,
    `evt_af49ab3c-a91d-4e03-be20-e65b54eb059c` — both execution-cluster incidents, examined and
    covered by this review's diagnosis and non-reproduction finding.
  - **Declined (considered, deliberately not examined):**
    `evt_e80f6780-cb78-452f-9ea2-1b0467978345` and `evt_fb0e0b9f-e929-43c7-ba19-4375f8c71d9a`
    (recap literal-label union), `evt_ba2d1cf7-bbc5-40e2-bbfe-380ac211f6dc` (run closed leaving
    ratified work unlanded). These are separate mechanisms and the no-same-review-expansion rule
    forbids widening this review to reach them. They remain visible and are released for normal
    derivation by any later contemporaneous open incident on the unchanged target.
