Use the supplied `grilling` skill definition and the frozen material in `input.md`.

User request:

> Sharpen this implementation sequence for delegated execution. I want a recommendation on seams, dependency order, verification gates, and where a delegated agent should stop rather than improvise. Do not implement it.

Respond exactly as you would at this turn. This is an analysis-only simulation: no network, repository edits, deliverable creation, or external mutation.
