This is a design/plan stress-test. The proposal’s three layers belong in one design scope: the peer taxonomy defines each operator’s change target and grounding arity; state-aware eligibility prevents inert records from qualifying; and minimum bundles make the promised distinctness operational. Approving only one or two layers would leave the central defect intact. Dormancy also belongs in this scope as an assignment policy, because removing it from the taxonomy without specifying its replacement would change slate behavior ambiguously.

The main pressure-test finding is that `Commit at a Cost` risks recreating the umbrella it replaces. It remains a peer only if its dominant endpoint is narrowly defined as **one actor becoming committed now**, with one named cost; any downstream plan failure, relationship turn, or option loss must remain a consequence rather than the operator’s identity. With that boundary, the nine moves can share one type discipline. Operator-id compatibility, status-edge semantics, reveal authorization, fixed-order bias, dormant wording, and semantic-evaluation limits are dependent downstream branches, not reasons to split the package.

Question 1: package boundary. My recommendation (Recommended): approve the taxonomy, typed fail-closed eligibility, minimum grounding bundles, and dormancy reassignment as one spec-scoped design package with separately testable normative layers, conditional on tightening `Commit at a Cost` to the present-commitment boundary above. Do you agree?
