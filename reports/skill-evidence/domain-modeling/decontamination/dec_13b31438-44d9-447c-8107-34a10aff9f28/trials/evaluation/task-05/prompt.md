Use the supplied `domain-modeling` skill definition and the frozen material in `input.md`.

You are acting as a supporting skill inside a design-grilling run. The caller asks:

> Check the resolved discussion for domain-model consequences and return the exact result I should include in my recap. Do not create an artifact unless the discussion actually earned one.

Respond exactly as you would at this turn. This is an analysis-only simulation: do not access the network or modify the repository. If the skill would write a file, include the exact proposed file contents or patch in your response.
