# Skill Evolution Review: implement

## Authorization

- Gate rule: `friction_recurrence:cost`.
- Trigger event IDs:
  `evt_d28b5081-e520-4890-97da-31521b9a6c6e`,
  `evt_d79a8ce2-0c3e-483e-91b0-f59f0d5bfa69`, and
  `evt_8aac62c1-3320-4b54-a681-aaf318f44a1d`.
- Target before hash:
  `877e856dc058ef9b54b4510c81701b2da094f7430f9e4c8827bad5599bb5d303`.
- Fresh-session/cooldown proof: different-session proof; threshold session
  `019f968d-6f21-7f22-8f64-e9f235c1c8da`, review session
  `019f96c7-7e1f-79a3-b260-4243763346eb`.

## Evidence adjudication

- Independence result: confirmed. The three prospective qualifying uses came
  from distinct top-level sessions and materially different implementation
  tasks; the threshold-crossing event was contemporaneous and all three target
  hashes matched the claimed live target.
- Confirmed mechanism: closeout guidance supplied parent and sibling-family
  scaffold routes but no truthful standalone route, while several
  validator-sensitive first-draft field contracts appeared only after the
  large template. This could route one issue through sibling wording or defer
  exact factual/token repair until validators rejected the first body.
- Target ownership: target compliance defect in the `implement` skill's
  closeout routing, scaffold helper, and placement of field guidance.

## Candidate

- Change hypothesis: add a manifest-checked standalone scaffold mode and move
  the three demonstrated field contracts to first-pass composition, while
  preserving sibling, parent/child, exactness, and mutation gates.
- Files changed in isolated candidate:
  `SKILL.md`, `references/closeout-templates.md`,
  `scripts/build-closeout-body.mjs`, and
  `scripts/build-closeout-body.test.mjs`.
- Runtime size before/after: full target directory 392,159 bytes before and
  395,861 bytes in the isolated candidate.

## Frozen validation plan

- Risk tier: high.
- Paired trials: five — standalone reproduction, validator-safe first draft,
  adjacent sibling issue set, parent/child regression, and fail-closed tracker
  mutation.
- Deterministic checks: all four target script test files on current and
  candidate; candidate standalone manifest/scaffold command; exact output-token
  rubrics; independent paired-output evaluation.

## Results

- Current version: passed all five behavioral rubrics. Its standalone route was
  manual rather than scaffold-backed. Target tests passed 80 checks with one
  pre-existing `.agents` symlink assertion failure.
- Candidate version: materially better on the standalone route and preferred
  on first-draft field wording; tied on parent/child and mutation safety.
  Target tests passed 81 checks with the same pre-existing `.agents` symlink
  assertion failure. The standalone command produced an `ok` size plan and the
  expected truthful heading.
- Regressions: in the sibling trial, the candidate executor gave the correct
  issue-set command and heading but omitted the explicit no-parent statement
  required by the frozen rubric. The unchanged version preserved it.
- Decision: rejected. High-risk acceptance required noninferiority on every
  protected core behavior, so the sibling regression barred landing.

## Landing

- Landed: no.
- Target after hash or unchanged hash:
  `877e856dc058ef9b54b4510c81701b2da094f7430f9e4c8827bad5599bb5d303`
  unchanged.
- Final disposition: `candidate_rejected_validation`.
- Landing verification: not reached — `candidate_rejected_validation`.

Terminal outcome: `candidate_rejected_validation`.
