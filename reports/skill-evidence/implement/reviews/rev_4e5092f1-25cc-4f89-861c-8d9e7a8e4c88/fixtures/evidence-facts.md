# Evidence facts

- The proof server ran with `npm start`.
- It had no watch or reload mode.
- PID 4242 owned `127.0.0.1:4173`.
- The server was restarted after the final build.
- `GET /api/health` returned HTTP 200 with `{"ok":true}`.
- Every acceptance row names its authoritative atoms and concrete proof
  surfaces.
- The pre-red record lives in the stable GitHub issue #901 closeout comment,
  under the `TDD evidence` heading.
- Recorded line order shows that preflight appears before the first red command.
