Backend process currentness: server command `npm start`; watch/reload mode: none; process/port ownership: PID 4242 owned `127.0.0.1:4173`; restart proof: the server was restarted after the final build; expected API field/behavior probe: `GET /api/health` returned HTTP 200 with `{"ok":true}`.

Acceptance atom map: all rows name their authoritative atoms and concrete proof surfaces.

Pre-red evidence reference: stable durable sink GitHub issue #901 closeout comment, under the `TDD evidence` heading; recorded line order shows that this preflight record precedes the first red command.
