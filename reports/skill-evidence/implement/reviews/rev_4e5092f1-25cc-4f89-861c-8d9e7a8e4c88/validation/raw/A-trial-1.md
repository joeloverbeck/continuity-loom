There is no truthful generated closeout-scaffold route for this scope in the supplied skill version. The builder supports only `--parent <issue>` and `--scope issue-set --anchor <issue>`: the first would invent a parent role for #901, while the second emits a sibling-issue-set heading. Generate the exact acceptance audit if desired, but draft the standalone closeout body manually from the documented compact audit and closeout-preflight shapes.

Closeout heading:

`Implementation closeout for #901`

Parent/sibling applicability:

`Parent PRD and sibling issue applicability: N/A because #901 is a standalone issue; no parent PRD or sibling issue is in scope.`
