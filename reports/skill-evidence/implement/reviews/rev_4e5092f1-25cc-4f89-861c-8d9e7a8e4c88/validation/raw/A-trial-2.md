Backend process currentness: server command `npm start`; no watch/reload mode; process/port ownership: PID 4242 owned `127.0.0.1:4173`; restart proof: the server was restarted after the final build; expected API behavior probe: `GET /api/health` returned HTTP 200 with `{"ok":true}`.

Acceptance atom map: all rows in the acceptance audit list authoritative atoms and concrete proof surfaces.

Pre-red evidence reference: durable sink GitHub issue #901 closeout comment, under the `TDD evidence` heading; recorded line-order chronology shows that the preflight precedes the first red command.
