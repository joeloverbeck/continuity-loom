# Skill Evolution Review: implement

## Authorization
- Gate rule: `friction_recurrence:cost` — three independent friction incidents in the `cost` symptom cluster on the unchanged target.
- Trigger event IDs: `evt_1b0b6bb4-4c29-4cb3-9d3d-04cc0cf87c54` (#199 + #195 closeout), `evt_5b770ac7-0c9b-4139-a9ab-ee9a4d8d19b1` (#204), `evt_5b2531a1-fe59-4023-9c6e-8e1ba4577fab` (#209).
- Target before hash: `ccd33707b0610dcf17d9f7b45115e7e7f22822b752f8b289e8b1d91cff6ce016`.
- Fresh-session/cooldown proof: threshold session `019fa42d-1fb6-7f11-a46b-23e83ea5d19c`; this review ran in a different top-level session, and the helper re-verified that term under the store lock at `claim`.

## Evidence adjudication
- Independence result: confirmed. Three distinct top-level sessions (`019fa0d0…`, `019fa2da…`, `019fa42d…`), three materially different tasks with distinct `task_fingerprint` and `same_run_group` values, all `qualifying_use: true` and `retrospective: false`, all carrying the current target hash. No retries, continuations, or duplicate accounts of one event.
- Confirmed mechanism: two acceptance predicates in `scripts/validate-closeout-body.mjs` rejected **truthful** closeout evidence and emitted messages that named no accepted form, forcing revision cycles on a mutation-blocking gate.
  1. The evidence-identity refresh scan tested the raw `Current evidence identities:` / `Superseded evidence identities:` / `Superseded-token sweep:` text for `TODO|TBD|pending|unknown` on a plain word boundary. Real repository identifiers embed those words as one component of a larger token (`reports/corpus/tasks/05-pending-source.md`, `task-08-unknown-future-contract/rubric.md`, `/tmp/pending-fixture.sqlite`), so they were unpublishable in their own identity category. Unlike its already-repaired sibling scan on satisfied audit rows, it honored no backtick/quote citation escape — the very remedy the skill's references teach — and its message named neither the offending token nor any repair.
  2. The verification-command ledger required the result cell to contain `passed|failed|blocked|unavailable|not applicable`. The repo's own gates do not print those words: `npm run typecheck` yields `exit 0; no errors`, `npm run lint` yields `0 errors, 0 warnings`, vitest yields `N passing`, a build yields `succeeded`. The message ("must contain an observed pass/fail/block result") enumerated nothing.
- Target ownership: **target defect**, wholly inside `.claude/skills/implement/scripts/validate-closeout-body.mjs`. Not routed anywhere; no other owner's files were read or changed.
- Honest limitation: the three incident narratives record the symptom class ("repaired validator-exact evidence fields", "repeated validator-driven formatting corrections", "repeated scaffold and validator revisions across TDD and review evidence") but carry no `evidence_refs` and name no field. The mechanism was therefore established by reproduction from the current target, not by tracing the specific runs — and it was then independently rediscovered, unprompted, by both blind current-arm executors, who also reproduced the incidents' recorded `workaround_taken` shape.

## Candidate
- Change hypothesis: if the two predicates accept truthful evidence and their messages enumerate accepted forms, the recurring revision cycles disappear without weakening the gate.
- Files changed in isolated candidate: `scripts/validate-closeout-body.mjs`, `scripts/validate-closeout-body.test.mjs`. Diff at `rev_5f41929f-…/candidate.diff`.
  - Identity scan: strip cited literals, then match a standalone-word predicate `(?<![\w./-])(?:TODO|TBD|pending|unknown)(?![\w./-])`; message now quotes the offending token and states both repairs.
  - Verification result: widened `observedResult` vocabulary (`passing`, `failing`, `succeeded`, `ok`, `exit <n>`, `<no|N> errors/warnings/failures`) gated by a new `vacuousResult` guard so a zero-work run is still not evidence; message enumerates every accepted form.
  - Consolidation: `citedLiteral` and the plain `unresolvedWord` hoisted to one canonical home; the audit-row scan now reuses them with **byte-identical behavior**.
- Runtime size before/after: 275,064 → 276,375 bytes (+1,311, +0.48%), confined to the two predicates, the vacuity guard, and the two enumerating messages. Growth is a proven missing capability — the diagnosability win *is* the message text — and could not be had by replacement alone.

## Frozen validation plan
- Risk tier: **high** (edits acceptance predicates in a mutation-blocking validation gate). Plan frozen at `rev_5f41929f-…/frozen-plan.md` before any candidate bytes existed.
- Binding constraint: a truthful body whose real evidence values collide with a brittle predicate — fully variable by the instrument, and independent of run length, so the trial set can express it.
- Paired trials (5): T1 reproduction (blind), T2 adjacent capability (blind), T3 unrelated core regression (blind), T4 safety/fragile fixtures (deterministic), T5 core regression suites (deterministic).
- Deterministic checks: D1, a 20-case accept/reject matrix (10 flip / 10 safety / 5 preserved) declared **decisive before the candidate existed**, precisely because prior reviews in this skill family proved blind round-count collapses to a tie when executors front-load the validator source. Hard vetoes V1–V5 (no safety leak, no preserved regression, no suite regression, no out-of-scope edit, mandatory vacuity guard).

## Results
- Current version: D1 **0/10 flips**, 10/10 safety rejected, 5/5 preserved accepted. D2 176/178. Blind T1 **1 round**, T2 **2 rounds**, T3 0 rounds. Both current-arm executors were forced into the validator source and shipped a workaround that published the true path outside its identity category.
- Candidate version: D1 **10/10 flips**, 10/10 safety rejected, 5/5 preserved accepted. D2 179/181. Blind T1 **0 rounds**, T2 **0 rounds**, T3 0 rounds; neither executor opened the source.
- Regressions: none. D2's two failures are pre-existing and identical on both arms (the locational `Codex implement adapter …` test, which also fails at the canonical live path, and the known `guidance carries sink, snapshot, exactness …` failure in the tdd suite); neither is in scope. Cross-validation control: every current-authored blind body also passes on the candidate; the only divergences are the two candidate-authored bodies that publish a true identifier in its own identity category.
- Decision: **accepted**. The decisive instrument was not tied, so the tie-break order never bound.

## Landing
- Landed: **yes**. Mirror status `ok` (`.agents/skills/implement` symlink). Backup at `rev_5f41929f-…/pre-land-backup`.
- Target after hash: `94b5c3dbcceaa99d42ec65670bf76241459e51633ef0e7ddf8ec70b63efe6027`. Post-land verification at the canonical path: 50/50 tests, D1 identical to the candidate arm.
- Final disposition: `resolved_by_change` (terminal outcome `resolved_by_validated_change`). Adjudicated the three trigger events; declined `evt_ac6bc79e`, `evt_2dcda0e4`, `evt_9acb2652` as considered-but-unexamined.

## Left for future evidence, deliberately unfixed
- The satisfied-audit-row scan still requires backticks for an identifier embedding one of the four words (D1 case S10). It was frozen as a safety case and kept byte-identical; its message already teaches the remedy. If it recurs as a real incident, it becomes evidence then.
- `references/closeout-templates.md` documents the backtick escape without noting the field it applies to — both current-arm executors were misled by it. Not touched: this review repaired the validator, and no-same-review expansion forbids widening into the docs.
