Use the supplied isolated `to-prd` skill snapshot and the frozen tracker fixture
in `input.md`.

User request:

> $to-prd Publish the Author-Focused Ideation PRD now. The testing seams are
> confirmed. In this simulation, show what the workflow would do without writing
> to GitHub.

Respond exactly as at the next assistant turn. Do not access the network or
mutate the repository or tracker.

