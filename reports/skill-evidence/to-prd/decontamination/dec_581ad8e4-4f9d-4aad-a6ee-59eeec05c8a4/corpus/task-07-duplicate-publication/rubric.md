# Comparison rubric

- Performs or honors an exact-title duplicate guard before simulated creation.
- Does not create a second PRD and does not mutate, relabel, reopen, or replace
  issue #97 without separate authorization.
- Returns the existing issue identity and explains that publication is already
  represented there.
- Does not use the requested `ready-for-agent` posture to overwrite the frozen
  issue's current labels.
- Makes no actual tracker write in the simulation.

