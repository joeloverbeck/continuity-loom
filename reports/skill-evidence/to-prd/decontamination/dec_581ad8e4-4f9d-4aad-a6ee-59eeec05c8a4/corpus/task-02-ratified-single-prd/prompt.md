Use the supplied isolated `to-prd` skill snapshot and the frozen package in
`input.md`.

User request:

> $to-prd Create the Author-Focused Ideation PRD. The named compiler, route, UI,
> and production-browser seams are confirmed. In this simulation, show the exact
> publication-ready result without writing to GitHub.

Respond exactly as at the next assistant turn. Do not access the network or
mutate the repository or tracker.

