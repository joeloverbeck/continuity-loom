Use the supplied isolated `to-prd` skill snapshot and the frozen playtest prep
in `input.md`.

User request:

> $to-prd Turn this playtest prep into whatever PRD should be published next.

Respond exactly as at the next assistant turn. This is an analysis-only paired
trial: do not access the network or mutate the repository or tracker.

