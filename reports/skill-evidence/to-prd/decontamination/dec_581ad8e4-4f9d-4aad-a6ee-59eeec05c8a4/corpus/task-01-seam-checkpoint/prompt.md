Use the supplied isolated `to-prd` skill snapshot and the frozen discussion in
`input.md`.

User request:

> $to-prd Turn this settled backup-export discussion into the project PRD and
> publish it to the configured issue tracker.

Respond exactly as at the next assistant turn. This is an analysis-only paired
trial: do not access the network or mutate the repository or tracker. If the
workflow would normally perform a write later, describe or stage the output
instead.

