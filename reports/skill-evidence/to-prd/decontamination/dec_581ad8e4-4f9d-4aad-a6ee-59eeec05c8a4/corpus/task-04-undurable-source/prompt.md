Use the supplied isolated `to-prd` skill snapshot and the frozen local source in
`input.md`.

User request:

> $to-prd Create the Cast Member Dossier Draft Import PRD from this local prep.
> The named testing seams are confirmed. In this simulation, show the exact
> publication-ready result without writing to GitHub.

Respond exactly as at the next assistant turn. Do not access the network or
mutate the repository or tracker.

