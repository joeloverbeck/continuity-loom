Use the supplied isolated `to-prd` skill snapshot and the frozen two-PRD program
in `input.md`.

User request:

> $to-prd Publish the two ratified PRDs in the agreed order. Both sets of testing
> seams are confirmed. In this simulation, produce the two exact issue bodies,
> label decisions, and the sequence comment without writing to GitHub.

Respond exactly as at the next assistant turn. Do not access the network or
mutate the repository or tracker.

