# Task 04 blind evaluation

| Rubric criterion | A | B | Comparative assessment |
| --- | --- | --- | --- |
| Complete PRD without treating the untracked local path as durable authority | Pass. Supplies a complete, publication-shaped PRD and never cites the local path. | Pass. Supplies a complete, publication-shaped PRD and never cites the local path. | A is more implementation-ready: it spells out stable rejection reasons, schema/vocabulary reuse, pre-mutation planning, UI outcomes, recovery, and detailed test coverage. |
| Explicit source posture: temporary source summarized, not cited | Pass. The response and body explicitly say the temporary source was summarized rather than cited and explain that it is unavailable at the publication ref. | Pass. The response and body explicitly say the temporary source is summarized, not cited. | Tie on compliance; A gives slightly more context without asserting the path as authority. |
| User-initiated and no-network behavior | Pass. Copy and paste are explicit local actions, the author invokes the external LLM, and the app makes no provider call. | Pass. Copy/import is author initiated, local, and makes no provider call. | Tie. |
| Pure parse-map-report seams | Pass. Defines a pure core pipeline with extraction, parse, independent validation/mapping, deterministic reporting, and table-driven seam coverage. | Pass. Defines pure core extraction, parsing, field validation, mapping, deterministic reporting, and seam coverage. | A is marginally clearer about the pre-mutation import plan and stable reasons. |
| Per-field validation and partial usefulness | Pass. Explicitly validates fields independently, maps valid present fields, leaves omitted fields untouched, and skips invalid entries with reasons. | Pass overall. The solution and implementation decisions specify field-by-field validation and valid-present mapping. | A is clearer. B's User Story 6 says malformed JSON and invalid fields are reported "without mutating my form," which could be read as making any invalid field block valid peers; its implementation section resolves the ambiguity, but A never introduces it. |
| `entity_id` protection | Pass. Excludes, rejects, and repeatedly proves that `entity_id` cannot change in create, edit, or linked-entity modes. | Pass. Excludes and rejects `entity_id` and preserves identity across all named modes. | A is slightly more explicit; no substantive difference. |
| Overwrite confirmation | Pass. Differing non-empty values enter an exact-field confirmation list and apply only after explicit confirmation; cancellation restores the prior form. | Pass. Each differing non-empty field requires exact-field confirmation, with no bundled confirmation; cancellation is lossless. | B is slightly stronger on per-field confirmation granularity, though both satisfy the criterion. |
| Ephemeral draft/report state | Pass. Keeps pasted response, report, provenance, and imported draft in ephemeral component state and excludes them from persistence and downstream surfaces. | Pass. Keeps pasted text, parsed candidates, reports, confirmation state, and unsaved values ephemeral and excludes them from persistence and downstream surfaces. | Tie. |
| Explicit-save canon boundary | Pass. Existing Save/Create is the sole continuity-authority gate; importing never saves or relinks automatically. | Pass. Explicit author save is the only transition to canonical Cast Member data. | Tie. |
| Labels only after body validation | Pass for this simulation. The response supplies `enhancement` and `ready-for-agent` with a complete body, settled decisions, confirmed seams, and an explicit publication posture contingent on live publication checks. | Pass for this simulation. The response supplies the same labels with a complete body, settled decisions, and confirmed seams. | A gives a somewhat stronger readiness rationale. Deterministic body validation remains a separate check and was not used in this judgment. |
| Does not block solely because the source is undurable | Pass. Produces the publication-ready PRD and explicitly summarizes the temporary source. | Pass. Produces the publication-ready PRD and explicitly summarizes the temporary source. | Tie. |

## Verdict

**A is better.** Both outputs are adequate and satisfy every rubric criterion. A wins narrowly because its body is more precise about independent per-field success, the pre-mutation plan, stable rejection reasons, lossless recovery, schema alignment, visible states, and corresponding test obligations. B remains publication-adequate, but its otherwise well-specified partial-import behavior is briefly blurred by the statement that invalid fields are reported "without mutating my form."

Neither output introduces a material regression or a safety regression. B is not unsafe; its ambiguity is resolved elsewhere in its own body. Deterministic validation and false-citation scanning are separate from this blind qualitative comparison and were not treated as evaluation evidence.
