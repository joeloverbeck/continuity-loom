interface:
  display_name: "To Issues"
  short_description: "Break approved plans into tracker issues."
  default_prompt: "$to-issues Break PLAN_SPEC_OR_PRD into independently grabbable tracer-bullet issues, then wait for approval before publishing."
policy:
  allow_implicit_invocation: false
