{
  "name": "@loom/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build --workspace @loom/core && tsc -p tsconfig.json",
    "typecheck": "npm run build --workspace @loom/core && tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "pretest": "npm run build --workspace @loom/core",
    "test": "vitest run"
  },
  "dependencies": {
    "@loom/core": "0.0.0",
    "fastify": "^5.6.2",
    "zod": "^4.1.13"
  },
  "devDependencies": {}
}
