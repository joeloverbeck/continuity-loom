# Enforce the Node baseline declared in package.json `engines` (>=24).
# With this, `npm ci`/`npm install` errors on an unsupported Node version
# instead of only printing an EBADENGINE warning — locally and in CI alike.
engine-strict=true
